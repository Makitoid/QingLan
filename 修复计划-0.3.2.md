# 青蓝 0.3.2 功能批次计划（agent-native）

- 日期：2026-09-23　分支：`dev-0.3.x`　基线：后端 `pytest` 307 passed / 1 skipped、前端 `npm run build` 0 错（实测于本文件落盘时）
- 来源：用户验收反馈 7 条 + 2 张截图（admin 教师详情的只读名单卡 / 教师管理列表）
- 执行方式：按 F 编号实施，**每个功能性更新一个 commit**（见文末「提交计划」）；每条 commit 提交前先过它自己的验收
- 全局约束：
  - 前端禁止硬编码色，一律主题令牌（`useTheme()` 的 `t.colorXxx`），暗色模式逐页走查
  - 后端业务错误一律 `raise APIError(status, CODE, 中文)`；错误码英文大写常量风格
  - 管理端说明文字是纯 JSX，写 `**` / `#` / `- ` 都不会生效（F2 的根因）
  - 《开发文档.md》末尾变更历史是 30k+ 字符单行：回写时用行号区间定位编辑，禁止整行重写/grep 全文
  - 提交由用户执行（或明确批准后执行）；commit 信息沿用 `feat(范围): 0.3.2 F1-1 — …` 风格
  - 不 push、不打 tag：云端只在 main 打整合 tag + Release，dev 线不推

## 已确认的决策（用户答复，不再讨论）

| # | 决策 |
|---|---|
| 1a | admin 教师详情的名单 = **该教师可教组别里的全部学生**（层 1 成员并集），用**矩阵**呈现，单元格式 `小明(ID:01)` |
| 1b | 教师端**取消「从班级拉学生」**，进页面直接就是全部可教学生；教师在其上**自建子分组** |
| 1c | 子分组**同时用于发布受众**（新建场次可选定子分组，不再是全部名单） |
| 1d | 「按学生 ID 添加单个学生」保留，且该操作**写入审计日志** |
| 1e | **组别即名单，立即移除**：管理员撤销某组后该组学生即时离开教师名单；但要给教师**弹出说明为什么组别被撤销** |
| 4 | 弱密码审核取**中等档**：≥8 位 + 字母与数字至少各 1 + 拒绝全同/长递增串 + 常见弱密码表（约 60 条） |
| 5 | 审计日志**仍是独立页面**；设置项放进改名后的「系统设置」；**系统设置里关掉审计日志则不显示该页面** |
| 6 | 采「**新增逐学生按题调分页**」方案 |
| 7 | 临近结束提醒用**页内横幅**（不弹窗） |

## 编号对照

| F 编号 | 本文小节 | commit |
|---|---|---|
| F1 名单模型重构 + 子分组 + 发布受众 | B1 | F1-1 ~ F1-4（四条） |
| F2 着重号未渲染 | A1 | 1 |
| F3 教师列表冗余「详情」按钮 | A2 | 2 |
| F4 密码小眼睛 + 弱密码审核 | A3 | 3 |
| F5 系统设置 + 审计可配置与导出 | B2 | F5-1、F5-2（两条） |
| F6 多题场次逐题调分 | A4 | 4 |
| F7 学生端「即将结束」 | A5 | 5 |

## 现状关键代码（已逐一读文件核实）

- 层 3 名单唯一写者：`backend/app/api/teacher.py:486-513`（`bind` / `bind_from_class`）、`:629`（`unbind`）
- 名单是受众的唯一来源：`backend/app/services/stats.py:13-18` `bound_students()`，被 `stats.py:60`（总览）、`stats.py:112`（逐学生）、`teacher.py:516`（学生列表）复用
- 学生端可见性：`backend/app/api/student.py:79-90` `_bound_teacher_ids()`，用于 `:152` 列表与 `:163` 详情
- admin 名单计数与反查：`admin.py:202`（名单学生数）、`:224`、`:239-245`（只读名单接口）、`:557`（学生→教师反查）
- 审计：表 `models.py:166-184`、写入 `services/audit.py:17`、查看 `admin.py:673`、页面 `pages/admin/AuditLogPage.tsx`、路由 `App.tsx:89`、导航 `Layout.tsx:36`
- 设置：单例行 `models.py:187-205` `SiteSetting`（`CheckConstraint("id = 1")`），写入口 `admin.py:788-809`，页面 `pages/admin/SettingsPage.tsx:125`，导航标签 `Layout.tsx:37`
- xlsx 能力已在：`backend/requirements.txt:10` `openpyxl>=3.1`；样板 `services/export.py:70-133`；前端下载 `api/client.ts:207-254` `downloadBlob`
- 调分链路：`AssignmentStudents.tsx:203-210` → `stats.py:152` 的 `last_submission_id` → `SubmissionDetail.tsx`
- 密码：后端 `core/security.py:80-91` `validate_new_password`（唯一权威）、前端镜像 `components/passwordPolicy.ts:23-35`、7 个 `type="password"` 输入框分布在 `Login.tsx:84-92`、`ChangePassword.tsx:130-157`、`Account.tsx:109-135`

---

# Batch A — 无数据库迁移（F2 / F3 / F4 / F6 / F7）

## A1（F2）可教组别说明的 `**` 没渲染

**根因**：`pages/admin/TeacherDetail.tsx:271` 把 `**能从哪些组里**` 当普通 JSX 文本写进 `<Caption1>`，Markdown 语法不会被解析。项目里 `react-markdown@9.0.1`（`package.json:21`）只被 `components/Markdown.tsx` 用于题面（`student/ProblemDetail.tsx`），管理端说明文字一律不走它。

**改法**：**不引入 Markdown 渲染**（为一句说明挂渲染器不划算）。改为内联 `<Text weight="bold">能从哪些组里</Text>`，删掉字面 `**`。文案随 B1 一并重写。

**顺带约束**：在 `开发文档.md` 的前端规范一节补一句「管理端说明文字是纯 JSX，Markdown 语法不生效」，避免复发。

## A2（F3）删掉教师列表的「详情」按钮

`pages/admin/TeacherList.tsx:172` 的 `详情` 按钮与姓名列链接（`:154`，同一条 `navigate('/admin/teachers/${item.id}')`）重复。**删按钮，保留姓名链接**作为唯一入口；操作列只剩「重置密码 / 启用|停用」。

## A3（F4）密码小眼睛 + 弱密码审核

**小眼睛**

1. 新建 `frontend/src/components/PasswordField.tsx`：`Field` + `Input`，用 Fluent v9 `Input` 的 `contentAfter` 槽放一个 `Button appearance="subtle"`，图标 `Eye24Regular` / `EyeOff24Regular`，`type` 在 `password`/`text` 间切换，`aria-label` 为「显示密码」/「隐藏密码」。props：`id / label / value / onChange / placeholder / autoComplete`。
2. 替换全部 7 处 `type="password"`：`Login.tsx:84-92`（1）、`ChangePassword.tsx:130-157`（旧/新/确认 3）、`Account.tsx:109-135`（3）。
3. 登录页 `autoComplete="current-password"`，改密页新密码 `autoComplete="new-password"`。

**弱密码审核（中等档）**

4. `backend/app/core/security.py:80-91` 内扩写 `validate_new_password`，保持错误码 `422 PASSWORD_POLICY` 不变（前端已原样显示 message）：
   - 保留：长度 ≥ `config.PASSWORD_MIN_LENGTH`、不含空格、不等于 用户名/姓名/旧密码/`DEFAULT_INITIAL_PASSWORD`
   - 新增：`has_letter and has_digit` 必须同时成立 → 「密码必须同时包含字母和数字」
   - 新增：`len(set(pw)) <= 2` 拒绝 → 「密码不能只由少数几种字符重复组成」
   - 新增：连续（升/降）码点 ≥5 的串拒绝 → 「密码不能使用连续字符」
   - 新增：`WEAK_PASSWORDS`（模块级 `frozenset`，约 60 条常见弱密码，比较时 `new_password.lower()`）→ 「密码过于常见」
5. 前端镜像：`components/passwordPolicy.ts:23-32` 补同样的四条，`:35` `PASSWORD_RULES_TEXT` 同步文案。**后端仍是唯一权威**，前端只做预校验。
6. 生效范围：`validate_new_password` 只被 `api/auth.py:38-39` 调用，而强制首登改密与自助改密共用 `ChangePassword.tsx`，因此**两条路径自动同等生效**，符合 PW-03「全角色统一」的既定决策。
7. 不动 `schemas.py:213` 的 `min_length=8`（与规则重复但无害），避免 Pydantic 层抢先报错绕过中文文案。

## A4（F6）多题场次只能调一道题

**根因（已确认，不是渲染 bug）**：`AssignmentStudents.tsx:203-210` 的「查看 / 调分」指向 `item.last_submission_id`，而它在 `services/stats.py:152` 是**该生该场次全部提交里时间最大的一条**（`max(mine, ...)`，`mine` 未按 `problem_id` 分组）。多题时其余题目在 UI 上没有任何入口；`problemCell`（`:52-56`）渲染的每题得分是纯文本，也不可点。后端 `teacher.py:387-391` 的 `assignment_students` 也不接受 `problem_id`。

**新增逐学生按题调分页**

1. 后端 `GET /api/teacher/assignments/{aid}/students/{sid}/problems` → `list[StudentProblemScoreOut]`，字段：`problem_id / seq / title / full_score / submission_count / latest_submission_id / score / manual_score / effective_score`。逐题有效分复用 `services/scoring.aggregate_scores`（与 `stats.py:129-132` 同一口径），最新提交按 `(submitted_at, id)` 取。
2. 新建 `frontend/src/pages/teacher/StudentScores.tsx`，路由 `/teacher/assignments/:id/students/:sid`（`App.tsx:81` 附近注册）。每行一题：题号+标题（链接到 `/teacher/problems/{pid}`）、满分、提交次数、系统分、教师调分、有效分，行内 `NumberInput` + 保存/撤销（复用 `setManualScore`，`api/index.ts:393`），以及「查看最新提交」跳到现有 `SubmissionDetail`。
3. `AssignmentStudents.tsx`：每题得分列（`:199-201`）与「调分」列（`:203-210`）都改为跳新页；`last_submission_id` 保留在响应里（总分口径不变），但不再作为唯一入口。
4. 删除页面底部冗余返回：`SubmissionDetail.tsx:236-238`（`navigate(-1)` 的「← 返回」）。保留顶部 `:112-114`「← 返回逐学生表」，并把目标改成新的逐学生按题页。
5. 改名：`AssignmentStudents.tsx:110` 列头 `查看提交 / 调分` → `调分`；`:206` 链接文字 `查看 / 调分` → `调分`。同步 `开发文档.md:680`、`部署操作指南.md:570`。
6. 测试：`backend/tests/test_import_export.py:823`（`test_drill_link_uses_latest_submission`）与 `:928`（`test_last_submission_is_max_utc_time`）断言的正是旧的单指针行为，需改为断言新端点逐题返回；新增一条「多题场次每题都能取到自己的 latest_submission_id」用例。

## A5（F7）学生端「即将结束」

1. 后端 `backend/app/api/student.py:141`：`state` 由二元扩为三元 —— `ended`（now > end）/ `ending`（end - now ≤ 3 小时且未结束）/ `ongoing`。阈值走 `config`：新增 `ENDING_SOON_HOURS = 3`。时间比较沿用现有 UTC 串字典序（坑 9）。
2. `backend/app/schemas.py` `StudentAssignmentOut.state` 与 `frontend/src/api/types.ts:404` `AssignmentState` 同步为 `'ongoing' | 'ending' | 'ended'`。
3. Badge 三态：`student/AssignmentList.tsx:59-70` 与 `student/AssignmentDetail.tsx:37-46`（两处逻辑当前逐字重复，一并改）。`ending` = 红色，沿用项目里已有的红 Badge 令牌（`AssignmentStudents.tsx:190` 的 `colorPaletteRedForeground1` + `colorPaletteRedBackground2`），并补 `className="ql-badge-status"`（`index.css:112`）与其他状态徽标对齐。
4. 页内横幅：`StudentAssignmentList` 顶部（`PageHeader` 之后）加一条 `MessageBar intent="error"`，列出所有 `state === 'ending'` 的场次与剩余时间。学生登录后 `HomeRedirect`（`App.tsx:42`）默认就落到 `/student/assignments`，所以这条横幅等价于"登录即提示"，且不会每次刷新都弹窗。
5. 已知边界，本次不动：`student.py:153` 用 `start_time <= now` 过滤，未开始的场次对学生不可见，因此不存在「未开始」徽标。

---

# Batch B — 含数据库迁移（F1 / F5）

先建 `backend/migrations/versions/0006_audit_settings.py`（B2，小），再建 `0007_roster_and_subgroups.py`（B1，大）。

## B1（F1）名单模型重构 + 教师子分组 + 发布受众

### 数据模型（migration 0007）

```
teacher_subgroups         id PK, teacher_id FK users CASCADE, name NOT NULL, created_at
                          UniqueConstraint(teacher_id, name), Index(teacher_id)
teacher_subgroup_members  subgroup_id FK CASCADE PK, student_id FK users CASCADE PK
                          Index(student_id)
assignments（改列）        audience_mode TEXT NOT NULL DEFAULT 'all'
                          CheckConstraint("audience_mode IN ('all','subgroup')")
assignment_subgroups      assignment_id FK CASCADE, subgroup_id FK CASCADE, 复合 PK
                          Index(subgroup_id)
teacher_notices           id PK, teacher_id FK users CASCADE, kind TEXT, payload TEXT(JSON),
                          created_at, dismissed_at TEXT   Index(teacher_id, dismissed_at)
```

`teacher_students`（层 3）**保留但语义收窄**为「教师按 ID 手动添加的学生」。`TeacherGroup`（层 2）不动。

### 名单口径（一处改，处处生效）

1. `services/groups.py` 新增 `teachable_student_ids(db, teacher_id) -> set[int]`：`group_members JOIN teacher_groups ON group_id WHERE teacher_id = ?` 去重。
2. `services/stats.py:13-18` `bound_students()` 改为返回 **可教组成员 ∪ 手动添加**，其余 WHERE 条件（`role='student'`、`is_active`）原样保留。这是决策 1b/1e 的落点：撤销组别 → 学生立即离开名单。
3. `admin.py:202` 的名单计数改为调用新增的 `groups.roster_count_by_teacher(db) -> dict[int,int]`（同一并集口径），否则列表页「名单学生数」仍显示 0。
4. `admin.py:557` 学生→教师反查、`student.py:79-90` 的绑定判断，都要走同一并集；`student.py` 侧改为受众判断（见下条）。

### 受众（决策 1c）

5. `services/groups.py` 新增 `audience_students(db, assignment) -> list[User]`：先取 `bound_students(assignment.created_by)`；`audience_mode == 'all'` 直接返回；`'subgroup'` 时再交上 `teacher_subgroup_members`（限定 `subgroup_id ∈ assignment_subgroups` **且** `teacher_id = assignment.created_by`，防跨教师越权）。
6. `stats.py:60` 与 `:112` 的 `bound_students(db, assignment.created_by)` 换成 `audience_students(db, assignment)`。
7. `student.py`：`list_assignments`（`:146-157`）与 `_get_audience_assignment` 的可见性判据从「created_by ∈ 我的绑定教师」改为「我在该场次的 `audience_students` 里」。规模是班级量级，逐场次解析可接受；若后续变慢再考虑物化，本次不预先优化。
8. 新建场次接口（`teacher.py` 的 assignment 创建）接受 `audience_mode` + `subgroup_ids`；校验每个 subgroup 属于该教师。老数据默认 `'all'`，行为不变。
9. **子分组删除保护**：`DELETE /api/teacher/subgroups/{id}` 若被任何场次引用 → `409 SUBGROUP_IN_USE`，消息里带上场次标题。绝不自动把受众放宽到全部名单（那会让一场测试突然对全班可见）。

### 教师端写接口（`backend/app/api/teacher.py`）

```
GET    /subgroups                     列表 + 成员数（按当前名单口径）
POST   /subgroups                     {name}
PATCH  /subgroups/{id}                {name}
DELETE /subgroups/{id}                受第 9 条保护
PUT    /subgroups/{id}/students       {student_ids} 全量替换，校验 ⊆ 当前名单
```

审计动作新增：`subgroup_create` / `subgroup_update` / `subgroup_delete` / `subgroup_member_change`；`POST /students/bind`（按 ID 添加）沿用现有 `teacher_student_bind`，把 `AuditLogPage.tsx:46` 的中文标签改成「手动添加学生进名单」（决策 1d）。

**移出名单的语义收窄**：`teacher.py:629` 的 unbind 只能移走 `source='manual'` 的学生；对组别派生的学生返回 `422 ROSTER_DERIVED_STUDENT` 并列出 id（这类学生只能由管理员撤销组别来移除）。前端对应按钮文案改为「移出手动添加」。

### 组别撤销说明（决策 1e）

10. `admin.py:265` `replace_teacher_groups` 保存前算差集：对每个被移除的组，统计「离开名单的人数」= 旧可教成员中该组的人 − 新可教成员 − 手动添加者；写一行 `teacher_notices(kind='group_revoked', payload={groups:[{id,name,lost_count}], total_lost})`，与组别变更同事务。
11. `GET /api/teacher/notices/pending` + `POST /api/teacher/notices/{id}/dismiss`。
12. 前端新建 `components/TeacherNoticeDialog.tsx`，挂在 `components/Layout.tsx:41` 的 `Layout()` 内（教师角色），登录后拉一次 pending 即以 `Dialog` 呈现：「管理员已撤销你的可教组别「X」，其中 N 名学生已离开你的名单（历史提交与成绩仍保留）」，确认后 dismiss。

### 前端页面

13. `pages/admin/TeacherDetail.tsx`
    - 名单卡片：标题去掉「（只读）」，`:324-358` 的 `DataGrid` 换成**矩阵**（`display:flex; flexWrap:'wrap'` 的 chip 流），单元格式 `小明(ID:01)`，`ID` = `User.id` 零补齐两位，学号（`username`）与来源放进 `Tooltip`；手动添加的 chip 加一个淡色角标。`:316` 的 `SearchBox` 保留，按姓名或学号过滤。空态文案随新口径重写。
    - 可教组别卡片（`:249-301`）精简为：① 已分配组列表（组名 + 成员数 + 「移除」）；② 一个「添加组」按钮 → 弹窗勾选要添加的组。删除左右双 `GroupPanel`（`:67-103`）与 `:364-397` 的 diff 确认弹窗；弹窗复用 `components/GroupPickerDialog.tsx:92-181`（Fluent `Combobox multiselect` + `Listbox/Option` + 已选 `Badge`），只保留「未分配」的组为候选。保存仍是 `PUT /admin/teachers/{id}/groups` 全量替换。
    - 说明文字重写并去掉字面 `**`（见 A1）。
14. `pages/admin/TeacherList.tsx`：删「详情」按钮（A2）。
15. `pages/teacher/StudentList.tsx`：删「从班级拉学生」按钮（`:286`、`:349`）与其 `Dialog`（`:400-481`）；`GET /teacher/classes` 降级为只读展示（组别徽标）。页面改成「左：我的子分组（新建/改名/删除/移入移出）｜右：成员」。保留「按学生 ID 添加」（`:213` 附近）。`GET /api/teacher/students`（`teacher.py:513`）现在天然返回并集名单，前端不再需要 `bound` 置灰逻辑。

## B2（F5）系统设置 + 审计日志可配置与导出

### 存储与开关（migration 0006）

1. `SiteSetting` 加两列：`audit_enabled INTEGER NOT NULL DEFAULT 1`、`audit_retention_days INTEGER`（NULL = 永久保存）。沿用 `models.py:187-205` 的单行 `id = 1` 约定，不新建 KV 表。
2. `admin.py:788-809` `PUT /admin/settings` 接受这两个字段；`audit_retention_days` 允许置 NULL，取值范围 1–3650，越界 `422 AUDIT_RETENTION_INVALID`。
3. 公开 `GET /api/settings` 的 DTO **只多暴露 `audit_enabled` 一个布尔**（前端要据此决定导航与路由是否放行；保留天数走 admin 端点，不给未认证访客）。
4. `services/audit.py`：`log_audit`（`:17`）在 `audit_enabled = 0` 时直接 return。判定结果进程内缓存，`PUT /admin/settings` 写入时显式失效，避免每次导入学生都多一次 SELECT。

### 过期清理

5. `services/audit.py` 新增 `prune_expired(db) -> int`：`audit_retention_days` 非空时删除 `created_at < now - N 天` 的行（UTC 串字典序比较）。调用点三处：应用启动（`main.py` 的 startup）、`PUT /admin/settings` 改完保留期之后、以及 `log_audit` 内每小时最多触发一次的惰性清理（进程内时间戳守卫）——判题 worker 是常驻独立进程，正好提供周期性。
6. 关闭 `audit_enabled` **不删**历史日志；重新打开后历史仍在。这一点要在设置页文案里写明。

### 导出 Excel

7. 后端 `GET /api/admin/audit_logs/export?action=&target_type=&start=&end=` → `StreamingResponse`，完全照 `services/export.py:70-133` 的既有骨架（`load_openpyxl` 惰性导入、`XLSX_MEDIA_TYPE`、`attachment_headers` 的 RFC 5987 文件名）。列：时间 / 操作人 / 工号 / 动作 / 对象类型 / 对象 ID / 详情 JSON。
8. 导出遵守保留期；行数上限 50000，超出 `422 AUDIT_EXPORT_TOO_LARGE`，提示先加筛选条件。
9. **动作中文名只留一份**：在 `services/audit.py` 建 `AUDIT_ACTION_LABELS` / `AUDIT_TARGET_LABELS`，导出用它们写中文列；同时给 `AuditLogOut`（`schemas.py:155`）加 `action_label`、`target_label`，由 `admin.py:656` `audit_row_to_out` 填充。前端 `AuditLogPage.tsx:33-60` 的本地映射改为消费服务端标签（下拉候选仍保留 `optionValues()` 的"已知+已见"并集逻辑），避免中英两份字典漂移。
10. 前端：`api/index.ts` 仿 `:376-381` `exportAssignmentStudents` 加 `exportAuditLogs()`，内部走 `client.ts:207-254` `downloadBlob`（要带 JWT 头，不能裸 `<a href>`）。按钮放在 `AuditLogPage.tsx` 的 `PageHeader.actions`，导出中显示 `Spinner`，失败沿用该页 `MessageBar` 样式。

### 页面与导航

11. `Layout.tsx:37` 标签 `主题设置` → `系统设置`；`SettingsPage.tsx` 的 `PageHeader` 标题同步。
12. `Layout.tsx:36` 的 `审计日志` 导航项在 `audit_enabled === false` 时不渲染；`App.tsx:89` 的 `/admin/audit` 路由加守卫，关闭时重定向到 `/admin/settings` 并提示。后端 `admin.py:673` `list_audit_logs` 同步返回 `403 AUDIT_DISABLED`（前端隐藏不是安全边界）。
13. `SettingsPage.tsx` 新增一张「审计日志」卡片：`Switch`（启用）、`NumberInput`（保留天数，留空 = 永久）、说明文案（关闭不删历史 / 过期自动删除）、以及一个「查看审计日志」跳转链接。与主题设置共用现有保存流程（`SettingsPage.tsx:327-345`）。

---

## 提交计划（每个功能性更新一个 commit）

沿用仓库既有风格（`feat(范围): 0.3.0 M6-N — 描述` / `fix: 0.3.1 修复批次 FIX-1~7` /
`docs(开发文档): v2.5 — …` / `chore(release): 更新版本号到 0.3.1`），分支 `dev-0.3.x`。
**一条 commit 只装一个功能性更新，且提交前该条自己的验收必须先过**（后端 `pytest` / 前端 `npm run build`）。

| 序 | commit message | 内容 | 前置 |
|---|---|---|---|
| 0 | `docs(计划): 0.3.2 修复与开发计划定稿` | 本文件 | — |
| 1 | `fix(管理员): 0.3.2 F2 — 可教组别说明的着重号未渲染` | A1 | — |
| 2 | `fix(管理员): 0.3.2 F3 — 删除教师列表冗余「详情」按钮` | A2 | — |
| 3 | `feat(密码): 0.3.2 F4 — 密码可见性开关与弱密码审核` | A3（`PasswordField` + `validate_new_password` + 前端镜像） | — |
| 4 | `fix(成绩): 0.3.2 F6 — 多题场次逐学生按题调分` | A4（新端点 + `StudentScores.tsx` + 改名 + 删底部返回 + 改两条旧测试） | — |
| 5 | `feat(学生): 0.3.2 F7 — 场次「即将结束」徽标与页内横幅` | A5（`state` 三元 + 两处 Badge + 横幅 + `ENDING_SOON_HOURS`） | — |
| 6 | `feat(数据模型): 0.3.2 F5-1 — 迁移 0006 审计设置列` | `SiteSetting` 两列 + migration | — |
| 7 | `feat(管理员): 0.3.2 F5-2 — 系统设置、审计保留期与 Excel 导出` | B2 其余全部（开关生效、`prune_expired`、导出端点、服务端标签、导航与路由隐藏） | 6 |
| 8 | `feat(数据模型): 0.3.2 F1-1 — 迁移 0007 子分组/受众/教师通知` | B1 的五张表与 `audience_mode` | — |
| 9 | `feat(权限): 0.3.2 F1-2 — 名单口径改为可教组别并集，受众支持子分组` | B1 后端服务层与接口（`teachable_student_ids` / `bound_students` / `audience_students` / 子分组 CRUD / unbind 收窄 / 组别撤销通知） | 8 |
| 10 | `feat(前端): 0.3.2 F1-3 — 管理员矩阵名单与「添加组」弹窗` | B1 的 admin 侧（`TeacherDetail.tsx` 矩阵 + `GroupPickerDialog` 复用 + 说明文字） | 9 |
| 11 | `feat(教师): 0.3.2 F1-4 — 教师子分组页与组别撤销提示` | B1 的教师侧（`StudentList.tsx` 重构、发布页受众选择、`TeacherNoticeDialog`） | 9、10 |
| 12 | `docs(开发文档): v2.7 — 0.3.2 实施回写` | 第 5 章 DDL、第 6.2 接口清单、第 8 章前端规范、`部署操作指南.md:570`、变更历史条目 | 1~11 |
| 13 | `chore(release): 更新版本号到 0.3.2` | 版本号 + 文末 Release Note | 12 |

拆分口径说明：F1 横跨 5 张表 + 后端 + 两端页面，一次 commit 会把上千行改动混在一起、出问题难回滚，
因此按「迁移 / 后端 / admin 前端 / 教师前端」切成 F1-1~F1-4 四条，仍属同一个功能性更新的四个阶段；
F5 同理切成迁移与功能两条。其余各项一条搞定。

收尾：commit 全部落好后交付文末 Release Note；是否 push、是否在 main 打 tag 与建 GitHub Release 由用户决定，不自行执行。

## 验证

- **后端**：`backend/.venv` 下 `python -m pytest`（基线 307 passed / 1 skipped）。新增/修改用例：
  - 弱密码：纯字母、纯数字、`aaaaaaa1`、`12345678`（= 初始密码）、`password`（黑名单）、连续串 `abcdefgh1` 全部 422；`Ql2026abc` 通过。
  - 名单并集：教师有可教组但未手动添加时，`bound_students` 非空；管理员撤销组别后该组学生立即消失，手动添加者仍在。
  - 受众：`audience_mode='subgroup'` 时，非子分组成员的学生在 `GET /api/student/assignments` 里看不到该场次，逐学生成绩表也只列子分组成员。
  - 子分组删除保护：被引用时 409。
  - 跨教师越权：A 教师用 B 教师的 subgroup id 调 `PUT /subgroups/{id}/students` → 403/404。
  - 调分：一场次两题，新端点两题各自返回自己的 `latest_submission_id` 与有效分。
  - 审计：`audit_enabled=0` 时 `log_audit` 不落库且查看接口 403；保留期 N 天时旧行被 `prune_expired` 删除、NULL 时不删；导出端点返回 xlsx 且中文标签列正确。
- **前端**：`npm run build` 0 错（Fluent v9 类型严格，`Button` 的 `icon` 槽只收 `ReactElement`，见 `TeacherDetail.tsx:60` 的既有注释）。
- **UI 走查**（按项目既有流程，本机端口读 `backend/data/dev-ports.json`）：admin 教师详情矩阵与「添加组」弹窗、教师端子分组增删改与移入移出、组别被撤销后的说明弹窗、登录/改密/账号三处小眼睛、学生端红色「即将结束」徽标 + 顶部横幅、逐学生按题调分页（多题场次）、审计日志导出与关闭后导航隐藏。**暗色模式逐页复查**；新徽标/矩阵 chip 用既有的 PNG 墨迹边缘自查法对一遍齐。
- **回归重点**：`bound_students` 是受众/成绩/可见性的唯一口径，改动后必须重跑总览、逐学生成绩、导出 xlsx 三处数字一致性。

## 风险与取舍

- **撤销组别立即清名单**（决策 1e）会让教师已发布的 `'all'` 场次受众自动缩小，学生的历史提交仍在（`submissions` 不级联删除），但学生端不再可见。这是用户明确选择的口径，务必在 B1 的教师提示与 `开发文档.md` 里写清，避免被当成 bug 报回来。
- 受众从"教师全部名单"变成"按子分组"，`stats.py` 与 `student.py` 两处查询都要逐场次解析子分组，SQLite 下班级规模无压力；不做提前的物化优化。
- 弱密码规则收紧后，历史上设过简单密码的账号不受影响（只在改密时校验），但**新首登改密的学生可能被挡**，因此前端 `PASSWORD_RULES_TEXT` 必须与后端逐字对齐，否则学生只会反复试。
- `audit_enabled` 出现在公开 `GET /api/settings` 是一个刻意的小让步（前端路由需要它来决定导航）；只放布尔，不放保留天数与日志内容。

---

## Release Note 0.3.2（收尾交付，可直接粘贴 GitHub Release）

标题：`青蓝 QingLan 0.3.2 — 名单口径重构、逐题调分与审计日志可配置`

> 本次是小版本号，但改动量大：7 项功能更新，含 2 次数据库迁移。
> 升级前请先备份 `backend/data/` 下的 SQLite 库（用 `sqlite3 .backup`，直接拷文件会漏掉 WAL）。

### 新增

- **教师子分组**：教师可在自己的名单里建子分组、改名、删除、移入移出学生。
- **按子分组发布**：新建场次时可选定一个或多个子分组作为受众，不再固定面向全部名单；不选即为全部名单。
- **逐学生按题调分页**：点开某位学生，即可看到他在本场次**每一道题**的系统分、教师调分与有效分，并逐题直接调分。
- **密码可见性开关**：登录页、修改密码页、账号页共 7 个密码输入框都加了小眼睛，点一下看明文，再点收回。
- **弱密码审核**：改密时新密码必须同时含字母和数字，且拒绝全同字符、连续字符（`abcdef` 之类）与约 60 条常见弱密码。
- **审计日志可配置**：「主题设置」更名为「系统设置」，新增审计日志卡片 —— 启用开关（默认开）、保留天数（默认 20 天，留空 = 永久保存）。
- **审计日志导出 Excel**：审计页新增导出按钮，可按动作与对象类型筛选后导出，含中文动作名列。
- **场次「即将结束」提醒**：距结束不足 3 小时的场次，学生端状态徽标变为红色「即将结束」，并在「我的场次」页顶部显示红色横幅提示。
- **组别撤销说明**：管理员收回某教师的可教组别后，该教师下次进入系统会收到弹窗，说明哪些组被撤销、各有几名学生离开了名单。

### 变更

- **管理员端的「学生名单」口径改了**：不再只显示教师手动拉取的学生，而是完整展示其可教组别里的全部学生，并改为矩阵式呈现（`小明(ID:01)`）。
- **教师端取消「从班级拉学生」**：进入「我的学生」直接就是全部可教学生，无需再逐个拉取；「按学生 ID 添加单个学生」保留，并纳入审计日志。
- **可教组别分配界面精简**：先列出已分配给该教师的组，再用「添加组」按钮弹窗勾选要新增的组，替代原来的左右双栏分配面板。
- **撤销组别即刻生效**：管理员收回组别后，该组学生立即离开教师的名单与该场次的受众（历史提交与成绩仍保留可查）。
- **「移出名单」范围收窄**：只能移走手动添加的学生；组别带来的学生需由管理员撤销组别。
- **教师列表去掉「详情」按钮**：点姓名即可进入详情，操作列只留「重置密码 / 启用|停用」。
- **「查看 / 调分」更名为「调分」**，列头同步。
- 审计日志的动作中文名改由后端统一下发，页面与 Excel 导出共用一份字典。

### 修复

- 多题场次里「调分」只能打开一道题（其余题目没有任何入口）—— 根因是入口链接取的是该生该场次的全局最新一次提交，未按题目分组。
- 教师详情「可教组别」说明文字里的 `**` 着重号按字面显示，未被渲染。
- 提交详情页同时存在「返回逐学生表」与页底「返回」两个按钮，已去掉页底那个。
- 学生端场次状态只有「进行中 / 已结束」两态，缺少临近结束的提示。

### 数据库迁移

- `0006`：`site_settings` 新增 `audit_enabled`、`audit_retention_days`。
- `0007`：新增 `teacher_subgroups`、`teacher_subgroup_members`、`assignment_subgroups`、`teacher_notices`；`assignments` 新增 `audience_mode`。
- 老数据无需人工处理：`audience_mode` 默认 `'all'`，行为与 0.3.1 一致；已有 `teacher_students` 记录转为「手动添加」语义继续生效。

### 升级须知

- 改密规则变严，用初始密码 `12345678` 首登的学生可能被挡下，请提前告知规则（改密页已逐字列出）。
- 关闭审计日志只停止后续记录，**不会删除**已有日志；要清空请自行处理数据库。
- 子分组被任何场次引用时不允许删除，请先调整相关场次的受众。
