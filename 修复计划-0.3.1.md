# 青蓝 0.3.1 修复批次计划（agent-native）

- 日期：2026-09-23　分支：`dev-0.3.x`　基线：后端 `pytest` 316 passed / 前端 `npm run build` 0 错
- 来源：用户验收反馈 6 条（含 2 条历史遗留）+ 4 张截图（审计日志/管理表/发布列表/逐学生成绩）
- 执行方式：按 FIX 编号顺序实施；每个 FIX 自带「改法」「验证」「验收」，可独立提交
- 全局约束：
  - 前端禁止硬编码色，一律主题令牌（`useTheme()` 的 `t.colorXxx`），暗色模式同步走查
  - 后端不改导出 xlsx 列布局（SC-02 契约），除非 FIX 明文要求
  - 《开发文档.md》末尾变更历史是 30k+ 字符单行：回写时用行号区间定位编辑，禁止整行重写/grep 全文
  - 提交由用户执行（或明确批准后执行）；commit 信息沿用 `fix(范围): 0.3.1 …` 风格

---

## FIX-1　教师「逐学生成绩」下钻链接恒为空（M5 遗留）

**现象**：`AssignmentStudents.tsx` 的「下钻」列永远渲染 `—`，`Link` 从不出现（截图四该列全空）。

**根因**：前端按 `item.last_submission_id` 渲染链接（`frontend/src/pages/teacher/AssignmentStudents.tsx:204-207`，类型声明 `frontend/src/api/types.ts:370`），但后端 `StudentRowOut` 与 `stats.student_rows` 从不产出该字段：
- `backend/app/services/stats.py:151` 只算了 `last_submitted_at = max((s.submitted_at for s in mine), default=None)`
- `backend/app/schemas.py:425-434` `StudentRowOut` 无 `last_submission_id`

**改法**：
1. `backend/app/services/stats.py`（student_rows 内，替换 :151 并在 :152-161 的 row dict 增键）：
   ```python
   last = max(mine, key=lambda s: (s.submitted_at or "", s.id), default=None)
   ...
   "last_submitted_at": last.submitted_at if last else None,
   "last_submission_id": last.id if last else None,
   ```
   口径：`submitted_at` 为 UTC 文本 `YYYY-MM-DD HH:MM:SS`，字典序即时间序；同刻以 id 大者为准。与现有 `last_submitted_at` 同源同刻，不新增查询（提交已一次全取）。
2. `backend/app/schemas.py` `StudentRowOut` 增 `last_submission_id: int | None = None`。
3. 前端零改动（类型与渲染已就绪）。
4. **导出不改**：`backend/app/services/export.py` 按显式键取数（:104-116），新增键不影响 xlsx 列布局。

**验证**：
- 新增 API 用例（放 `backend/tests/test_import_export.py`，紧邻 `test_rows_match_students_endpoint_payload` :807）：同一学生两次提交（`submitted_at` 不同）→ `/api/teacher/assignments/{id}/students` 该行 `last_submission_id` == 较晚提交的 id、`last_submitted_at` 与之一致；未交学生两字段为 `None`。
- 回归：`backend/.venv/Scripts/python -m pytest -q` 全绿（现有用例无整行 dict 断言，`test_teacher_binding_api.py:386-387` 断言空列表不受影响）。
- 浏览器：逐学生成绩页「下钻」列对已交学生出现「查看提交」链接，点击落到 `/teacher/submissions/{id}`；未交仍为 `—`。

**验收**：链接可达且指向该生最后一次提交；导出 xlsx 表头/列数不变。

---

## FIX-2　教师调分框手输数值失焦被清空（`components/NumberInput.tsx`）

**现象**：只能靠 ± 箭头改值；手输数字失焦即被清空，点「保存」恒报「请先输入调分数值」。

**根因**（已对 `@fluentui/react-spinbutton` 9.54.0 `useSpinButton` 源码核实）：受控模式（`value={value ?? null}`）下，键入只更新组件内部 `textValue`、不回调 `onChange`；blur/Enter 走 `commit(e, currentValue, textValue)` → `valueChanged=false`、`displayValueChanged=true`，而受控模式跳过内部解析分支，回调数据为 `{ value: undefined, displayValue: '手输文本' }`；现包装器 `NumberInput.tsx:19-21` 只认 `typeof d.value === 'number'`，把手输值丢弃，随后 `setTextValue(undefined)` 把显示清回受控的 null。

**改法**（`frontend/src/components/NumberInput.tsx`，onChange 增加 displayValue 兜底解析）：
```tsx
onChange={(_, d) => {
  if (typeof d.value === 'number' && !Number.isNaN(d.value)) {
    onValue(d.value);
    return;
  }
  const parsed = Number.parseFloat(String(d.displayValue ?? '').trim());
  if (!Number.isNaN(parsed)) onValue(parsed);
}}
```
箭头/键盘步进仍走 `d.value` 分支，行为不变；blur 前点击「保存」会先触发 blur→commit→state 更新，保存拿到新值。

**可选增强（默认不做，待用户确认）**：`NumberInput` 增 `max?: number` 透传 + 后端提交详情增 `full_score`（`teacher.py` GET `/teacher/submissions/{id}` 经 `assignment_problems` 联查，与调分上界同源），让箭头在上界处即停。手输越界仍由服务端 422 `MANUAL_SCORE_OUT_OF_RANGE` 兜底（现有行为，提示正常）。

**验证**（前端无单测设施，浏览器走查即回归守卫）：
- 提交下钻页手输 `88` → 失焦输入框仍显示 88 → 保存成功、`教师调分` 显示 88、审计 `score_manual_adjust` 一条
- 手输 `-5` → 保存 → 422 越界提示（不再误报「请先输入」）
- ± 箭头、Enter 提交、撤销调分按钮均不变
- `npm run build` 0 错

**验收**：手输任意合法值一次保存成功；越界/非法输入有准确提示。

---

## FIX-3　admin 审计日志页：去代号、加「操作者 ID」列、刷新按钮对齐

**文件**：`frontend/src/pages/admin/AuditLogPage.tsx`

**改法**：
1. 去代号（只留主文字）：
   - 删 :316 `<Caption1 …>{item.action}</Caption1>`（动作徽标下的原始 code）
   - 删 :322 `<Caption1 …> {item.target_type}</Caption1>`（对象类型后的原始 code）
   - 筛选下拉原本就只显示中文标签，不动
2. 「操作者 ID」独立成列（用户要求放在「操作者」后面）：
   - 列定义 :131-138 在 `actor` 后插入 `createTableColumn({ columnId: 'actor_id', renderHeaderCell: () => '操作者 ID' })`
   - 单元格：`{columnId === 'actor_id' && (item.actor_id === null ? '—' : item.actor_id)}`
   - 同时删 :306-308 操作者格内叠放的 `ID n` 小字（信息已独立成列，姓名恢复单行）
3. 刷新按钮与筛选框对齐：:252-259 的 `Button` 加 `style={{ alignSelf: 'flex-end' }}`。
   根因：`PageHeader` 的 actions 行 `alignItems: center`，两个筛选组是「Caption1 标签 + Dropdown」的列结构（高约 50px），裸按钮按行居中遂悬在标签与控件之间；`flex-end` 使按钮底边与筛选组底边齐平，即与 Dropdown 同一水平带。

**验证**：浏览器 `/admin/audit`——徽标/对象类型下无等宽代号；新列显示数字 ID（已删除账号行显示 `—`）；刷新按钮与两个下拉框同排对齐；按动作/对象类型筛选与「加载更多」不变；暗色模式走查无硬编码色。`npm run build` 0 错。

**验收**：表格列为 时间 | 操作者 | 操作者 ID | 动作 | 对象类型 | 对象 ID | 详情，且无原始 code 泄漏。

---

## FIX-4　所有管理表去掉「改密状态」栏（2026-09-23 用户已确认扩围）

**范围**：前端四处表格的「改密状态」列与「未改密/已改密」徽标**全删**，并删除相关提示文案；后端 API 字段不动。

| 页面 | 文件 | 删除点 |
|---|---|---|
| admin 教师管理 | `frontend/src/pages/admin/TeacherList.tsx` | 列定义 :44；单元格 :160-164 |
| admin 学生管理 | `frontend/src/pages/admin/StudentList.tsx` | 列定义 :83；单元格 :471-475；「改密状态」筛选 Dropdown :346-357 及其配套 `PasswordFilter` 类型 :75-76、state :95、`mustChange` :108、fetch 入参与依赖 :105/:111-112（列删后筛选结果不可见，须同删） |
| admin 教师详情·名单表 | `frontend/src/pages/admin/TeacherDetail.tsx` | 列定义 :51；单元格 :349-353 |
| 教师端 我的学生 | `frontend/src/pages/teacher/StudentList.tsx` | 列定义 :64；单元格 :388-392；提示句 :339（「出现『未改密』徽标，说明该生还在用统一初始密码，请提醒其尽快登录改密。」——只删这一句，:335-338 的人数统计 Caption1 保留） |

**明确保留（勿顺手删）**：
- 后端一切不动：`must_change_password` 字段与 PW-02 未改密拦截、`?must_change=` 查询参数、强制改密路由/守卫均依赖它；前端 `types.ts:61` 的字段声明保留（`/auth/me` 恢复拦截态要用）。
- `admin/StudentList.tsx:263` 批量重置弹窗里的 `alreadyChanged` 统计（读 API 字段，与表格列无关）。
- 「导出初始密码表」CSV 功能。

**验证**：
- 浏览器四页各看一眼：均无「改密状态」列/徽标；学生管理筛选区只剩 搜索 + 组别；教师端名单页提示语无「未改密」句。
- 批量重置 >20 人询问弹窗文案仍正常（含「已改密 N 人」）。
- 后端 `pytest` 不变即绿（无后端改动）；`npm run build` 0 错（注意 :105/:111-112 删参后无残留未用变量，tsc 会报）。

**文档同步**：《部署操作指南.md》:533 与 :560 两条 checklist 改写（去掉「未改密徽标/可筛选」表述，保留 CSV 导出）；《开发文档.md》LI-02 相关行标注「0.3.1 起徽标仅存 API，不再展示」。

---

## FIX-5　教师「发布」列表加状态列：未开始 / 进行中 / 已结束

**文件**：`frontend/src/pages/teacher/AssignmentList.tsx`、`frontend/src/components/time.ts`

**改法**：
1. `time.ts` 增助手（复用既有 `toDayjs`，dayjs utc 插件已加载）：
   ```ts
   export type WindowPhase = 'not_started' | 'ongoing' | 'ended';
   export function windowPhase(start?: string | null, end?: string | null): WindowPhase | null {
     if (!start || !end) return null;
     const s = toDayjs(start);
     const e = toDayjs(end);
     if (!s.isValid() || !e.isValid()) return null;
     const now = dayjs.utc();
     if (now.isBefore(s)) return 'not_started';
     if (now.isAfter(e)) return 'ended';
     return 'ongoing';
   }
   ```
2. `AssignmentList.tsx` 列定义 :49-56 在 `window`（时间窗）后插入 `createTableColumn({ columnId: 'phase', renderHeaderCell: () => '状态' })`；单元格按 `windowPhase(item.start_time, item.end_time)` 渲染 `Badge`（`className="ql-badge-status" size="large"`）：
   - 进行中：`colorPaletteGreenForeground1 / colorPaletteGreenBackground2`
   - 未开始：`colorPaletteBlueForeground1 / colorPaletteBlueBackground2`
   - 已结束：`colorNeutralForeground3 / colorNeutralBackground4`
   - `null`（缺时间窗，理论不出现）→ `Caption1` 的 `—`
3. 口径：纯时间窗推导，与「放分」列（仅 test 模式）互不耦合；列表为前端全量拉取，状态随每次加载计算，不做 ticking 定时器。

**验证**：浏览器发布页——过去/当前/未来时间窗的场次分别显示 已结束/进行中/未开始；跨时刻边界（把某场次时间窗改到包含当前）刷新后状态翻转；暗色走查；`npm run build` 0 错。

---

## FIX-6　「查看做题情况 + 手动调分」界面的可达性（答疑 + 可发现性）

**答疑（用户问题 5）**：该界面是 `/teacher/submissions/:sid`（`SubmissionDetail.tsx`：代码只读 + 逐测试点 + 调分/撤销/重判）。它**唯一的入口**一直是「逐学生成绩」页的下钻链接，而该链接因 FIX-1 缺陷恒为 `—`——所以实际上无路可达，这正是「找了半天找不到」的原因（测试时看到的是种子数据直连 URL 的页面）。FIX-1 修复后标准路径为：

> 教师端「发布」列表 → 行尾 ⋯ 菜单 → 「逐学生成绩」 → 该行「查看提交」链接 → 提交下钻页（调分/重判）
> （或：场次总览页「逐学生成绩 →」进入同一张表）

**可发现性改法**（`AssignmentStudents.tsx`）：
- 列头 :110 `'下钻'` → `'查看提交 / 调分'`（列 id `drill` 不变）
- 链接文案 :206 `'查看提交'` → `'查看 / 调分'`

**默认不做**：在发布列表 ⋯ 菜单加别名按钮（其目的地与「逐学生成绩」完全相同，属重复入口）。若用户坚持要「表内按钮」，再按待确认 Q3 加 `MenuItem`「调分 / 查看做题」→ `navigate(/teacher/assignments/${id}/students)`。

**文档同步**：《部署操作指南.md》教师验收小节补一行入口路径说明；《开发文档.md》:680 页面表已记载该路由，补「入口=逐学生成绩下钻列」一句。

**验收**：新账号教师按上述路径 3 次点击内可到达调分页；页内「← 返回逐学生表」回跳正确。

---

## FIX-7　逐学生成绩：已交学生姓名去加粗

**现象**：姓名列对 `submitted_count > 0` 的学生渲染 semibold（截图四中 m1s01/m3s01/m3s02 加粗），用户要求统一字重。

**改法**：`frontend/src/pages/teacher/AssignmentStudents.tsx:187`
`<Text weight={item.submitted_count === 0 ? 'regular' : 'semibold'}>` → `<Text>`（删 weight prop）。
「未交」红色徽标保留（:189-193），未交识别不依赖字重。

**验证**：浏览器该页所有姓名字重一致；未交徽标仍在；`npm run build` 0 错。

---

## 待确认问题

| # | 问题 | 默认决策 |
|---|------|----------|
| Q1 | ~~FIX-4 的「管理表格」实指哪张表~~ **已确认（2026-09-23）**：四处管理表全删，含相关提示文案 | 见 FIX-4 |
| Q2 | FIX-2 是否带可选增强（`max` + 后端 `full_score`）？ | 不带；手输越界由服务端 422 提示已足够准确 |
| Q3 | FIX-6 是否仍要在发布列表 ⋯ 菜单加「调分 / 查看做题」别名入口？ | 不加；FIX-1 + 列头改名后路径已清晰 |
| Q4 | FIX-5 状态列位置（时间窗后）与三色配色 | 按本文执行 |

## 回归与收尾

1. 后端：`cd backend && .venv/Scripts/python -m pytest -q` → 316+1（FIX-1 新用例）passed
2. 前端：`cd frontend && npm run build` → tsc 0 错
3. 浏览器走查清单（登录自动化可用；换角色需 reload）：
   - [ ] 逐学生成绩：下钻链接可达、姓名字重统一、导出 Excel 列不变
   - [ ] 提交下钻：手输调分保存/越界/撤销/重判四路径
   - [ ] 审计日志：无代号、操作者 ID 列、刷新对齐、筛选与加载更多
   - [ ] 四处管理表（admin 教师/学生/教师详情名单、教师端我的学生）均无「改密状态」列与徽标；学生管理筛选区无「改密状态」下拉；教师端名单提示语无「未改密」句；批量重置弹窗「已改密 N 人」仍正常
   - [ ] 发布列表：状态列三态正确、⋯ 菜单不变
   - [ ] 以上各页暗色模式各看一眼
4. 文档回写：《开发文档.md》删除两条「本批次遗留」（:870 下钻链接、:873 调分框）并追加 0.3.1 变更历史行（单行巨行，按行号区间编辑）；《部署操作指南.md》同步 FIX-6 路径与 FIX-4  checklist 变化
5. 提交：fix(后端)/fix(前端)/docs 分笔，由用户执行或批准后执行
