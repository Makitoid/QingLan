# 青蓝 QingLan · 部署操作指南

- 版本：v1.0
- 日期：2026-09-17
- 读者：零 Linux 基础的站点管理员（就是你）
- 前提：《开发文档.md》的 **M4 里程碑已完成**，即仓库里已有 `docker/compose.yml`、`.env.example`、`scripts/backup.sh`、`scripts/reset_password.py`。代码还没开发到 M4 时，本指南先通读建立概念，勿执行。
- 阅读约定：
  - 命令**逐行**复制执行：粘贴一行、回车、等它跑完，再粘下一行。
  - 没特别标注的命令都在**服务器**上执行；标注"本机"的在你 Windows 上执行。
  - `sudo` = 以管理员身份执行，可能要求输入密码；**输密码时屏幕不显示任何字符，是正常的**，输完回车。
  - 每步给了"预期输出"；对不上就去第 8 章 FAQ。
  - 文中 `ubuntu@1.2.3.4` 这类都是示例，替换成你自己的用户名和 IP。

---

## 0. 总览：你将要做的 7 件事

1. 选一台服务器（学校机器 或 云服务器）并拿到登录方式
2. 从你的 Windows 用 SSH 连上服务器
3. 在服务器上装 Docker
4. 把项目代码弄上服务器
5. 配置并启动（3 个容器：web / worker / go-judge）
6. 初始化管理员账号、浏览器验收
7. 配置每日自动备份，学会看日志与更新

---

## 1. 服务器选择与准备

### 1.1 二选一

| 方案 | 优点 | 缺点 | 适合 |
|---|---|---|---|
| 学校自有机器（机房 PC/旧电脑装 Ubuntu） | 免费、内网访问快、不用备案 | 校外访问不了、要常开机 | 主要上课机房用 |
| 云轻量服务器（腾讯云/阿里云等） | 校外能访问、稳定 | 约几十到一百元/月 | 宿舍/家里也要交作业 |

**硬性要求（两种方案都一样）**：
- 系统：Ubuntu 22.04 或 24.04 LTS（64 位）
- 虚拟化：物理机或 KVM 虚拟化（**OpenVZ 等老式容器虚拟化跑不了判题沙箱**；云厂商的"轻量应用服务器/ECS"都是 KVM，放心）
- 配置：2 核 4G 内存起步，硬盘 40G 起步
- **不要选 Windows Server**

### 1.2 云服务器购买要点

1. 产品选"轻量应用服务器"或入门级 ECS 即可
2. 镜像选 **Ubuntu 22.04/24.04 LTS**
3. 购买后在控制台做三件事并记录：
   - 记下**公网 IP**
   - 设置/重置**登录密码**（用户名通常是 `ubuntu` 或 `root`，控制台会写）
   - 在"防火墙/安全组"里**放行 TCP 端口 22 和 8000**（22 是 SSH 远程登录，8000 是网站）

### 1.3 学校机器准备要点

1. 用 Rufus 把 Ubuntu 24.04 镜像写进 U 盘安装（官网有教程）
2. 装好联网后，打开终端执行 `ip a`，找到内网 IP（形如 `192.168.x.x` 或 `10.x.x.x`），记下来
3. 开启 SSH 服务（供你的 Windows 远程连）：

```bash
sudo apt install -y openssh-server
sudo systemctl enable --now ssh
```

### 1.4 从你的 Windows 第一次连服务器（SSH）

本机操作：开始菜单搜"PowerShell"打开，执行：

```powershell
ssh ubuntu@1.2.3.4
```

- 第一次连会问 `Are you sure you want to continue connecting (yes/no)?` → 输入 `yes` 回车
- 然后提示输密码：**屏幕不显示字符是正常的**，输完回车
- 成功标志：提示符变成类似 `ubuntu@VM-0-1:~$`，此后你敲的命令都在服务器上执行
- 以后每次运维都从这个窗口开始；断开输入 `exit`

> 连不上的两种典型：`Connection timed out` = IP 错或 22 端口没放行；`Permission denied` = 密码错，去云控制台重置。

---

## 2. 服务器初始化（装 Docker）

SSH 连上后，逐行执行：

```bash
sudo apt update
sudo apt upgrade -y
sudo apt install -y curl git unzip ca-certificates
curl -fsSL https://get.docker.com | sh
sudo systemctl enable --now docker
sudo docker run --rm hello-world
sudo timedatectl set-timezone Asia/Shanghai
```

逐行解释：
- 1–2 行：更新系统软件清单并升级（第 2 行要跑几分钟）
- 3 行：装常用小工具
- 4 行：Docker 官方一键安装脚本
- 5 行：让 Docker 开机自启并立即启动
- 6 行：**验证**。预期输出里有一段 `Hello from Docker!`，看到即成功
- 7 行：把服务器时区设为北京时间（日志时间才看得懂）

> 失败处理：`apt` 报网络错就重跑一次；第 6 行失败先执行 `curl -I https://www.baidu.com` 看服务器有没有外网。

顺手建一个短命令别名，后面所有 compose 命令都用 `dc` 代替：

```bash
echo "alias dc='sudo docker compose -f docker/compose.yml'" >> ~/.bashrc
source ~/.bashrc
```

---

## 3. 把代码弄上服务器（二选一）

### 方式 A：git 私有仓库（推荐，日后更新一条命令）

1. 本机：在 GitHub（或 Gitee）建**私有**仓库 `qinglan`，把项目推上去（M1 开工时就会建）
2. 本机：GitHub → 头像 → Settings → Developer settings → Personal access tokens → 生成一个 token，权限勾 `repo`，**复制保存（只显示一次）**
3. 服务器：

```bash
git clone https://<你的用户名>:<token>@github.com/<你的用户名>/qinglan.git
cd qinglan
```

> token 等同密码，别贴到公开地方；泄露了去 GitHub 删掉重生成。
> 服务器 clone 卡住几分钟不动 = 网络到 GitHub 不通畅：`Ctrl+C` 放弃，改用方式 B。

### 方式 B：zip 上传（不用 git）

1. 本机：进入 `qinglan` 文件夹，**先删掉** `data/`、`.venv/`、`node_modules/` 三个目录（都是运行时产物，服务器会重新生成；删的是你开发机的副本内容，介意就先复制一份再删）
2. 本机：右键 `qinglan` 文件夹 → 压缩为 `qinglan.zip`
3. 本机 PowerShell：

```powershell
scp qinglan.zip ubuntu@1.2.3.4:~/
```

4. 服务器：

```bash
cd ~ && unzip -q qinglan.zip && cd qinglan
```

> 方式 B 的代价：以后每次更新都要重新上传覆盖（第 7 章有覆盖更新流程）。

---

## 4. 配置并启动

服务器上，确保在 `~/qinglan` 目录：

```bash
cp .env.example .env
openssl rand -hex 32
```

- 第 2 行会输出一串 64 位随机字符，**复制它**（这是登录令牌的加密密钥）

```bash
nano .env
```

- 用方向键把光标移到 `JWT_SECRET=` 后面，粘贴刚才那串；其余项看不懂就保持默认
- nano 编辑器三步：编辑完按 `Ctrl+O` 再按回车（保存），按 `Ctrl+X`（退出）

```bash
dc up -d --build
```

**go-judge 容器前置（cgroup v2 系统必做一次，Ubuntu 22.04+/Debian 12+ 都算）**：判题沙箱需要在"宿主 cgroup 命名空间"里创建资源组，Docker 默认是隔离命名空间，会让 go-judge 启动即退出（`cgroup path is empty`）。给 Docker 守护进程设一次全局参数：

```bash
sudo mkdir -p /etc/systemd/system/docker.service.d
printf '[Service]\nExecStart=\nExecStart=/usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.sock --default-cgroupns-mode=host\n' | sudo tee /etc/systemd/system/docker.service.d/qinglan.conf
sudo systemctl daemon-reload && sudo systemctl restart docker
```

> 注意：重启 docker 会短暂停掉正在运行的容器，装好 QingLan 后若再执行此步，需 `dc up -d` 拉回。

- 第一次要下载基础镜像并构建，**5–15 分钟**，屏幕持续滚日志是正常的；结束回到命令提示符即完成
- `-d` 表示后台运行

```bash
dc ps
```

- 预期：3 行服务 `web` / `worker` / `go-judge`，STATE 列都是 `Up`（带 `(healthy)` 更好）

```bash
dc exec web python scripts/init_admin.py
```

- 预期输出：已创建管理员账号 `admin`

**浏览器验收**：打开 `http://你的IP:8000`（学校机器用内网 IP），应看到青蓝登录页。用 `admin / admin123` 登录，**立刻在右上角改密码**。

---

## 5. 上线前验收清单（照《开发文档.md》M1–M3 功能点抽验）

- [ ] 登录页打开，主题/背景正常，暗色模式切换正常
- [ ] admin 登录成功且已改密码
- [ ] 建 1 个教师、2 个学生账号，并把学生绑定给教师
- [ ] 教师建一道题（2 个测试点，1 个标样例），发布为作业
- [ ] 学生提交正确 C 代码 → AC 满分；提交错误代码 → WA 且按通过点给部分分
- [ ] `dc ps` 三服务 Up
- [ ] 管理员后台改一次主题色，全站生效

任何一条失败 → 第 8 章 FAQ；FAQ 没有 → 把报错原文发给 AI。

---

## 6. 自动备份（必做）

备份脚本是仓库自带（M4 交付）：把数据库 + 学生代码 + 测试用例打包到 `~/qinglan/backups`，自动只保留最近 7 份。

```bash
chmod +x scripts/backup.sh
sudo ./scripts/backup.sh
ls ~/qinglan/backups
```

- 预期：`~/qinglan/backups` 下出现一个带日期的 `.tar.gz` 包

配置每天凌晨 3 点自动备份：

```bash
crontab -e
```

- 第一次会问选编辑器，选 nano（输入对应数字回车）
- 在文件**最后一行**新增：

```
0 3 * * * cd /home/ubuntu/qinglan && sudo ./scripts/backup.sh >> /var/log/qinglan-backup.log 2>&1
```

- 五个数字含义：分 时 日 月 星期 → `0 3 * * *` = 每天 3:00
- `Ctrl+O` 回车保存、`Ctrl+X` 退出
- 验证：`crontab -l` 能看到这一行；明早 `ls ~/qinglan/backups` 多一个新包

**每月做一次恢复演练**（备份没恢复过 = 没备份）：

```bash
cd ~/qinglan
sudo docker compose stop web worker
sudo rm -rf backend/data && sudo mkdir -p backend/data
sudo tar -xzf backups/<最新包名>.tar.gz -C backend/data --strip-components=1
sudo docker compose up -d web worker
```

- 备份包内路径是 `data/…`，`--strip-components=1` 剥掉这层后正好放回 `backend/data/`；恢复后登录站点抽查一条旧提交还在
- 注意：恢复会**覆盖当前数据**，演练建议用空闲时段或备用机；完整恢复流程以《开发文档.md》M4 验收为准

**异地留存**：每月一次，本机 PowerShell：

```powershell
scp ubuntu@1.2.3.4:qinglan/backups/<最新包名>.tar.gz D:\backup\
```

---

## 7. 日常运维

### 7.1 看日志（出问题的第一动作）

```bash
dc logs -f web        # 实时看 web 日志；按 Ctrl+C 只是退出查看，不会停服务
dc logs --tail=100 worker
dc logs --tail=100 go-judge
```

### 7.2 重启 / 停 / 起

```bash
dc restart            # 重启全部
dc down               # 停站（数据不丢）
dc up -d              # 起站
```

### 7.3 更新代码

- 方式 A（git）：

```bash
cd ~/qinglan && git pull && dc up -d --build
```

- 方式 B（zip）：本机重新上传 zip 后，服务器 `cd ~ && unzip -oq qinglan.zip -d . && cd qinglan && dc up -d --build`
  - zip 里没有 `data/`，覆盖解压**不会碰**你的数据库和学生代码
- 更新中断时间 = 构建的几十秒到几分钟

### 7.4 巡检

```bash
df -h                 # 磁盘用量；Use% 接近 100 要清理
free -h               # 内存
dc ps                 # 三服务状态
```

### 7.5 改端口

编辑 `docker/compose.yml` 里 web 的端口映射（如 `8000:8000` 改成 `80:8000`），云控制台安全组同步放行新端口，然后 `dc up -d`。

---

## 8. FAQ（症状 → 排查 → 解决）

| 症状 | 排查 | 解决 |
|---|---|---|
| 浏览器打不开网站 | 服务器上 `curl http://127.0.0.1:8000` 通不通 | 通 → 防火墙/安全组没放行：云控制台放行 8000；学校机器 `sudo ufw allow 8000/tcp`。不通 → `dc ps` 看 web 是否 Up，`dc logs --tail=50 web` 看报错 |
| 某服务反复 Restarting | `dc logs --tail=50 <服务名>` | 常见原因：`.env` 没填 JWT_SECRET、GO_JUDGE_URL 写错；改完 `dc up -d` |
| 提交一直"判题中" | `dc ps` 看 worker 与 go-judge | go-judge 不在就 `dc restart go-judge`；再看 `dc logs --tail=50 worker` |
| 构建/拉镜像报网络错 | — | 重跑一次；仍失败把报错发给 AI |
| 磁盘满（`df -h` 100%） | `ls ~/qinglan/backups` 看旧包 | `sudo docker system prune -f`（只清闲置镜像，不碰数据与运行中容器）；必要时删最旧备份包 |
| 忘记 admin 密码 | — | `dc exec web python scripts/reset_password.py admin "新密码"` |
| SSH 连不上 | 看报错类型 | timed out = IP/22 端口/关机问题；permission denied = 密码错，控制台重置 |
| 误操作想重来 | 确认没动 `data/` 与 `~/qinglan/backups` | 容器随时可重建：`dc down && dc up -d --build`；数据都在卷里，安全 |

**黄金法则**：只要没执行过 `rm -rf` 去删 `data/` 或 `~/qinglan/backups`，你的数据就是安全的；容器坏了都能重建。

---

## 9. 可选增强（做之前先和项目主人确认，属文档变更）

- **域名 + HTTPS**：在前面加一层 Caddy（自动申请证书），compose 加一个 caddy 服务、配置约 10 行；公网用域名访问建议做，纯内网可不做
- **改用 80/443 端口**：见 7.5，配合域名使用
- **校内起名字**：学校 DNS 或路由器里把内网 IP 绑一个易记名字（如 `qinglan.local` / 校内域名）

---

## 附录 A：命令速查表

| 场景 | 命令 |
|---|---|
| 连服务器（本机） | `ssh ubuntu@你的IP` |
| 进项目目录 | `cd ~/qinglan` |
| 起站 / 停站 / 重启 | `dc up -d` / `dc down` / `dc restart` |
| 看状态 | `dc ps` |
| 看日志 | `dc logs -f web`（Ctrl+C 退出） |
| 更新代码 | `git pull && dc up -d --build` |
| 手动备份 | `sudo ./scripts/backup.sh` |
| 重置 admin 密码 | `dc exec web python scripts/reset_password.py admin "新密码"` |
| 磁盘 / 内存 | `df -h` / `free -h` |
| 编辑配置文件 | `nano 文件名`（Ctrl+O 回车保存，Ctrl+X 退出） |

---

*指南结束。遇到本指南与《开发文档.md》冲突时，以《开发文档.md》为准并提请更新本指南。*
