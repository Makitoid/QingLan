# 青蓝 QingLan · 运行与部署指南

- 版本：v1.2
- 日期：2026-09-20
- 变更：v1.2 修正容器内脚本路径（`dc exec web python scripts/…` 实际会找不到文件）、补 `sqlite3` 依赖与备份 FAQ、补 §4.4 学生账号批量导入与初始密码策略、迁移版本说明与 M5 后的验收项；v1.1 合并原《本地运行手册.md》为本指南第一部分；§4 的 cgroup 处理方式按实测更新（compose 已支持 `cgroup: host`）；v1.0（2026-09-17）服务器部署指南初稿
- 读者：两类都适用，且默认你**没有 Linux 基础**
  - 只想在你自己电脑上跑起来改代码 → 看**第一部分**
  - 要把站点放上服务器给别人用 → 看**第二部分**
- 阅读约定：
  - 命令**逐行**复制执行：粘贴一行、回车、等它跑完，再粘下一行。
  - 没特别标注的命令都在**服务器**上执行；标注"本机"的在你 Windows 上执行。
  - `sudo` = 以管理员身份执行，可能要求输入密码；**输密码时屏幕不显示任何字符，是正常的**，输完回车。
  - 每步给了"预期输出"；对不上就去第一部分 §L6（本机）或 §8（服务器）的 FAQ。
  - 文中 `ubuntu@1.2.3.4` 这类都是示例，替换成你自己的用户名和 IP。
  - 本指南第一部分的所有命令与端口均在本机（Windows + Docker Desktop）实测通过；未实测的部分在 §L7 明确标出。

---

## 0. 总览

### 0.0 先分清两种运行形态

同一个项目有两种跑法，差别不小，别混着看：

| | 本机开发（第一部分） | 服务器生产（第二部分） |
|---|---|---|
| 怎么起 | 一键脚本，或手动开 4 个终端 | `docker compose up -d --build`，3 个容器 |
| 页面从哪来 | Vite 开发服务器，**5173** | web 容器内的静态文件（多阶段构建），**8000** |
| 访问地址 | `http://localhost:5173` | `http://你的IP:8000` |
| API 地址 | `http://127.0.0.1:8000` | 同上（页面和 API 同端口） |
| 判题沙箱 | 单独 `docker run`，**发布 5050** | compose 的 go-judge 服务，**不发布端口** |
| 需不需要 `.env` | 不需要（代码默认值就对） | **需要**，且 `JWT_SECRET` 必须换 |
| 改代码生效 | 后端 `--reload` 自动重启 | 要 `dc up -d --build` 重建镜像 |

一句话：**本机开发看 5173，服务器上访问 8000。**

> 服务器上的 go-judge 之所以不发布 5050：`judge` 网络是 `internal: true`，沙箱出不去外网，这是安全设计（见《开发文档.md》7.7）。本机开发要让原生 worker 连上它，才用 `docker run -p 5050:5050` 单独起一个。

### 0.1 本机开发你要做的事（3 件）

1. 装好 Docker Desktop 和 PowerShell 7
2. 双击 `scripts\start-local.cmd`
3. 浏览器开 `http://localhost:5173`

### 0.2 服务器部署你要做的 7 件事

1. 选一台服务器（学校机器 或 云服务器）并拿到登录方式
2. 从你的 Windows 用 SSH 连上服务器
3. 在服务器上装 Docker
4. 把项目代码弄上服务器
5. 配置并启动（3 个容器：web / worker / go-judge）
6. 初始化管理员账号、浏览器验收
7. 配置每日自动备份，学会看日志与更新

---

# 第一部分 · 本机（Windows）开发运行

---

## L1. 系统由四个进程组成

| 进程 | 作用 | 端口 | 停止方式 |
|---|---|---|---|
| go-judge | C 代码沙箱（编译 + 运行 + 资源限额） | 5050 | `docker rm -f qinglan-judge` |
| 后端 API | FastAPI，接口与数据库 | 8000 | Ctrl+C |
| worker | 轮询待判提交，调用 go-judge | 无 | Ctrl+C |
| 前端 | Vite 开发服务器，`/api` 代理到 8000 | 5173 | Ctrl+C |

数据库是 SQLite 单文件 `backend/data/cg.db`，后端和 worker 共用。

启动顺序必须按上表自上而下：worker 依赖 go-judge，前端依赖后端。

---

## L2. 一次性准备（只做一次）

**Docker Desktop 已启动。** 判题沙箱只能跑在容器里，这一步绕不过去。

**建后端虚拟环境并装依赖**（实测 Python 3.14 可用，`requirements.txt` 全部能装）：

```powershell
cd D:\somehow\GitHub\qinglan\backend
py -3.14 -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

**装前端依赖**（`frontend\node_modules` 已存在则跳过）：

```powershell
cd D:\somehow\GitHub\qinglan\frontend
npm install
```

**构建带编译器的判题镜像**（只需一次，约 5 分钟）：

```powershell
docker compose -f docker\compose.yml build go-judge
```

产出镜像 `qinglan-go-judge:v1.12.3-gcc`。

> **为什么不能直接用官方镜像**：`criyle/go-judge` 是纯沙箱 runner，里面**不含任何编译器**，用它判题时每一次提交都会返回
> `Internal Error: execve: handle: gcc: executable file not found in $PATH`。
> `docker/Dockerfile.gojudge` 的作用就是装上 gcc 和 libc6-dev。这个坑本机与服务器都一样。

---

## L3. 日常启动

### 方式一：一键脚本（推荐，需 PowerShell 7）

双击 `scripts\start-local.cmd`，或在 PowerShell 7 里执行：

```powershell
D:\somehow\GitHub\qinglan\scripts\start-local.ps1
```

脚本会依次完成：检查 Docker 是否就绪（没起就尝试拉起并等待）→ 缺 venv 就建、缺 `node_modules` 就装、缺判题镜像就 build → 起沙箱并健康检查 → 起后端 / worker / 前端 → 逐个 HTTP 探活后打印访问地址。

**幂等**，重复执行只会输出 `[skip]`，不会起出第二份服务。

默认给每个常驻服务开一个独立窗口方便看日志；不想开窗口就用 `-Headless`，改为后台运行并把输出写到 `logs\`（该目录已被 `.gitignore` 忽略）：

```powershell
.\start-local.ps1 -Headless      # 后台运行，日志见 logs\backend.err.log 等
.\start-local.ps1 -NoReload      # 后端不加 --reload（改代码不自动重启）
.\start-local.ps1 -SkipFrontend  # 只起沙箱 + 后端 + worker
.\start-local.ps1 -OpenBrowser   # 完成后自动打开前端
.\stop-local.ps1                 # 全部停止（含移除沙箱容器）
.\stop-local.ps1 -KeepJudge      # 停应用但保留沙箱容器
.\stop-local.ps1 -KeepLogs       # 停止但保留 logs\ 目录
```

> **必须是 PowerShell 7**（本机 `C:\Program Files\PowerShell\7\pwsh.exe`，实测 7.6.6）。脚本首行
> `#requires -Version 7.0`，用系统自带的 5.1 跑会被直接拒绝，不会跑出半吊子状态。
> 用到了 PS7 才有的三元运算符和 `Invoke-WebRequest -SkipHttpErrorCheck`。

> **执行策略**：PS7 本机是 `RemoteSigned`，`.ps1` 可直接运行；Windows PowerShell 5.1 的有效策略是
> `Restricted`，所以保留 `.cmd` 包装（只对该次调用加 `-ExecutionPolicy Bypass`，不改系统设置）。
> 想长期免掉包装可管理员执行 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`。

> **脚本存为 UTF-8 无 BOM**：PS7 读取无 BOM 的 UTF-8 正常。若改用 5.1 跑，无 BOM 会被按 ANSI 解析导致中文乱码，
> 这也是脚本强制要求 PS7 的原因之一。

> **`-Headless` 下 python 调用带 `-u`**：Python 的 stdout 重定向到文件时默认块缓冲，不加 `-u` 日志会长时间是 0 字节，看着像服务没启动。

### 方式二：手动四个终端

以下均在 PowerShell 执行。Git Bash 下把 `.venv\Scripts\python.exe` 换成 `.venv/Scripts/python.exe`。

**① 判题沙箱**

```powershell
docker run -d --name qinglan-judge --restart unless-stopped `
  --privileged --cgroupns=host --shm-size=256m `
  -p 5050:5050 qinglan-go-judge:v1.12.3-gcc
```

验证：`curl http://localhost:5050/version` 返回含 `"buildVersion":"v1.12.3"` 的 JSON 即正常。

**② 后端 API**

```powershell
cd D:\somehow\GitHub\qinglan\backend
.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

验证：浏览器打开 `http://127.0.0.1:8000/docs` 能看到 Swagger。

**③ 判题 worker**

```powershell
cd D:\somehow\GitHub\qinglan\backend
.venv\Scripts\python.exe -m app.judge.worker
```

这个进程**不打印任何日志**，终端空白是正常的，不要以为它没起来。

**④ 前端**

```powershell
cd D:\somehow\GitHub\qinglan\frontend
npm run dev
```

打开 **http://localhost:5173**，用 `admin` / `admin123` 登录（首次登录后请改密码）。

> 若 5173 已被占用，Vite 会自动改用 5174 并在终端提示，按提示的地址访问即可。

---

## L4. 环境变量与数据库

**原生启动不需要 `.env`。** `backend/app/core/config.py` 的默认值对本机开发就是正确的：

- `GO_JUDGE_URL` → `http://127.0.0.1:5050`
- `DATA_DIR` → `backend/data`
- `JWT_SECRET` → `dev-secret-change-me`（仅开发用，上服务器必须换，见 §4）

**在本机用 compose 启动时才需要根目录的 `.env`**，因为 compose 的 `env_file: ../.env` 是强制的，缺文件会直接报错：

```powershell
Copy-Item .env.example .env
```

**数据库迁移**：迁移脚本按序号累加（`backend/migrations/versions/`，当前到 `0004`）。库已经在 head 时日常启动**不需要**跑；但**每次 `git pull` 拉到新代码后要补跑一次**——新版本可能加了字段（如 `0003` 建分组表、`0004` 加暗色主题色），不迁移后端读写新列就会报错：

```powershell
.venv\Scripts\python.exe -m alembic upgrade head
.venv\Scripts\python.exe ..\scripts\init_admin.py
```

`init_admin.py` 幂等：admin 已存在时只打印"跳过创建"，不会覆盖。改密码：

```powershell
.venv\Scripts\python.exe ..\scripts\reset_password.py <用户名> <新密码>
```

因为后端和 worker 同时读写 `cg.db`，动手改库前先备份：

```powershell
Copy-Item data\cg.db "data\cg.db.bak-$(Get-Date -Format yyyyMMdd-HHmmss)"
```

---

## L5. 判题沙箱的两个硬性前提

这一节是本指南里唯一"不照做就 100% 跑不通"的地方，本机与服务器通用，根因写一次。

### L5.1 必须让容器用宿主的 cgroup 命名空间

**症状**：go-judge 容器反复重启，日志里

```
using current cgroup prefix for nesting   scope_name: ""
failed to create cgroup   prefix: ""   error: cgroup path is empty
create environment builder failed   → fatal
```

**根因**（读上游源码确认，不是猜）：cgroup v2 下 go-judge 先尝试连 systemd 的 dbus 建临时 scope；容器里没有
`/var/run/dbus/system_bus_socket`，于是退而读 `/proc/self/cgroup` 的第一行当"当前 cgroup 前缀"。容器有自己独立的
cgroup 命名空间，那一行是 `0::/`，代码切掉开头的 `/` 得到**空串**；而容器内进程是 root，`createAndNestCgroup`
在 root 情况下不走降级分支，直接返回错误 → 主程序 `logger.Fatal`。

**解法**：让容器加入宿主 cgroup 命名空间，那一行就变成 `0::/docker/<容器id>`，前缀非空即正常。三种写法任选：

| 场景 | 写法 |
|---|---|
| 本机手动 `docker run` | 加 `--cgroupns=host` |
| compose（**推荐，仓库已配好**） | go-judge 服务下的 `cgroup: host` |
| 服务器 compose 版本太旧、不认 `cgroup` 键 | 给 dockerd 加全局 `--default-cgroupns-mode=host`，见 §4 |

> `cgroup` 是 compose-spec 的官方键（`05-services.md` 与 JSON schema 均有），语义就是"指定加入哪个 cgroup 命名空间"。
> **`cgroupns_mode` 不是合法键**，写了会被判 `additional properties 'cgroupns_mode' not allowed`。
> 旧文档里"compose 不支持 cgroupns 键"的说法是找错了键名，已于 2026-09-19 更正。

自检你的 compose 认不认这个键（在仓库根执行，不启动任何容器）：

```bash
docker compose -f docker/compose.yml config > /dev/null && echo "OK：支持 cgroup 键"
```

报错提到 `cgroup` → 版本太旧，走 §4 的 dockerd 兜底方案。

### L5.2 镜像里必须有编译器

见 §L2 的说明：官方镜像不带 gcc，必须用 `docker/Dockerfile.gojudge` 构建派生镜像。compose 与本机 `docker run` 都已指向派生镜像 `qinglan-go-judge:v1.12.3-gcc`。

---

## L6. 本机故障排查（FAQ）

| 症状 | 原因 | 处理 |
|---|---|---|
| go-judge 容器 `Restarting`，`cgroup path is empty` | 见 §L5.1 | 加 `--cgroupns=host`；compose 用 `cgroup: host` |
| 每次提交都是 `Internal Error` + `gcc: executable file not found` | 用了官方镜像而非自建镜像 | 见 §L2 重新 build 镜像 |
| 提交后永远不出结果 | worker 没起，或连不上 5050 | 先 `curl http://localhost:5050/version`，再确认 worker 进程在 |
| `Bind for 0.0.0.0:5050 failed: port is already allocated` | 已有另一个 go-judge 容器占着 5050 | `docker ps` 找到后 `docker rm -f <名字>` |
| compose 里 go-judge 起不来但 `docker run` 正常 | 该 compose 不认 `cgroup` 键，或 `judge` 网络 `internal: true` 本就不发布端口 | 按 §L5.1 自检；本机开发要用原生 worker 就单独 `docker run -p 5050:5050` |
| 容器内 `apt-get` 报 500 且提到 `127.0.0.1:7890` | Docker Desktop 继承了宿主机系统代理，而代理已关闭 | 开代理，或 build 时用 `--build-arg http_proxy= --build-arg https_proxy=` 置空 |
| 清华 apt 源返回 403 | 该源对 pool 文件有防盗链 | 换 `mirrors.aliyun.com`（`Dockerfile.gojudge` 已用此源） |
| 前端 `/api` 请求 404 或拒绝连接 | 后端没起，或不在 8000 | `vite.config.ts` 把 `/api` 固定代理到 `http://localhost:8000`，后端换端口要同步改 |
| 双击 `.ps1` 报 `UnauthorizedAccess` | Windows PowerShell 5.1 有效策略是 `Restricted` | 改用 `start-local.cmd`，或装 PS7 用 `pwsh` |
| `-Headless` 日志文件一直 0 字节 | Python/Node 输出重定向到文件时块缓冲 | 脚本已加 `-u`；自己改脚本时别去掉 |

---

## L7. 本机验证边界

**已实测通过：**

- go-judge 在 `--cgroupns=host` 下稳定启动，cpu / cpuset / memory / pids 四个控制器全部可用
- 沙箱限额真实生效：死循环按 `cpuLimit` 判 `Time Limit Exceeded`（exit 9）；狂 malloc 精确卡在 `memoryLimit` 判 `Memory Limit Exceeded`
- 编译错误的 gcc 诊断能从响应的 `files.stderr` 完整取到
- 自建镜像可正常编译含 `<stdio.h>` `<math.h>` 并用 `-lm` 链接的程序
- 后端 `/docs` 与 `admin` 登录接口返回正常；前端 5173 的 `/api` 代理连通后端
- `start-local.ps1`（PS7 版）从干净状态完整起齐四个服务（5050 / 8000 / 5173 均独立复核为 200），重复执行全部走 `[skip]`；`-Headless` 模式日志能正常落盘；`stop-local.ps1` 精确停掉本项目进程并移除沙箱容器
- 背景图连续换图（上传 A → 清除 → 上传 B）：URL 带 `?v=<文件名>` 随上传变化，新 URL 的字节内容即新图，旧 URL 返回 `Cache-Control: no-store`（用临时后端实例验证，不依赖浏览器缓存行为）
- 登录页动态背景（`LoginBackdrop`）亮/暗两态取色、动画运行、无横向溢出已在浏览器实测；前端 `tsc` 与 `npm run build` 通过

**尚未实测：**

- 登录页动态背景的**窄屏（≤640px）观感**未实测：只确认了 `@media (max-width: 640px)` 规则已生效，没有真机或窄窗口走查
- **应用层端到端提交**（学生登录 → 提交代码 → worker 领取 → 判分落库 → 前端看结果）这一整条链路还没有跑通过一次，§L6 里"提交后永远不出结果"那行因此属于推断而非实测结论
- 一个已知行为需要留意：刷屏型程序（输出远超 1MB）在测试中被判为 `Memory Limit Exceeded` 而非输出超限，输出确实在 1MB 处截断。若需要区分"输出超限"，worker 侧的判定要单独处理
- **§L5.1 的 `cgroup: host` 是在 Docker Desktop（WSL2）上验证的**，Ubuntu 服务器上的 compose 行为未实测；本指南第二部分仍保留 dockerd 兜底方案作为退路
- v1.2 对第二部分的两处修正（容器内脚本必须写 `/app/scripts/…`、备份依赖 `sqlite3`）是照 `docker/Dockerfile.web` 与 `scripts/backup.sh` 的源码核对得出的，**没有在真机上跑过**；第一次按本指南部署时请顺手确认这两条成立，不成立就回写本节

---

# 第二部分 · 服务器部署

---

## 1. 服务器选择与准备

### 1.1 二选一

| 方案 | 优点 | 缺点 | 适合 |
|---|---|---|---|
| 学校自有机器（机房 PC/旧电脑装 Ubuntu） | 免费、内网访问快、不用备案 | 校外访问不了、要常开机 | 主要上课机房用 |
| 云轻量服务器（腾讯云/阿里云等） | 校外能访问、稳定 | 约几十到一百元/月 | 宿舍/家里也要交作业 |

**硬性要求（两种方案都一样）**：
- 系统：Ubuntu 22.04 或 24.04 LTS（64 位）
- 虚拟化：物理机或 KVM 虚拟化（**OpenVZ 等老式容器虚拟化跑不了判题沙箱**；云厂商的"轻量应用服务器/ECS"都是 KVM，放心）
- 配置：2 核 4G 内存起步，硬盘 40G 起步
- **不要选 Windows Server**

### 1.2 云服务器购买要点

1. 产品选"轻量应用服务器"或入门级 ECS 即可
2. 镜像选 **Ubuntu 22.04/24.04 LTS**
3. 购买后在控制台做三件事并记录：
   - 记下**公网 IP**
   - 设置/重置**登录密码**（用户名通常是 `ubuntu` 或 `root`，控制台会写）
   - 在"防火墙/安全组"里**放行 TCP 端口 22 和 8000**（22 是 SSH 远程登录，8000 是网站）

### 1.3 学校机器准备要点

1. 用 Rufus 把 Ubuntu 24.04 镜像写进 U 盘安装（官网有教程）
2. 装好联网后，打开终端执行 `ip a`，找到内网 IP（形如 `192.168.x.x` 或 `10.x.x.x`），记下来
3. 开启 SSH 服务（供你的 Windows 远程连）：

```bash
sudo apt install -y openssh-server
sudo systemctl enable --now ssh
```

### 1.4 从你的 Windows 第一次连服务器（SSH）

本机操作：开始菜单搜"PowerShell"打开，执行：

```powershell
ssh ubuntu@1.2.3.4
```

- 第一次连会问 `Are you sure you want to continue connecting (yes/no)?` → 输入 `yes` 回车
- 然后提示输密码：**屏幕不显示字符是正常的**，输完回车
- 成功标志：提示符变成类似 `ubuntu@VM-0-1:~$`，此后你敲的命令都在服务器上执行
- 以后每次运维都从这个窗口开始；断开输入 `exit`

> 连不上的两种典型：`Connection timed out` = IP 错或 22 端口没放行；`Permission denied` = 密码错，去云控制台重置。

---

## 2. 服务器初始化（装 Docker）

SSH 连上后，逐行执行：

```bash
sudo apt update
sudo apt upgrade -y
sudo apt install -y curl git unzip ca-certificates sqlite3
curl -fsSL https://get.docker.com | sh
sudo systemctl enable --now docker
sudo docker run --rm hello-world
sudo timedatectl set-timezone Asia/Shanghai
```

逐行解释：
- 1–2 行：更新系统软件清单并升级（第 2 行要跑几分钟）
- 3 行：装常用小工具。**`sqlite3` 不要删**——第 6 章的备份脚本用它给数据库做一致性快照，缺了会在 `sudo ./scripts/backup.sh` 那一步直接报 `sqlite3: command not found`（脚本是 `set -e`，会当场退出）
- 4 行：Docker 官方一键安装脚本
- 5 行：让 Docker 开机自启并立即启动
- 6 行：**验证**。预期输出里有一段 `Hello from Docker!`，看到即成功
- 7 行：把服务器时区设为北京时间（日志时间才看得懂）

> 失败处理：`apt` 报网络错就重跑一次；第 6 行失败先执行 `curl -I https://www.baidu.com` 看服务器有没有外网。

顺手建一个短命令别名，后面所有 compose 命令都用 `dc` 代替：

```bash
echo "alias dc='sudo docker compose -f docker/compose.yml'" >> ~/.bashrc
source ~/.bashrc
```

---

## 3. 把代码弄上服务器（二选一）

### 方式 A：git 私有仓库（推荐，日后更新一条命令）

1. 本机：在 GitHub（或 Gitee）建**私有**仓库 `qinglan`，把项目推上去（M1 开工时就会建）
2. 本机：GitHub → 头像 → Settings → Developer settings → Personal access tokens → 生成一个 token，权限勾 `repo`，**复制保存（只显示一次）**
3. 服务器：

```bash
git clone https://<你的用户名>:<token>@github.com/<你的用户名>/qinglan.git
cd qinglan
```

> token 等同密码，别贴到公开地方；泄露了去 GitHub 删掉重生成。
> 服务器 clone 卡住几分钟不动 = 网络到 GitHub 不通畅：`Ctrl+C` 放弃，改用方式 B。

### 方式 B：zip 上传（不用 git）

1. 本机：进入 `qinglan` 文件夹，**先删掉** `data/`、`.venv/`、`node_modules/` 三个目录（都是运行时产物，服务器会重新生成；删的是你开发机的副本内容，介意就先复制一份再删）
2. 本机：右键 `qinglan` 文件夹 → 压缩为 `qinglan.zip`
3. 本机 PowerShell：

```powershell
scp qinglan.zip ubuntu@1.2.3.4:~/
```

4. 服务器：

```bash
cd ~ && unzip -q qinglan.zip && cd qinglan
```

> 方式 B 的代价：以后每次更新都要重新上传覆盖（第 7 章有覆盖更新流程）。

---

## 4. 配置并启动

### 4.1 填配置

服务器上，确保在 `~/qinglan` 目录：

```bash
cp .env.example .env
openssl rand -hex 32
```

- 第 2 行会输出一串 64 位随机字符，**复制它**（这是登录令牌的加密密钥）

```bash
nano .env
```

- 用方向键把光标移到 `JWT_SECRET=` 后面，粘贴刚才那串；其余项看不懂就保持默认
- nano 编辑器三步：编辑完按 `Ctrl+O` 再按回车（保存），按 `Ctrl+X`（退出）

### 4.2 确认 cgroup 命名空间这一关能过

判题沙箱需要"宿主 cgroup 命名空间"，原因见第一部分 §L5.1（那里有完整根因）。

**仓库自带的 `docker/compose.yml` 已经配好了**：go-judge 服务下有 `cgroup: host`。所以正常情况下**你不需要做任何额外操作**，直接跳到 4.3。

只有当你的 Docker/Compose 版本太旧、不认这个键时，`dc` 命令会在校验阶段就报错。用这条自检（在 `~/qinglan` 执行，不会启动容器）：

```bash
sudo docker compose -f docker/compose.yml config > /dev/null && echo "OK：继续 4.3，无需改动"
```

- 输出 `OK` → 什么都不用做，跳到 4.3
- 报错里提到 `cgroup`（形如 `additional properties 'cgroup' not allowed`）→ 版本太旧，执行下面这段给 dockerd 设一次全局参数，然后重试自检：

```bash
sudo mkdir -p /etc/systemd/system/docker.service.d
printf '[Service]\nExecStart=\nExecStart=/usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.sock --default-cgroupns-mode=host\n' | sudo tee /etc/systemd/system/docker.service.d/qinglan.conf
sudo systemctl daemon-reload && sudo systemctl restart docker
```

> 注意：重启 docker 会短暂停掉正在运行的容器；若你已起过 QingLan，做完这步要 `dc up -d` 拉回。
> 用了这个全局参数后，compose 里的 `cgroup: host` 与之重复但无害，可保留。

### 4.3 构建并启动

```bash
dc up -d --build
```

- 第一次要下载基础镜像并构建（含在 go-judge 派生镜像里 apt 装 gcc，见 §L5.2），**5–15 分钟**，屏幕持续滚日志是正常的；结束回到命令提示符即完成
- `-d` 表示后台运行

```bash
dc ps
```

- 预期：3 行服务 `web` / `worker` / `go-judge`，STATE 列都是 `Up`（带 `(healthy)` 更好）
- 若 `go-judge` 是 `Restarting` → 回 4.2，或看 `dc logs --tail=50 go-judge` 是否报 `cgroup path is empty`

```bash
dc exec web python /app/scripts/init_admin.py
```

- 预期输出：`admin 账号已存在，跳过创建`。**这不是出错**——web 容器的启动命令里已经自动跑过一次建管理员，所以手动再跑必然是「跳过」；真输出「已创建初始账号 admin/admin123」才说明容器那次没跑成，这条命令就是给它兜底的
- 路径必须写 `/app/scripts/…`：镜像里脚本在 `/app/scripts`，而容器的工作目录是 `/app/backend`，照搬本机的 `scripts/init_admin.py` 会报 `No such file or directory`
- 同理，**数据库迁移也不用你操心**：web 容器每次启动都会先自动执行 `alembic upgrade head`（`docker/Dockerfile.web` 的 CMD），建表、加字段都在那一步完成

**浏览器验收**：打开 `http://你的IP:8000`（学校机器用内网 IP），应看到青蓝登录页。用 `admin / admin123` 登录，**立刻在右上角改密码**。

> 服务器上页面和 API 都在 8000：web 镜像是多阶段构建，前端 `npm run build` 的产物由后端进程按 `STATIC_DIR` 一起托管。这与本机开发（页面 5173、API 8000）不同，别搞混。

### 4.4 导入学生账号与初始密码

**初始密码策略：批量导入的新学生，密码就是自己的学号**（不是 `admin123`，也不是全班统一密码）。导入完把名单发下去，让学生用「学号 / 学号」登录后自己改。

- 入口：admin 登录 → 学生管理 →「导入学生」。支持 `.xlsx` / `.csv` / `.txt`，三列 `学生ID | 姓名 | 组别`：
  - 组别可留空；一行多个组用 `、 ， , ； ; /` 分隔；**不存在的分组会自动创建**
  - `.xlsx` 取第一个工作表的前三列；首行写「学号」或「学生ID」会当表头跳过
  - 文本文件用 UTF-8 或 GBK 编码（Excel「另存为 → CSV UTF-8」即可），单文件上限 5MB
- 结果会显示成功几条、哪几行失败及原因（最常见是「学号重复」——该学号已有账号，**不会覆盖老账号的密码**）。整批一个事务，坏行只跳过、不写坏其它行。
- 忘了密码：学生管理里「重置密码」，或多选后批量重置成同一个新密码。**只有管理员有重置入口**，教师侧任何页面都不会出现密码相关按钮。
- 服务器上想走命令行：`dc exec web python /app/scripts/import_students.py <路径>`，注意文件要先放进挂载进容器的 `backend/data/` 下（脚本路径同样必须带 `/app/`，见 §4.3）。

---

## 5. 上线前验收清单（照《开发文档.md》M1–M5 功能点抽验）

- [ ] 登录页打开：品牌色动态背景正常（登录页不显示管理员上传的背景图），暗色模式切换正常
- [ ] admin 登录成功且已改密码
- [ ] 建 1 个教师、2 个学生账号，并把学生绑定给教师
- [ ] 学生管理「导入学生」跑一个 3 行名单（故意让其中 1 行的学号重复）→ 提示成功 2 / 失败 1 且失败行显示学号；用「学号 / 学号」能登录新生（见 §4.4）
- [ ] 教师建一道题（2 个测试点，1 个标样例），发布为作业
- [ ] 学生提交正确 C 代码 → AC 满分；提交错误代码 → WA 且按通过点给部分分
- [ ] 教师端进场次总览 →「逐学生成绩」→「导出 Excel」能下到 `.xlsx`，表内时间是北京时间（服务器时区已在 §2 设为 Asia/Shanghai）
- [ ] `dc ps` 三服务 Up
- [ ] 管理员后台改一次主题色，全站生效
- [ ] 右上角切到暗色模式后逐页翻一遍：正文不近黑、卡片不近白
- [ ] 右上角头像 →「账号设置」改密码成功；退出后用新密码能登录（首次必须把 admin123 改掉）

任何一条失败 → 第 8 章 FAQ；FAQ 没有 → 把报错原文发给 AI。

---

## 6. 自动备份（必做）

备份脚本是仓库自带（M4 交付）：把 `backend/data/` 整个目录（数据库 `cg.db` + 学生代码 `codes/` + 测试用例 `cases/` + 背景图 `bg/`）打包到 `~/qinglan/backups`，其中数据库用 `sqlite3 .backup` 取一致性快照（所以 §2 必须装上 `sqlite3`），自动只保留最近 7 份。

```bash
chmod +x scripts/backup.sh
sudo ./scripts/backup.sh
ls ~/qinglan/backups
```

- 预期：`~/qinglan/backups` 下出现一个带日期的 `.tar.gz` 包

配置每天凌晨 3 点自动备份：

```bash
crontab -e
```

- 第一次会问选编辑器，选 nano（输入对应数字回车）
- 在文件**最后一行**新增：

```
0 3 * * * cd /home/ubuntu/qinglan && sudo ./scripts/backup.sh >> /var/log/qinglan-backup.log 2>&1
```

- 五个数字含义：分 时 日 月 星期 → `0 3 * * *` = 每天 3:00
- `Ctrl+O` 回车保存、`Ctrl+X` 退出
- 验证：`crontab -l` 能看到这一行；明早 `ls ~/qinglan/backups` 多一个新包

**每月做一次恢复演练**（备份没恢复过 = 没备份）：

```bash
cd ~/qinglan
sudo docker compose stop web worker
sudo rm -rf backend/data && sudo mkdir -p backend/data
sudo tar -xzf backups/<最新包名>.tar.gz -C backend/data --strip-components=1
sudo docker compose up -d web worker
```

- 备份包内路径是 `data/…`，`--strip-components=1` 剥掉这层后正好放回 `backend/data/`；恢复后登录站点抽查一条旧提交还在
- 注意：恢复会**覆盖当前数据**，演练建议用空闲时段或备用机；完整恢复流程以《开发文档.md》M4 验收为准

**异地留存**：每月一次，本机 PowerShell：

```powershell
scp ubuntu@1.2.3.4:qinglan/backups/<最新包名>.tar.gz D:\backup\
```

---

## 7. 日常运维

### 7.1 看日志（出问题的第一动作）

```bash
dc logs -f web        # 实时看 web 日志；按 Ctrl+C 只是退出查看，不会停服务
dc logs --tail=100 worker
dc logs --tail=100 go-judge
```

### 7.2 重启 / 停 / 起

```bash
dc restart            # 重启全部
dc down               # 停站（数据不丢）
dc up -d              # 起站
```

### 7.3 更新代码

- 方式 A（git）：

```bash
cd ~/qinglan && git pull && dc up -d --build
```

- 方式 B（zip）：本机重新上传 zip 后，服务器 `cd ~ && unzip -oq qinglan.zip -d . && cd qinglan && dc up -d --build`
  - zip 里没有 `data/`，覆盖解压**不会碰**你的数据库和学生代码
- 更新中断时间 = 构建的几十秒到几分钟
- **数据库迁移不用手动跑**：web 容器每次启动都会先自动 `alembic upgrade head`（见 §4.3），新版本加的建表/加字段在重建后那几秒内完成
- 但**更新前先备份一次**：`sudo ./scripts/backup.sh`。迁移本身不删数据，万一更新后站点起不来，你还有整包可回滚（恢复流程见 §6）

### 7.4 巡检

```bash
df -h                 # 磁盘用量；Use% 接近 100 要清理
free -h               # 内存
dc ps                 # 三服务状态
```

### 7.5 改端口

编辑 `docker/compose.yml` 里 web 的端口映射（如 `8000:8000` 改成 `80:8000`），云控制台安全组同步放行新端口，然后 `dc up -d`。

---

## 8. FAQ（服务器侧，症状 → 排查 → 解决）

本机开发的问题查 §L6；cgroup 与编译器这两个共性根因见 §L5。

| 症状 | 排查 | 解决 |
|---|---|---|
| 浏览器打不开网站 | 服务器上 `curl http://127.0.0.1:8000` 通不通 | 通 → 防火墙/安全组没放行：云控制台放行 8000；学校机器 `sudo ufw allow 8000/tcp`。不通 → `dc ps` 看 web 是否 Up，`dc logs --tail=50 web` 看报错 |
| 某服务反复 Restarting | `dc logs --tail=50 <服务名>` | go-judge 报 `cgroup path is empty` → 回 §4.2；其余常见原因：`.env` 没填 JWT_SECRET、GO_JUDGE_URL 写错；改完 `dc up -d` |
| 提交一直"判题中" | `dc ps` 看 worker 与 go-judge | go-judge 不在就 `dc restart go-judge`；再看 `dc logs --tail=50 worker` |
| 提交全是 CE / `gcc not found` | `dc logs --tail=50 go-judge` | 判题镜像里没有 gcc → 确认用的是 `qinglan-go-judge:v1.12.3-gcc`（`dc up -d --build` 重建），见 §L5.2 |
| `dc` 命令在校验阶段就报错 | 报错提到 `cgroup` | 该 compose 版本太旧 → 按 §4.2 走 dockerd 全局兜底 |
| 构建/拉镜像报网络错 | — | 重跑一次；仍失败把报错发给 AI |
| 磁盘满（`df -h` 100%） | `ls ~/qinglan/backups` 看旧包 | `sudo docker system prune -f`（只清闲置镜像，不碰数据与运行中容器）；必要时删最旧备份包 |
| 忘记 admin 密码 | — | `dc exec web python /app/scripts/reset_password.py admin "新密码"` |
| `dc exec web python scripts/xxx.py` 报 `No such file or directory` | 容器的工作目录是 `/app/backend`，脚本装在 `/app/scripts` | 路径写全：`dc exec web python /app/scripts/xxx.py`（见 §4.3） |
| `sudo ./scripts/backup.sh` 报 `sqlite3: command not found` | 服务器没装 sqlite3（备份要用它做快照） | `sudo apt install -y sqlite3`，然后重跑；§2 的安装清单已包含 |
| 管理员换了背景图，页面仍显示旧图 | 后端是否 v1.8 之前的版本（背景图 URL 不带 `?v=` 参数） | 更新代码后重建：`dc up -d --build`（见 §7.3）；旧版本上临时可 Ctrl+F5 强制刷新 |
| SSH 连不上 | 看报错类型 | timed out = IP/22 端口/关机问题；permission denied = 密码错，控制台重置 |
| 误操作想重来 | 确认没动 `data/` 与 `~/qinglan/backups` | 容器随时可重建：`dc down && dc up -d --build`；数据都在卷里，安全 |

**黄金法则**：只要没执行过 `rm -rf` 去删 `data/` 或 `~/qinglan/backups`，你的数据就是安全的；容器坏了都能重建。

---

## 9. 可选增强（做之前先和项目主人确认，属文档变更）

- **域名 + HTTPS**：在前面加一层 Caddy（自动申请证书），compose 加一个 caddy 服务、配置约 10 行；公网用域名访问建议做，纯内网可不做
- **改用 80/443 端口**：见 7.5，配合域名使用
- **校内起名字**：学校 DNS 或路由器里把内网 IP 绑一个易记名字（如 `qinglan.local` / 校内域名）

---

## 附录 A：命令速查表

### 本机（Windows + PowerShell 7）

| 场景 | 命令 |
|---|---|
| 一键起全部 | 双击 `scripts\start-local.cmd`，或 `.\start-local.ps1` |
| 后台起（日志进 `logs\`） | `.\start-local.ps1 -Headless` |
| 一键停全部 | `.\stop-local.ps1` |
| 只停应用、留沙箱 | `.\stop-local.ps1 -KeepJudge` |
| 看沙箱日志 | `docker logs -f qinglan-judge` |
| 沙箱健康检查 | `curl http://localhost:5050/version` |
| 进 API 文档 | 浏览器开 `http://127.0.0.1:8000/docs` |
| 打开站点 | `http://localhost:5173`（admin / admin123） |
| 备份数据库 | `Copy-Item data\cg.db "data\cg.db.bak-$(Get-Date -Format yyyyMMdd-HHmmss)"` |

### 服务器

| 场景 | 命令 |
|---|---|
| 连服务器（本机） | `ssh ubuntu@你的IP` |
| 进项目目录 | `cd ~/qinglan` |
| 起站 / 停站 / 重启 | `dc up -d` / `dc down` / `dc restart` |
| 看状态 | `dc ps` |
| 看日志 | `dc logs -f web`（Ctrl+C 退出） |
| 更新代码 | `git pull && dc up -d --build` |
| 手动备份 | `sudo ./scripts/backup.sh` |
| 重置 admin 密码 | `dc exec web python /app/scripts/reset_password.py admin "新密码"` |
| 命令行导入学生名单 | 先把文件放进 `~/qinglan/backend/data/`，再 `dc exec web python /app/scripts/import_students.py /app/backend/data/<文件名>` |
| 磁盘 / 内存 | `df -h` / `free -h` |
| 编辑配置文件 | `nano 文件名`（Ctrl+O 回车保存，Ctrl+X 退出） |

---

*指南结束。遇到本指南与《开发文档.md》冲突时，以《开发文档.md》为准并提请更新本指南。*
