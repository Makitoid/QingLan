---
title: "青蓝 QingLan · 权限与密码体系修改意见"
date: 2026-09-21
status: implemented（v1.2 实施回写 —— 0.3.0 批次 M6 已按本文落地；编号与结论保持 v1.1 原样）

scope: "admin/教师-学生管理域 + 密码安全方案（学校私有部署）"
audience: "产品负责人、后端工程师、权限实现 Agent"
doc_type: "change-proposal / machine-readable"
source_docs: ["开发文档.md v2.4", "backend/app/* 现状代码"]
---

# 青蓝 QingLan · 权限与密码体系修改意见（v1.1 审查修订版）

> 本文档沉淀对 QingLan 现有实现（`backend/app`）在**密码安全**与 **admin/教师-学生管理域**的修改结论，面向**人**与**Agent**双读者：人阅读正文，Agent 依据编号规则（PW / AU / BD / LI / SC / GR / IM / DM / QA）与附录机器可读策略块直接实现与校验。规则编号全局唯一，改动必须保留编号并更新 `status`。
>
> **v1.1 修订说明（2026-09-21 代码审查后）**：初稿引用的现状代码位置已逐条核实无误（teacher.py:472-483、admin.py:183、schemas.py:324、stats.py:113、export.py 固定 5 列、bcrypt cost=12、迁移下一号 0005）。修订项：① DM-05 由 P2 可选升为 **P0 必选**（否则 PW-05 的 7 天过期无法实施）；② PW-08 字符集实为 **56** 字符（JSON 块额外排除了小写 `o`，以 JSON 为准）；③ IM-01 教师列语义由"仅校验"改为**写入 teacher_groups**（用户拍板）；④ PW-06 批量重置增加阈值 20 的"统一/随机"二选一与并行哈希（用户拍板）；⑤ PW 方案**同等适用于教师账号**（用户拍板）；⑥ 存量账号迁移后**一律强制改密**（用户拍板）；⑦ 新增 PW-09 凭证导出、AU-06 审计查看页。所有修订处标注【v1.1】。

---

## 0. 本文档的使用规则

1. 本文档是对现有代码的**修改指令**，不是重新设计；凡未提及的现有行为一律保持不变。
2. **密码方案以用户拍板为准**：新建学生/教师统一初始密码 `12345678`、首次登录强制改密、管理员重置返回 8 位随机密码且登录后再次强制改密（PW-01/PW-04）。【v1.1：范围含教师】
3. **学生归属权限以用户拍板为准（方案 B）**：组别（行政班）是唯一组织概念，由 admin 唯一维护成员；admin 额外维护"组-教师分配"；**教师不得更改组别及其成员，只能在可教的组内拉/移自己的学生名单**（BD 章节）。
4. 开工任一任务前先通读本文档，再精读对应编号章节；发现本文档与代码冲突时停下提问，不自行猜测。
5. 所有数据库变更统一走 **Alembic 迁移 0005**（DM 章节），不得手改表结构。
6. 每个修改项完成后，必须逐条执行对应 QA 验收标准并贴出结果。
7. 引用现状代码位置以说明"改哪里"，实现时以最终代码为准。
8. 【v1.1】**每完成一个独立更新（任务清单中可独立验收的一项）就提交一个 commit，只提交本地、不推送远端**；commit 信息风格与仓库既有历史一致（中文、`fix/feat/docs(scope): 摘要`）。
9. 【v1.1】**本批次版本号固定为 `0.3.0`**：无论期间产生多少 commit，`frontend/package.json` 的 version 在本批次内一律保持 `0.3.0`，不做递增。
10. 【v1.1】本文档与《开发文档.md》主文档在 0.3.0 批次范围内冲突时，**以本文档为准**；批次完成后须把最终状态回写《开发文档.md》§1.3/§5/§6.2/§11/§12（含坑 24 改写）。

---

## 1. 修改总览

优先级：P0 = 本次必须改（安全/计分正确性）；P1 = 本次/0.3.x 排期（体验与职责）；P2 = 可后置打磨。

| 优先级 | 编号 | 修改项 | 主要涉及文件 |
|---|---|---|---|
| P0 | PW-01~09 | 密码安全方案（初始密码/强制改密/随机重置/凭证导出，含教师账号） | auth.py、admin.py、security.py、前端 Guard/改密页 |
| P0 | AU-01~05 | 审计日志（写日志 + 查看 API） | models.py、admin.py、teacher.py、services/audit.py |
| P0 | SC-03/04 | 调分边界校验 + rejudge 清 manual_score | teacher.py、schemas.py |
| P0 | DM-01~05 | 数据模型变更（DM-05 已升必选） | models.py、migrations/0005 |
| P1 | BD-01~07 | 学生归属权限（方案 B） | models.py、admin.py、teacher.py、groups.py、前端 |
| P1 | GR-01~04 | 组别唯一概念：admin 唯一写者，教师只读可教组 | models.py、admin.py、teacher.py、groups.py |
| P1 | LI-01/02 | 学生/教师列表搜索 + 未改密徽标 | admin.py、teacher.py、前端 |
| P1 | SC-01/02 | 成绩总分/每题分（导出对齐） | stats.py、export.py、前端 |
| P1 | IM-01/02 | 导入可选教师列（写 teacher_groups）+ 密码策略对齐 | admin.py |
| P1 | AU-06 | 【v1.1 新增】admin 审计日志查看页 | 前端 admin |
| P2 | LI-03 | 列表分页（可后置） | admin.py、teacher.py |

---

## 2. 密码安全方案（PW）

### 2.1 核心决策

| 编号 | 内容 | 状态 |
|---|---|---|
| PW-01 | **新建账号统一初始密码 `12345678`**（学生单建、批量导入、教师单建一致；`AccountCreate` 的 `password` 字段移除，admin 不再手填初始密码）；明文只用于展示/告知，入库一律 bcrypt hash（cost=12），`must_change_password=1`。【v1.1：范围含教师；`scripts/init_admin.py` 种子 admin 同样置 `must_change_password=1`（admin/admin123 首登强制改密）】 | 用户拍板 |
| PW-02 | 初始密码仅允许**一次登录**：登录响应携带 `must_change_password=true`，前端**路由级拦截**——除强制改密页外所有页面不可访问（拦截式，不是可关闭的弹窗）。后端同步强制：未改密时业务 API 一律 `403 MUST_CHANGE_PASSWORD`。【v1.1 实施注意：拦截必须放行 `/api/auth/login`、`/api/auth/me`、`/api/auth/password` 与匿名的 `/api/settings*`——`/auth/password` 也走 `get_current_user`，若在其内部直接拦截会把改密接口自己锁死；推荐独立依赖或路径白名单。`UserOut` 增加 `must_change_password` 字段，`/auth/me` 返回，供前端刷新后恢复拦截态】 | 用户拍板 + 强约束 |
| PW-03 | 强制改密与普通自助改密统一新密码规则（全角色）：≥8 位、不含空格、不得等于学号（username）/display_name/旧密码/初始密码 `12345678`；`PasswordChangeRequest` 的 `min_length` 由 6 改 8 | 补充【v1.1：范围扩至全角色】 |
| PW-04 | 管理员端"重置密码"（学生与教师同）：系统生成 **8 位随机密码**（PW-08 字符集），响应返回给管理员转告本人；同时写审计（AU-04）、置 `must_change_password=1`、记 `password_updated_at=now`（过期锚点，见 PW-05） | 用户拍板 + 补充【v1.1：含教师，action=`teacher_reset_pw`】 |
| PW-05 | 随机密码**一次性 + 7 天过期**：改密成功后旧密码自然作废（hash 被替换）；自生成起 7 天内未改密则过期。过期判定依赖 `users.password_updated_at`（DM-05，v1.1 升为必选）：登录时若 `must_change_password=1` 且 `password_updated_at` 非空且距今 >7 天 → `403 PASSWORD_EXPIRED`（提示"临时密码已过期，请联系管理员重新重置"）。统一密码 `12345678`（新建/统一重置）**不过期**——这些场景 `password_updated_at` 置 NULL | 补充【v1.1：补数据支撑与判定规则】 |
| PW-06 | 批量重置（仅学生）：前端选中人数 **>20（阈值 `BATCH_RESET_ASK_THRESHOLD=20`，config.py）** 时弹 Dialog 询问二选一：**A. 统一重置为 `12345678`**（整批只算一次 bcrypt，沿用坑 24 优化；`password_updated_at=NULL` 不过期）或 **B. 逐学生独立随机密码**（每行独立 hash + 独立过期时间）；≤20 人直接走 B 随机模式。随机模式下人数 >20 时后端用 **线程池并行哈希（`ThreadPoolExecutor(max_workers=8)`，bcrypt 释放 GIL）**，整批仍单事务提交，接口同步返回；前端全程加载态。两种模式响应均含逐生明细（见 PW-09），审计 detail 记 `mode=unified|random` | 用户拍板【v1.1：阈值/二选一/并行为本次拍板新增】 |
| PW-07 | 密码修改行为（新建/单个重置/批量重置/强制改密成功）全部进审计（AU-04） | 补充 |
| PW-08 | 随机密码字符集以附录 A JSON 的 `charset` 为准：`abcdefghijkmnpqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ23456789`，排除易混字符 `0 O 1 l I o`，**共 56 字符**（56^8 ≈ 9.7e13，配合 bcrypt cost=12 在线爆破不可行）。【v1.1：初稿正文误记为 57/排除 5 个，实际 JSON 多排除了小写 o，以 JSON 为准】 | 补充 |
| PW-09 | 【v1.1 新增】**凭证导出**：① 单个/批量重置接口响应改为返回 JSON 明细 `[{student_id, username, display_name, temp_password, expires_at}]`（统一模式 `temp_password="12345678"`、`expires_at=null`），**前端由 JSON 拼 CSV 下载**（学号, 姓名, 临时密码, 有效期）——不得用 `<a href>` 直链（坑 22：blob 带不了 JWT）；② admin 学生管理页新增"导出初始密码表"：勾选学生后纯前端拼 CSV（数据列表里已有，初始密码恒为 `12345678`），仅对 `must_change_password=1` 的学生有意义，无需新后端接口 | 用户拍板 |

### 2.2 流程约束

```text
[学生/教师首次使用]
  输入学号(工号) + 12345678 → 登录校验通过 → 返回 must_change_password=true
  → 前端强制跳转改密页（其他路由全部重定向；后端业务 API 403 MUST_CHANGE_PASSWORD）
  → 输入新密码（满足 PW-03）→ POST /auth/password 置 must_change_password=0、password_updated_at=now
  → 进入正常页面

[忘记密码]
  找管理员 → 管理员在 [学生管理/教师管理→重置密码] 点击
  → 系统生成 8 位随机密码（PW-04/05）并在响应中返回（可复制/下载 CSV），写审计，置 must_change_password=1
  → 管理员转告本人 → 用随机密码登录 → 强制改密 → 随机密码作废
  → 若 7 天未处理：登录返回 403 PASSWORD_EXPIRED，需管理员重新重置

[批量重置（>20 人）]
  admin 勾选学生 → 前端弹询问：统一 12345678 / 各自随机
  → 后端按模式执行（随机且 >20 人时并行哈希）→ 返回逐生明细 → 前端拼 CSV 下载分发
```

### 2.3 存量数据处理（用户已拍板）

- **全部存量账号（学生、教师、admin）迁移后 `must_change_password=1`**：升级后每个账号下次登录先强制改密一次（安全最优；系统尚未正式投入使用，存量多为测试数据，打扰成本≈0）。迁移注释中写明本选择。
- 不提供"查看当前密码"能力；遗忘一律走重置（PW-04）。

---

## 3. 审计日志（AU）

### 3.1 决策

| 编号 | 内容 |
|---|---|
| AU-01 | 新增 `audit_log` 表（DM-02），本批实现**写日志 + 日志查看 API**（只读，admin 权限） |
| AU-02 | 强制审计动作：重置密码（单/批量，detail 记 mode）、账号停用/启用（is_active 变更，**逐人一条**，含批量启停）、组别成员变更、组-教师分配变更、教师名单拉/移、批量导入、分组删除/改名、成绩手动调分（manual_score 变更）。【v1.1 补 action：`teacher_create_pw`、`teacher_reset_pw`】 |
| AU-03 | 审计记录字段：`actor_id, action, target_type, target_id, detail(JSON), created_at`；独立服务 `services/audit.py` 提供统一 `log_audit(db, actor, action, target_type, target_id, detail=None)`，禁止绕过（业务代码不得直接 insert audit_log） |
| AU-04 | 密码相关调用点必须写审计：新建学生（`student_create_pw`）、新建教师（`teacher_create_pw`）、单个重置（`student_reset_pw` / `teacher_reset_pw`）、批量重置（`student_batch_reset_pw`，detail 含 mode 与人数） |
| AU-05 | 日志只追加不修改不删除（不提供任何 UPDATE/DELETE 路径）；`GET /api/admin/audit_logs?action=&target_type=&limit=&offset=` 供管理员查看，按 created_at 倒序 |
| AU-06 | 【v1.1 新增】admin 前端"审计日志"页（新路由 `/admin/audit`）：表格展示时间/操作者/动作/对象/详情，支持按 action、target_type 筛选与"加载更多"；入口在管理员导航。没有页面的审计日志在实际使用中等于不存在 |

---

## 4. 学生归属权限：方案 B（BD）

### 4.1 总体模型（用户拍板）

学生归属拆成三个互相独立的层，**admin 管前两层，教师只管第三层**：

```text
层 1  组别（行政班）          groups + group_members        唯一写者：admin
层 2  组-教师分配（任教安排） teacher_groups                唯一写者：admin
层 3  教师学生名单            teacher_students              唯一写者：teacher（仅在可教组内拉/移）
```

- **admin**：可修改学生的行政班（组别成员：入组/转组/出组），可修改"组-教师绑定"；**不得**直接操作第三层教师名单。
- **teacher**：不得更改组别及其成员，只能在**自己可教的组**内把学生拉入/移出**自己的名单**。
- 明确废除现有代码中教师改组成员的能力（现状 `POST /teacher/students/group_members`，teacher.py:472-483，已核实）——`group_members` 写操作全部收归 admin。

### 4.2 决策

| 编号 | 内容 |
|---|---|
| BD-01 | **组别成员（层 1）**：维持现有 `groups` / `group_members`，admin 唯一写者（增删学生-组关系、组 CRUD）。教师端对该层**只读**，且只能读到"自己可教的组"（经层 2 过滤），看不到不可教组的成员明细 |
| BD-02 | **组-教师分配（层 2）**：新增 `teacher_groups` 表（DM-04）。admin 接口：`PUT /api/admin/teachers/{tid}/groups`（body: group_ids，全量替换，页面 diff 确认）+ `GET` 查看；保存时写审计（`teacher_group_assign`）。**取代**现有 admin 对学生-教师绑定的写管理 |
| BD-03 | **教师拉名单（层 3，主路径）**：新增 `GET /api/teacher/classes`（返回我可教的组及其成员，只读；停用学生照常返回并带 is_active 供前端标注）；新增 `POST /api/teacher/students/bind_from_class`（body: student_ids，后端校验每个 student_id 均属于我可教的某个组，否则整批 403；拉入我的 `teacher_students`，幂等） |
| BD-04 | **教师按学号添加（兜底）**：**新增**（v1.1 更正：初稿误记为"默认保留"，现状代码无此接口）`POST /api/teacher/students/bind`（body: student_ids，仅校验 `role=student` 的有效账号）——覆盖转学生、旁听等"暂不在组"场景；不暴露全量学生列表（不返回明细，仅按学号精确匹配） |
| BD-05 | **教师移除名单**：新增 `POST /api/teacher/students/unbind`（body: student_ids，仅能从自己名单中移除，幂等）；移除后历史提交/成绩保留、教师统计仍可见；学生本人对历史成绩可见性不变（现状即"场次受众=当前绑定学生动态计算"，解绑后该教师场次从学生端消失，属既有行为） |
| BD-06 | **删除** admin 现有 `PUT /api/admin/teachers/{id}/students`（教师-学生直接绑定写接口）——admin 不再持有第三层写权限。【v1.1 明确：`GET /api/admin/teachers/{id}/students` **保留只读**，admin 仍可查看教师名单，只是不能改】 |
| BD-07 | 所有层 2 / 层 3 变更写审计（`teacher_group_assign` / `teacher_student_bind` / `teacher_student_unbind`）；转组（层 1 变更）也写审计，detail 记原组与新组 |

### 4.3 边界案例（一致性约束）

- 学生转班（admin 调整层 1）：教师已有名单**不受影响**（层 3 是教师主动选择，不随组自动增删）；教师下次"从组拉人"时该生已在新组可见。
- 教师停用/启用：其 `teacher_groups`（层 2）与 `teacher_students`（层 3）全部保留，恢复后原样。
- 管理员移除某组的教师分配：该教师层 3 已有名单**保留**（历史关系不断），只是不能再从该组拉新人；`/teacher/classes` 不再返回该组。
- 一名学生可被多个教师拉入名单（多对多，跨班/多科任课独立存在）。

---

## 5. 学生/教师列表（LI）

| 编号 | 内容 |
|---|---|
| LI-01 | `GET /api/admin/students` 增加可选参数：`?q=`（username/display_name 模糊匹配）、`?group_id=`（按组过滤）；`GET /api/teacher/students` 同样支持 `?q=`；参数缺省时行为与现在完全一致（向后兼容） |
| LI-02 | 学生列表增加 **`must_change` 徽标**（`must_change_password=1` 显示"未改密"），并支持按该状态筛选；教师端"我的学生"同步显示（提示学生尽快处理）。`StudentOut` / `BoundStudentOut` 增加 `must_change_password` 字段 |
| LI-03 | （P2）列表分页 `?limit=&offset=` 可后置实现；学校规模下搜索优先 |

---

## 6. 成绩（SC）

| 编号 | 内容 | 现状 → 目标 |
|---|---|---|
| SC-01 | **总分口径**：新增 `total_score = Σ(每题有效分)`；`stats.student_rows` / 导出两处同步（overview 直方图维持每题口径不变） | 现有 `best_effective_score` 是"最高单题分"（stats.py:113 已核实），多题场次误导教师 |
| SC-02 | 成绩导出 Excel：**"最佳有效分"列更名"最高单题分"**（分值语义不变），**新增"总分"列**，并**每题得分各一列**（列头=题目标题，顺序即题单 seq），未提交为空（openpyxl 空串读回是 None，坑 23） | 现固定 5 列（export.py XLSX_HEADER 已核实） |
| SC-03 | **manual_score 边界校验**：非 null 时须 `0 ≤ manual_score ≤ 该题 full_score`（经 assignment_problems 联查），非法值 422；写审计（`score_manual_adjust`）。【v1.1 明确：**`null` 仍合法=撤销调分**（现状能力，前端有入口），边界校验只作用于非 null 值】 | 现可传任意 float（负分/超满分，schemas.py:324 已核实） |
| SC-04 | **rejudge 清 manual_score**：重判提交时 `manual_score=None, manual_score_updated_at=None`——调分不随旧成绩残留覆盖新判结果 | 现 rejudge 不清（teacher.py:415-432 已核实），存在旧调分覆盖新成绩的计分错误 |
| SC-05 | 有效分优先级沿用现有实现：`manual_score` 非空则覆盖系统分，否则用 `score`（scoring.effective_score_for） | 不变 |

---

## 7. 组别（GR）

### 7.1 现状问题

`groups` 为全站共享单类型组，但教师端 `POST /teacher/students/group_members` 允许对自己绑定学生改任意组成员——组被当"行政班"用时，教师 A 把学生移出"1班"会污染所有教师看到的公共数据。

### 7.2 决策（与方案 B 对齐）

| 编号 | 内容 |
|---|---|
| GR-01 | **组别是唯一组织概念（即行政班）**：维持 `groups` / `group_members` 两表结构不变；**admin 是唯一写者**（组 CRUD、成员增删、批量导入入组） |
| GR-02 | **教师端组别只读**：删除 `POST /teacher/students/group_members`（teacher.py:472-483）及其前端入口；教师仅经 `GET /api/teacher/classes` 查看可教组及成员（经 `teacher_groups` 过滤）；`GET /api/teacher/groups`（全部组只读列表）一并废弃——教师可见的组以 `/teacher/classes` 为准，不再暴露不可教组的存在 |
| GR-03 | 组改名/删除、成员变更写审计（`group_update` / `group_delete` / `group_member_change`）；现有存量组原样保留，不加 `managed_by` |
| GR-04 | 前端的组别管理入口仅出现在 admin 端；教师端"我的学生"页只展示名单（层 3）+ "从班级拉学生"入口，不再出现组别编辑控件 |

---

## 8. 批量导入（IM）

| 编号 | 内容 |
|---|---|
| IM-01 | 导入格式新增**可选"教师"列**（第 4 列，列头 `教师`，值为教师用户名，可多教师、分隔符同组别列）：【v1.1 用户拍板语义】教师用户名精确匹配，不存在则按**失败行**处理；匹配成功且该行**同时给出组别**时，为每个（教师, 组）写入 `teacher_groups`（幂等，已有不重复插）；**教师列非空但该行组别为空 → 失败行**（reason="教师列需与组别列同时提供"，避免出现"校验了却无任何效果"的初稿矛盾）；不写教师-学生名单（层 3 仍由教师经 BD-03/04 自拉）。txt/csv 解析的"第 3 列之后并入组别"现行为改为"**第 4 列起并入教师列**"，xlsx 读取范围 max_col 3→4；旧三列文件行为完全不变 |
| IM-02 | 导入/单建学生的密码策略对齐 PW-01：统一 `12345678`、`must_change_password=1`、写审计（AU-04 `student_create_pw` 逐生一条 + `student_import` 汇总一条）。现状"初始密码即学号"（admin.py:362 `hash_password(username)`）废除 |

---

## 9. 数据模型变更（DM，全部走迁移 0005）

| 编号 | 变更 |
|---|---|
| DM-01 | `users.must_change_password` INTEGER NOT NULL DEFAULT 1；**存量行全部置 1**（2.3 用户拍板：全角色首登强制改密，迁移注释写明） |
| DM-02 | 新表 `audit_log`：`id` PK、`actor_id` FK users.id、`action` TEXT NOT NULL、`target_type` TEXT NOT NULL、`target_id` INTEGER、`detail` TEXT（JSON）、`created_at` TEXT NOT NULL DEFAULT (datetime('now'))；`Index(actor_id)`、`Index(action)`。（现有系统从不删用户，FK 不设 CASCADE，保证日志不可被级联清除） |
| DM-03 | `submissions.manual_score_updated_at` TEXT NULL |
| DM-04 | 新表 `teacher_groups`（组-教师分配，方案 B 核心）：`teacher_id` INTEGER FK users.id（ON DELETE CASCADE）、`group_id` INTEGER FK groups.id（ON DELETE CASCADE）、`created_at` TEXT NOT NULL DEFAULT (datetime('now'))；复合主键 `(teacher_id, group_id)`，`Index(group_id)`。**不引入 `Group.managed_by`** |
| DM-05 | 【v1.1 升为 P0 必选】`users.password_updated_at` TEXT NULL——随机重置时写入（PW-05 的 7 天过期锚点）、用户改密成功时写入（台账）；新建/统一重置置 NULL（不过期）。**没有此列 PW-05 无法实施** |

迁移风格沿用手写、仿 0002/0004，`render_as_batch=True`；执行前 `scripts/backup.sh` 备份，并在含旧数据的库副本上验证 upgrade→downgrade→upgrade 可逆。

---

## 10. 机器可读策略块（附录 A，Agent 直接实现用）

```json
{
  "password": {
    "default_initial": "12345678",
    "applies_to_roles": ["student", "teacher", "admin_seed"],
    "must_change_on_create": true,
    "must_change_on_reset": true,
    "random_reset": {
      "length": 8,
      "charset": "abcdefghijkmnpqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ23456789",
      "charset_note": "56 chars, excludes 0 O 1 l I o; JSON is authoritative over prose",
      "expire_days": 7,
      "expiry_anchor": "users.password_updated_at (NULL = never expires, e.g. unified/initial password)",
      "expired_login_error": "403 PASSWORD_EXPIRED",
      "one_time": true
    },
    "batch_reset": {
      "ask_threshold": 20,
      "modes": ["unified_12345678", "per_student_random"],
      "unified_bcrypt_once": true,
      "parallel_hash_workers": 8,
      "parallel_when": "mode=per_student_random AND count > ask_threshold",
      "response": "JSON list [{student_id, username, display_name, temp_password, expires_at}]; frontend builds CSV (never <a href> direct link, see pitfall 22)"
    },
    "change_rules": {
      "min_length": 8,
      "no_whitespace": true,
      "not_in": ["username", "student_no", "display_name", "old_password", "12345678"],
      "applies_to": "all roles, both forced and voluntary change"
    },
    "credential_export": {
      "reset_flow": "CSV from reset response JSON (学号,姓名,临时密码,有效期)",
      "student_list": "frontend-only CSV of selected must_change students, password column fixed 12345678, no new backend API"
    }
  },
  "login_flow": {
    "response_field": "must_change_password (also on UserOut via /auth/me)",
    "guard": "when true, frontend routes except /change-password and /logout redirect to /change-password; backend rejects business APIs with 403 MUST_CHANGE_PASSWORD",
    "backend_allowlist": ["/api/auth/login", "/api/auth/me", "/api/auth/password", "/api/settings", "/api/settings/bg_image"]
  },
  "audit": {
    "service": "services/audit.py :: log_audit(db, actor, action, target_type, target_id, detail)",
    "required_actions": [
      "student_create_pw", "student_reset_pw", "student_batch_reset_pw",
      "teacher_create_pw", "teacher_reset_pw",
      "user_is_active_change", "group_member_change", "group_update", "group_delete",
      "teacher_group_assign", "teacher_student_bind", "teacher_student_unbind",
      "student_import", "score_manual_adjust"
    ],
    "immutable": true,
    "view_api": "GET /api/admin/audit_logs?action=&target_type=&limit=&offset= (created_at desc)",
    "view_ui": "admin route /admin/audit (AU-06)"
  },
  "binding": {
    "layer1_group_members": "owner=admin only; teacher write paths removed entirely",
    "layer2_teacher_groups": {
      "owner": "admin",
      "api": "PUT /api/admin/teachers/{tid}/groups (full replace, frontend diff confirm) + GET",
      "audit": "teacher_group_assign"
    },
    "layer3_teacher_students": {
      "owner": "teacher",
      "admin_write_removed": "PUT /api/admin/teachers/{id}/students deleted; GET kept read-only",
      "classes": "GET /api/teacher/classes (my teachable groups + members, read-only)",
      "bind_from_class": "POST /api/teacher/students/bind_from_class, rules: [every student_id in one of my teachable groups] else whole batch 403, idempotent",
      "bind_by_id": "POST /api/teacher/students/bind (NEW endpoint), rules: [role=student, active account], fallback only, no roster exposure",
      "unbind": "POST /api/teacher/students/unbind, rules: [student_id in my roster], idempotent"
    }
  },
  "groups": {
    "concept": "唯一组织概念(行政班); 不做教学组/行政班区分",
    "admin_write": ["group_crud", "group_member_change", "import_to_group"],
    "teacher_access": "read-only via /teacher/classes only (teacher_groups filtered); GET /api/teacher/groups deprecated; group_members write forbidden",
    "legacy_removed": "POST /teacher/students/group_members 删除"
  },
  "scoring": {
    "manual_score_range": "null allowed (=clear override); non-null must satisfy 0 <= manual_score <= full_score (via assignment_problems) else 422",
    "rejudge_resets": ["score", "verdict", "judge_log", "worker_id", "manual_score", "manual_score_updated_at", "judged_at", "status->pending"],
    "total_score": "sum(each_problem_effective_score); export adds 总分 column + per-problem columns (header=problem title, order=seq)"
  },
  "import": {
    "columns": "学号|姓名|组别|教师(optional 4th)",
    "teacher_column": "exact username match, multi allowed (same separators as group); missing teacher -> failed row; teacher given without group -> failed row; effect = upsert teacher_groups pairs; never touches teacher_students",
    "txt_csv_merge_rule": "cells beyond col 4 merge into teacher column (was: beyond col 3 into group)",
    "password": "12345678 + must_change_password=1 (replaces legacy hash_password(username))"
  },
  "delivery": {
    "version": "frontend/package.json version stays 0.3.0 for the whole batch",
    "commits": "one local commit per completed update; never push"
  }
}
```

---

## 11. 验收清单（QA）

> 前 15 条为初稿（个别按 v1.1 修订），QA-16~21 为 v1.1 新增。全部通过 + 全量回归后才算批次完成。

| 编号 | 验收动作（手工 + 接口） |
|---|---|
| QA-01 | 新建/导入学生后用 `12345678` 可登录，响应含 `must_change_password=true`；未改密前访问任何业务 API 返回 403 MUST_CHANGE_PASSWORD，而 `/auth/me`、`/auth/password` 可用；改密成功后置 0，全站页面可用 |
| QA-02 | 强制改密拒绝：长度<8、含空格、等于学号/用户名/display_name/旧密码/`12345678` 的输入 → 422；合法新密码 → 成功且 `password_updated_at` 写入 |
| QA-03 | admin 重置单个学生 → 响应返回 8 位随机密码（字符集符合 PW-08 的 56 字符集）、学生 `must_change_password=1`、`password_updated_at=now`、audit_log 新增 `student_reset_pw`（actor/target 正确） |
| QA-04 | 批量重置（≤20 人，随机模式）→ 每生独立随机密码，响应 JSON 明细行数=选中数，前端下载 CSV 含学号/姓名/临时密码/有效期列；audit_log 记 `student_batch_reset_pw`（detail.mode=random） |
| QA-05 | 同一随机密码仅能用一次：强制改密完成后，再用该随机密码登录 → 401 |
| QA-06 | admin 分配/替换教师可教组（PUT teacher_groups）：替换后教师 `GET /teacher/classes` 只返回新组及成员；audit_log 记 `teacher_group_assign`；`PUT /admin/teachers/{id}/students` 已删除 → 404/405，而 `GET` 同路径仍可用（只读） |
| QA-07 | 教师 `bind_from_class`：拉入属于可教组的学生 → 成功且幂等；传入不在可教组的学生 ID → 整批 403 且库零变化；`bind`（学号添加）合法学生 → 成功，响应不泄露学生明细 |
| QA-08 | `GET /admin/students?q=张&group_id=1` 与 `GET /teacher/students?q=` 过滤结果正确；不传参数时结果与旧接口一致 |
| QA-09 | 学生列表/教师学生列表显示"未改密"徽标，可按状态筛选 |
| QA-10 | 多题场次：`student_rows.total_score` = 各题有效分之和；导出 xlsx 含"最高单题分"（更名）与"总分"列及每题得分列（列头=题目标题、顺序=seq），分值正确，未提交为空 |
| QA-11 | 调分传 -5 / 超满分数值 → 422；传 null → 撤销调分成功（现状能力保留）；合法调分 → 写审计 `score_manual_adjust`、`manual_score_updated_at` 更新 |
| QA-12 | rejudge 后 `manual_score` 与 `manual_score_updated_at` 均为 NULL，成绩回到系统判分 |
| QA-13 | 教师调用已删除的 `POST /teacher/students/group_members` → 404/405；`GET /teacher/groups` → 404/405；admin 改组成员 → 成功且写审计；教师界面无任何组别编辑控件 |
| QA-14 | 导入文件含"教师"列：学生建号、入组，且 `teacher_groups` 新增对应行；教师用户名不存在 → 失败行；教师列有值但组别为空 → 失败行（reason 明确）；旧三列文件行为不变；不自动写教师名单 |
| QA-15 | 迁移 0005：`alembic upgrade head` 成功、`teacher_groups`/`audit_log` 表存在、**全部存量账号** `must_change_password=1`；在含真实数据的库副本上 upgrade→downgrade→upgrade 可逆；`pytest` 全量回归通过 |
| QA-16 | 【v1.1】随机密码过期：把某生 `password_updated_at` 改到 8 天前（`must_change_password=1`），用该随机密码登录 → 403 PASSWORD_EXPIRED；admin 重新重置后可登录。统一密码/新建账号（`password_updated_at=NULL`）不受 7 天限制 |
| QA-17 | 【v1.1】批量重置 21 人：前端弹"统一 12345678 / 各自随机"询问；统一模式 → bcrypt 只算一次（monkeypatch 计数守护）、全员 `must_change_password=1`、CSV 密码列全为 12345678、审计 detail.mode=unified；随机模式 → 21 个互不相同的密码、走线程池并行（500 人压测接口 <60s） |
| QA-18 | 【v1.1】教师账号同规则：新建教师无需填密码、初始 `12345678` + 强制改密；admin 重置教师密码 → 随机 8 位 + 审计 `teacher_reset_pw`；种子 admin 首登（admin/admin123）被强制改密 |
| QA-19 | 【v1.1】admin 审计页 `/admin/audit`：可见上述操作记录，按 action/target_type 筛选正确，"加载更多"分页可用；无任何修改/删除审计的入口 |
| QA-20 | 【v1.1】学生列表勾选 → "导出初始密码表" CSV（学号,姓名,初始密码=12345678），纯前端生成、不发新请求 |
| QA-21 | 【v1.1】前端 `npm run build`（含 tsc）0 错误；`frontend/package.json` version=0.3.0；暗色模式走查新页面（改密页/审计页/分配界面）无硬编码色；本批次每个独立更新均有对应本地 commit 且未推送 |

---

## 12. 实施结果回写（v1.2，2026-09-21，0.3.0 批次 M6）

> 编号与本文结论一字未改；本节只登记"做到哪一步、证据在哪、哪些是实现的取舍"。逐条 QA 的实测记录写在《开发文档.md》§9 M6 的验收小节。

| 编号 | 状态 | 落地位置 |
|---|---|---|
| PW-01~09 | done | `core/security.py`（`generate_temp_password` / `generate_unique_temp_passwords` / `hash_password_many` / `validate_new_password` / `temp_password_expires_at` / `is_temp_password_expired`）、`api/auth.py`（登录标志 + `PASSWORD_EXPIRED` + 改密）、`api/admin.py`（建号去密码、单/批重置双模式与明细响应）、`services/groups.py` + 导入路径、前端 `/change-password` 与 `RequirePasswordChanged`、`components/credentialCsv.ts` + `CredentialDialog.tsx`、`scripts/init_admin.py` |
| AU-01~06 | done | `models.AuditLog` + 迁移 0005、`services/audit.py::log_audit`、admin/teacher/auth 各写操作调用点、`GET /api/admin/audit_logs`、前端 `/admin/audit`（导航入口在 `Layout`） |
| BD-01~07 | done | `models.TeacherGroup`、`GET/PUT /api/admin/teachers/{tid}/groups`、`GET /api/teacher/classes`、`POST /api/teacher/students/bind_from_class` / `bind` / `unbind`；`PUT /api/admin/teachers/{id}/students` 已删（GET 保留只读）、`POST /api/teacher/students/group_members` 已删 |
| GR-01~04 | done | 组别唯一写者收归 admin（`apply_membership_changes` + 审计）；`GET /api/teacher/groups` 已删；教师「我的学生」页只剩名单维护 + 从班级拉人，无任何组别编辑控件 |
| LI-01/02 | done | `?q=` / `?group_id=` / `?must_change=`（admin students）、`?q=`（admin teachers、teacher students）；`StudentOut`/`BoundStudentOut`/`TeacherOut` 带 `must_change_password` + 前端「未改密」徽标与筛选 |
| LI-03（P2） | **未做** | 分页按本文 §5 的判定后置；学校规模下先靠搜索 |
| SC-01~04 | done | `services/stats.py`（`total_score` + `problem_scores`，`best_effective_score` 口径不变）、`services/export.py`（更名 + 总分列 + 动态每题列）、`api/teacher.py`（调分边界含 `null`=撤销、rejudge 清 `manual_score*`）；SC-05 未动 |
| IM-01/02 | done | `api/admin.py` 解析层（第 4 列教师、`max_col` 3→4、txt/csv「第 4 列起并入教师列」）+ `assign_teacher_groups` 幂等写层 2 + 两条失败行规则 + 统一初始密码 |
| DM-01~05 | done | 迁移 `0005_password_audit_binding.py`，已在含真实数据副本上 upgrade→downgrade→upgrade 可逆，存量 14 账号一律 `must_change_password=1` |
| QA-01~21 | 逐条已跑 | 见《开发文档.md》§9 M6 验收小节；其中 QA-17 的 500 人压测实测 11.4s（<60s） |

**实现取舍（规格未逐条点名处，按此定稿）**：

1. 教师重置密码复用 `TempCredentialOut`，其中 `student_id` 承载的是 `users.id`（教师场景即教师 id）；不改字段名以免破坏 PW-09 的明细契约。
2. 审计 `target_type` 取值：账号启停用 `student`/`teacher`、组相关用 `group`、调分用 `submission`、名单拉移用 `teacher`（目标是"该教师的名单"）、`student_import` 用 `user`；`target_id` 在多对象操作（批量重置、导入、跨多组的成员变更）时留 `NULL`，细节全在 `detail`。
3. 撤销调分（`manual_score=null`）时 `manual_score_updated_at` 一并置 `NULL`——无覆盖即无时间戳。
4. 强制改密拦截做在 `get_current_user` 内部 + 路径白名单（`/api/auth/login|me|password`、`/api/settings`、`/api/settings/bg_image`），而不是每个接口各挂一个独立依赖：新接口不会漏挂。前端另有路由级 gate，两道并行。
5. 教师「我的学生」列表仍返回该生所属的组名（`BoundStudentOut.groups`）。BD-01 禁止的是暴露**不可教组的成员明细**，单个学生挂在哪些组名上不算成员明细；教师可见的组入口依旧只有 `/teacher/classes`。
6. `GET /api/admin/audit_logs` 的 `limit` 越界（>200 或 <0）按 422 `VALIDATION_ERROR` 处理，不静默夹紧；`must_change` 同理。

**本批次遗留（另案）**：教师「逐学生成绩」页的下钻链接一直为空——前端 `types.ts` 声明了 `last_submission_id?` 并按它渲染 `Link`，但后端 `StudentRowOut` 从不返回该字段（M5 遗留缺陷，修复需给 `student_rows` 增列并连带调整导出与测试口径，故未并入本批次）。

---

## 附录 B：涉及改动文件清单（v1.1 修订）

| 文件 | 改动 |
|---|---|
| `backend/app/core/config.py` | 新增 `BATCH_RESET_ASK_THRESHOLD=20`、`TEMP_PASSWORD_EXPIRE_DAYS=7`、`BCRYPT_PARALLEL_WORKERS=8` |
| `backend/app/core/security.py` | 新增 `generate_temp_password()`（secrets，56 字符集）、`validate_new_password()`（PW-03）；强制改密拦截依赖（含白名单，PW-02） |
| `backend/app/services/audit.py` | 【新文件】`log_audit()` 统一入口（AU-03） |
| `backend/app/models.py` | `User.must_change_password`、`User.password_updated_at`、`AuditLog`、`TeacherGroup`、`Submission.manual_score_updated_at`；不加 `Group.managed_by` |
| `backend/app/schemas.py` | `UserOut/TokenResponse.must_change_password`、`PasswordChangeRequest` min_length 8、`ManualScorePatch` 边界说明、`AuditLogOut`、`TeacherGroupsRequest`、`BindStudentsRequest`（复用）、重置响应 `ResetResultOut`/`BatchResetResultOut`（PW-09 明细）、`StudentOut/BoundStudentOut.must_change_password`、`AccountCreate` 移除 password、`StudentRowOut.total_score` |
| `backend/app/api/auth.py` | 登录响应加标志；PASSWORD_EXPIRED 判定；改密走新校验、成功后置 0 + 写 `password_updated_at` + 审计 |
| `backend/app/api/admin.py` | 建学生/教师去 password 字段 + 置标志 + 审计；单/批量重置改随机或统一（阈值/并行/明细响应）；**删 `PUT /admin/teachers/{id}/students`**（GET 保留）；新增 `GET/PUT /admin/teachers/{tid}/groups`；`GET /admin/audit_logs`；组员管理/导入审计；列表 `?q=&group_id=&must_change=`；导入第 4 列教师 + 初始密码改 12345678 |
| `backend/app/api/teacher.py` | 新增 `GET /classes`、`POST /students/bind_from_class`、`/students/bind`、`/students/unbind` + 审计；**删 `POST /students/group_members` 与 `GET /groups`**；`GET /students` 加 `?q=`；rejudge 清 manual_score；调分边界 + 审计 |
| `backend/app/services/groups.py` | 移除教师写组员路径相关复用；导入 `teacher_groups` upsert 辅助 |
| `backend/app/services/stats.py` | `student_rows` 加 `total_score` 与每题有效分（SC-01） |
| `backend/app/services/export.py` | 表头改"最高单题分"+新增"总分"+动态每题列（SC-02） |
| `backend/migrations/versions/0005_*.py` | DM-01~05 全量迁移（含 `teacher_groups`、`audit_log` 建表，存量行 must_change=1） |
| `backend/scripts/init_admin.py` | 种子 admin 置 `must_change_password=1` |
| `backend/tests/*` | 【v1.1 影响面】`test_groups_api.py` 中教师 group_members 4 例、批量重置单 hash 2 例、PUT teachers/students 相关用例将失效，按新规则重写；新增密码流程/审计/绑定三层/调分边界/导入教师列测试；全量回归通过 |
| `frontend/src/...` | 强制改密路由 `/change-password` + Guard 拦截；重置结果展示/CSV 下载（PW-09）；批量重置询问 Dialog（阈值 20）；学生列表"导出初始密码表"；未改密徽标 + 搜索框；admin"教师-组分配"界面（diff 确认）+ 审计日志页（AU-06）；教师端"从班级拉学生"界面、移除组别编辑入口与创建表单密码框；`api/index.ts` 清理废弃端点；`package.json` version=0.3.0 |
