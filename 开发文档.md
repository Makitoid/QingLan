# 青蓝 QingLan · 开发文档（AI-Native）

> 正式名：**青蓝（QingLan）**——取"青出于蓝而胜于蓝"（学生超越老师）；仓库/包名 `qinglan`；"蓝"呼应 Fluent 2 蓝色主色（#0F6CBD）。

- 版本：v2.7
- 日期：2026-09-23
- 状态：0.2.x 已交付；0.3.0 批次（M6 权限与密码体系，实施依据为仓库根《权限与密码体系修改意见.md》v1.2）已实施完成、QA-01~21 逐条取证齐备（未推送）；**0.3.1 批次（M7 本机开发端口自适应）已完成**
- 适用读者：① 执行开发的 AI 助手；② 项目主人（网页开发零基础，本文所有术语在附录 A 有解释）
- v1.1 变更：贯彻"优先开源、不造轮子"——判题沙箱改为开源 go-judge 服务；新增 Alembic / CodeMirror / recharts / react-markdown / dayjs 等选型；记录整套开源 OJ 的评估结论（2.1）

---

## 0. 本文档的使用规则（最重要的一章）

### 0.1 对 AI 的规则

1. **本文档是唯一事实来源（Single Source of Truth）。** 表名、字段名、API 路径、枚举值、判定规则一律以本文档为准，不得"顺手优化"。
2. 开工任何一个任务前，**先通读全文**，再精读对应里程碑章节。
3. 发现文档与代码冲突、或文档自身矛盾时：**停下，向用户提问**，不要自行猜测修改。
4. 需要引入本文档未列出的依赖、或修改表结构/API 时：**先提出变更，用户确认并更新本文档后**，再改代码。
5. **开源优先**：任何功能动手自写前，先检查 2.1 选型表与开源生态是否已有成熟方案；确需自写的，仅限 2.1 中列明的"刻意自写"三项。
6. 每个里程碑结束前，必须逐条执行该里程碑的"验收标准"，并把每条的执行结果（命令 + 输出摘要）贴给用户。
7. 代码注释默认不写；只在本文明确要求的地方写。
8. **每完成一个独立更新（任务清单中可独立验收的一项）就提交一个 commit；只提交本地，不推送远端**。commit 信息用中文、风格与仓库既有历史一致（`feat/fix/docs(scope): 摘要`）。

### 0.2 对人的规则（项目主人）

- 你不需要读懂全部细节。每个里程碑的"验收标准"是给你手工点按验证用的，照着做即可。
- 想改需求时：直接告诉 AI"修改开发文档第 X 章的 Y"，让 AI 先改文档、你确认后再改代码。

### 0.3 与 AI 协作的开场白模板（复制即用）

```
你要开发 青蓝 QingLan 项目（仓库 qinglan）。请先完整阅读《开发文档.md》，
然后执行里程碑 M{n} 的任务清单第 {k} 项：{任务标题}。
完成后按 M{n} 的验收标准逐条自测，并贴出每条结果。
遇到文档未覆盖的情况，停下问我。
```

---

## 1. 项目概述

### 1.1 一句话描述

面向一所学校的 **C 语言作业提交与自动测评打分平台**：教师发布作业/测试，学生提交 `.c` 代码，系统自动编译、沙箱运行、比对输出并打分，教师查看统计并放分。

### 1.2 参考与边界

- 参考对象：希冀 Educg（course.educg.net）的作业/评测部分。
- **不做**：课程章节资源、开放课程展示、云实验、排行榜、代码查重、移动端。

### 1.3 三种角色与核心流程

| 角色 | 能做什么 |
|---|---|
| admin（管理员） | 创建/停用教师与学生账号（**不收密码**：统一初始密码 + 首登强制改密）、重置密码（单个/批量返回一次性随机临时密码，可下载凭证 CSV；批量 >20 人可选「统一初始密码」或「各自随机」）、xlsx/txt/csv 四列（`学生ID\|姓名\|组别\|教师`）导入学生、分组管理（新建/重命名/删除，全站共享，admin 是组及成员的唯一写者）、学生批量加入/移出分组、批量启停、**分配教师可教哪些组**（层 2）、查看审计日志、配置全站主题色与背景图 |
| teacher（教师） | 维护题库（题目+测试用例、题库搜索与 `group_name` 自由文本分组、行内删题、题目编辑草稿）、发布作业/考试、查看统计、下钻个人提交、调分/重判、放出测试结果、「我的学生」页（`/teacher/students`：维护自己的名单——从可教班级拉人/按学号兜底拉人/移出，组别对本角色只读）、导出场次成绩 xlsx |
| student（学生） | 查看自己被布置的场次、读题（含样例输入输出）、提交代码、按反馈策略查看判定与分数 |

> **红线**：**重置密码（含批量）仅管理员具备**；教师端没有任何重置密码入口（见 6.2 教师接口下的红线说明），教师只能改自己的密码。**学生归属分三层**（0.3.0）：组别（层 1）与组-教师分配（层 2）由 admin 唯一维护，教师名单（层 3）由教师本人唯一维护，admin 只读；三层之间不做级联增删。

核心流程：

```
admin 建账号（统一初始密码，首登强制改密）→ admin 建组并把学生分进组 → admin 分配教师可教哪些组
   → teacher 在自己可教的组里拉出「我的学生」名单
   → teacher 建题（题面 + 测试用例，标记哪些是样例）
   → teacher 发布场次（作业 或 测试：时间窗、提交次数、计分策略）
   → student 在窗口内提交 .c
   → judge worker 调 go-judge 沙箱：编译 + 逐测试点运行 → 比对输出 → 写回判定与分数
   → 作业：学生即时可见详情；测试：结束后 teacher 点"放出"才可见
   → teacher 看总览/下钻/调分/重判
```

### 1.4 关键产品决策（已定稿，勿再讨论）

1. 测试模式下学生提交后**看不到任何判定细节**，由教师在结束后手动"放出"统一可见。
2. 作业模式即时显示逐测试点判定。
3. 测试用例分**样例**（学生可见）与**隐藏**（仅判分）两类。
4. 场次受众 = 该教师**当前绑定**的学生（动态计算，不快照）。
5. 计分策略 per 场次：`best`（取历次最高）或 `last`（取最后一次），默认 best。
6. 比对模式三选一：`exact`（字节级）/ `trim`（忽略行尾空白与末尾空行，**默认**）/ `float`（浮点容差）。
7. 视觉风格：Microsoft **Fluent Design 2**。
8. 组件/功能开源优先，不造轮子（见 2.1）。
9. 暗色模式全适配：默认跟随系统偏好，手动切换覆盖并持久化；一切自定义样式取色于主题令牌，禁止硬编码。
10. 主题色、网页背景图及其透明度由管理员在后台配置（`site_settings` 单行表），全站生效；**登录页例外**——它使用独立的品牌色动态背景，不显示该背景图（见 8.1/8.2）。

---

## 2. 技术栈

| 层 | 选型 | 版本基线 | 理由 |
|---|---|---|---|
| 后端 | Python + FastAPI | Python 3.11、FastAPI ≥0.110 | 代码量少；worker 同语言 |
| ORM/库 | SQLAlchemy 2.0 + SQLite | SQLite ≥3.35（需要 RETURNING） | 单文件零运维；开 WAL 模式 |
| 迁移 | Alembic | 与 SQLAlchemy 配套 | 表结构演进有版本可回滚 |
| 认证 | PyJWT（HS256）+ bcrypt | — | 库实现，不手写密码学 |
| Excel 读写 | openpyxl | ≥3.1 | 学生导入解析 .xlsx、成绩导出生成 .xlsx（MIT 许可） |
| 前端 | React 18 + TypeScript + Vite | Vite 5 | Fluent 2 官方实现是 React |
| UI 库 | @fluentui/react-components + @fluentui/react-icons | v9 | 官方 Fluent 2：令牌/主题/亮暗模式/图标现成 |
| 代码编辑器 | @uiw/react-codemirror（CodeMirror 6） | — | 提交框与代码查看，C 语法高亮 |
| 图表 | recharts | — | 分数分布直方图 |
| Markdown | react-markdown | — | 题面渲染，默认不渲染原始 HTML |
| 时间处理 | dayjs | — | 前端格式化/时区 |
| 判题沙箱 | **go-judge**（开源沙箱执行服务） | 见 2.1 | 隔离执行不可信二进制，返回耗时/内存/退出状态 |
| 判题编排 | 自研 worker（同仓库独立进程） | — | 领任务循环 + 调 go-judge + 三模式比对 |
| 部署 | Docker Compose 单机 | — | M4 |

**明确不引入**：Redis、消息队列、MySQL（人数上千再议）、Nginx（Compose 内 web 容器直接暴露端口即可）、整套开源 OJ 套件（Hydro/QDUOJ/SDUOJ/HUSTOJ，评估结论见 2.1）、Judge0（备选，默认不装，见 7.5）。

### 2.1 开源选型清单（优先开源，不造轮子）

| 功能 | 开源选型 | 仓库 | 承担职责 |
|---|---|---|---|
| 沙箱执行器 | go-judge | github.com/criyle/go-judge | 在隔离环境（命名空间+seccomp+资源限制+无网络）运行不可信二进制；返回 time/memory/exitStatus/stdout |
| 判题备选 | Judge0 CE | github.com/judge0/judge0 | 仅当 go-judge 经评估不可用时，经适配器接口切换；默认不装 |
| 数据库迁移 | Alembic | github.com/sqlalchemy/alembic | SQLite schema 版本管理 |
| Fluent 2 UI | Fluent UI v9 | github.com/microsoft/fluentui | 组件、主题、令牌、图标 |
| 代码编辑器 | CodeMirror 6（React 封装 @uiw/react-codemirror） | github.com/codemirror / github.com/uiwjs/react-codemirror | 提交框、代码只读查看 |
| 图表 | recharts | github.com/recharts/recharts | 分数分布 |
| Markdown | react-markdown | github.com/remarkjs/react-markdown | 题面 |
| 时间 | dayjs | github.com/iamkun/dayjs | 前端时间 |
| 后端基础 | FastAPI / SQLAlchemy / Pydantic / PyJWT / bcrypt / pytest | 各自官方 | 框架、ORM、校验、令牌、哈希、测试 |
| 部署 | Docker / Docker Compose | — | 容器化 |
| 色阶生成 | chroma-js | github.com/gka/chroma.js | 由管理员设定的主题色插值生成 Fluent brandRamp；也从背景图里挑主题色 |
| 图片压缩 | browser-image-compression | github.com/Donaldcwl/browser-image-compression | 超过 5MB 的背景图在浏览器本地压缩（Canvas + Web Worker），不占服务端资源、不造轮子 |
| Excel 读写 | openpyxl（≥3.1，MIT） | github.com/openpyxl/openpyxl | 服务端解析学生导入的 `.xlsx`；生成场次成绩导出的 `.xlsx`（导入解析仍兼容 `.txt`/`.csv` 纯文本） |

**评估过但不采用整套开源 OJ**（Hydro、QDUOJ/OnlineJudge、SDUOJ、HUSTOJ）：
① 需求错位——作业/测试双模式与"结束后放分"策略、教师↔学生绑定模型、Fluent 2 视觉，都要对框架做大手术；
② 代码库体量大（数万行至数十万行），AI agent 读取上下文与精准修改的成本远高于小自研库；
③ 许可与二次分发约束不一。
结论：走"**小自研业务代码 + 独立开源判题服务**"的组装路线。

**刻意自写（开源没有对应物，合计 <300 行且全单测覆盖）**：
1. `compare.py`：exact/trim/float 三模式比对——本校"按字符比对造成假 WA"的核心痛点需求，开源判题器均不提供 trim/float 语义；
2. `visibility.py`：反馈放出策略（6.4）；
3. 绑定模型与受众计算（teacher_students + 场次受众）。

---

## 3. 环境准备（零基础分步）

### 3.1 Windows 开发机

1. 安装 Git：官网安装包，一路默认。验证：`git --version`
2. 安装 Python 3.11：官网安装包，**勾选 "Add python.exe to PATH"**。验证：`python --version`
3. 安装 Node.js 20 LTS：官网安装包默认。验证：`node --version`、`npm --version`
4. 安装 Docker Desktop（含 WSL2 后端）：安装后重启，打开 Docker Desktop 等到左下角绿色 "Engine running"。验证：`docker run --rm hello-world`
   - **go-judge 沙箱服务必须跑在 Linux 内核上**：Windows 开发机用 Docker Desktop 容器运行即可；web 与 worker 因不直接接触学生二进制，可在 Windows 本机跑。
5. 编辑器：VS Code。

### 3.2 启动四件套（开发期）

```bash
# 终端0：go-judge 沙箱服务（已按官方 README 核实：仓库为 criyle/go-judge，
#        REST API 端口 5050（5051 为 gRPC 且默认关闭），固定镜像 tag v1.12.3）
#        宿主机侧若报 EACCES，说明 5050 落在 Windows 开机保留段里，换端口并同步 GO_JUDGE_URL（start-local.ps1 会自动顺延）
docker run -d --name go-judge --restart unless-stopped \
  --privileged --shm-size=256m -p 127.0.0.1:5050:5050 \
  criyle/go-judge:v1.12.3

# 终端1：后端
cd backend
python -m venv .venv && .venv\Scripts\activate      # PowerShell 用 .venv\Scripts\Activate.ps1
pip install -r requirements.txt
alembic upgrade head                                  # 建表/迁移
uvicorn app.main:app --reload --port 8000
# 浏览器打开 http://localhost:8000/docs 可看到自动生成的接口文档

# 终端2：判题 worker（只经 HTTP 调 go-judge，Windows 本机可跑）
python -m app.judge.worker

# 终端3：前端
cd frontend
npm install
npm run dev        # http://localhost:5173（被系统保留时读 backend/data/dev-ports.json 里脚本记录的顺延端口）
```

配置项（环境变量）：`GO_JUDGE_URL` 默认 `http://127.0.0.1:5050`（本机默认端口被 Windows 保留段占用时，改读 `backend/data/dev-ports.json` 中 `start-local.ps1` 记录的顺延端口；compose 显式覆盖为 `http://go-judge:5050`）；`JWT_SECRET`；`DATA_DIR` 默认 `backend/data`。

### 3.3 目录与数据约定

- 运行时数据（数据库、学生代码、用例文件）一律放 `backend/data/`，**不进 git**（.gitignore）。
- 时间一律存 ISO 字符串 `YYYY-MM-DD HH:MM:SS`（UTC 存储，前端按本地时区显示）。

---

## 4. 仓库结构

```
qinglan/
├─ 开发文档.md                  # 本文件
├─ backend/
│  ├─ requirements.txt
│  ├─ alembic.ini  migrations/  # Alembic 迁移
│  │  └─ versions/0005_password_audit_binding.py  # 最新迁移：must_change_password/password_updated_at + audit_log + teacher_groups + manual_score_updated_at
│  ├─ app/
│  │  ├─ main.py               # FastAPI 入口、路由挂载
│  │  ├─ core/
│  │  │  ├─ config.py          # 配置：JWT 密钥、数据目录、GO_JUDGE_URL、判题参数
│  │  │  ├─ db.py              # engine(WAL)、Session 依赖
│  │  │  └─ security.py        # bcrypt 哈希/校验（含线程池并行哈希）、JWT 签发/解析、角色依赖、随机临时密码与密码策略、未改密拦截（白名单）
│  │  ├─ models.py             # 全部 SQLAlchemy 表（第 5 章 DDL 的映射）
│  │  ├─ schemas.py            # Pydantic 请求/响应模型
│  │  ├─ api/
│  │  │  ├─ auth.py  admin.py  teacher.py  student.py
│  │  ├─ services/
│  │  │  ├─ audit.py           # 统一审计入口 log_audit()（只 add 不 commit；业务代码禁止直接 insert audit_log）【自写】
│  │  │  ├─ visibility.py      # 反馈可见性规则（6.4）【自写】
│  │  │  ├─ scoring.py         # 有效分计算（best/last、manual 覆盖）
│  │  │  ├─ stats.py           # 教师总览统计与逐学生成绩（含每题有效分与总分）
│  │  │  ├─ groups.py          # 组别（层 1）成员关系与组-教师分配（层 2）写入辅助（admin 唯一写者，单事务）
│  │  │  └─ export.py          # 场次成绩 xlsx 生成（openpyxl，动态每题列，UTC→本地按 tz_offset 换算）
│  │  └─ judge/
│  │     ├─ worker.py          # 领任务循环、状态机驱动、编排编译与逐点运行
│  │     ├─ gojudge.py         # go-judge HTTP 适配器【隔离层：字段名/单位差异全部收敛于此】
│  │     └─ compare.py         # exact/trim/float 三种比对【自写】
│  ├─ migrations/versions/     # 0001 建表 / 0002 双背景 / 0003 分组与题目草稿 / 0004 暗色品牌色 / 0005 权限与密码体系
│  ├─ tests/                   # 判题与 API 测试（test_groups_api.py 分组与权限红线、test_import_export.py 三格式导入与 xlsx 导出）
│  │  ├─ test_groups_api.py    # 分组 CRUD、批量加入/移出、批量重置密码与启停、教师越权
│  │  └─ test_import_export.py # xlsx/txt/csv 三列导入（含错误码）与成绩 xlsx 导出
│  └─ data/                    # 运行时：cg.db、codes/、cases/（git 忽略）
├─ frontend/
│  ├─ src/
│  │  ├─ main.tsx  App.tsx     # FluentProvider、路由、登录守卫
│  │  ├─ theme.ts             # Fluent 2 主题与 mica/acrylic 近似样式
│  │  ├─ api/                 # fetch 封装 + 各模块接口函数
│  │  ├─ components/          # 布局、CodeMirror 代码框、判定徽章等
│  │  │  ├─ BulkActionBar.tsx      # DataGrid 多选后的批量操作条（按钮全部由调用方 props 注入）
│  │  │  ├─ GroupPickerDialog.tsx  # 勾选分组的对话框（加入/移出，admin 与 teacher 复用）
│  │  │  └─ GroupsManageDialog.tsx # 分组增删改管理对话框（仅 admin 入口）
│  │  └─ pages/
│  │     ├─ Login.tsx
│  │     ├─ student/  teacher/  admin/   # teacher/ 下含 StudentList.tsx（我的学生）
│  │     │            └─ teacher/StudentList.tsx  # 教师「学生」页：我绑定的学生 + 批量调整分组（无任何密码操作）
│  └─ vite.config.ts          # /api 代理到 :8000
├─ scripts/
│  ├─ init_admin.py            # 创建初始 admin
│  └─ import_students.py       # xlsx/txt/csv 三列导入（复用 admin 的解析逻辑，admin 页面之外的兜底）
└─ docker/
   ├─ Dockerfile.web  Dockerfile.judge  compose.yml   # compose 含 go-judge 服务
```

---

## 5. 数据库设计（SQLite DDL，权威定义）

枚举值全集：

- `role`: `admin | teacher | student`
- `compare_mode`: `exact | trim | float`
- `mode`: `homework | test`
- `score_policy`: `best | last`
- `submissions.status`: `pending | judging | done | failed`
- `verdict`: `AC | WA | TLE | MLE | RE | CE`
- `assignments.audience_mode`: `all | subgroup`（0.3.2 F1：发布受众 = 全部名单 / 指定子分组）
- 学生端场次 `state`（运行时派生值，不落库）：`ongoing | ending | ended`（0.3.2 F7；`ending` = 未结束且距 `end_time` ≤ `ENDING_SOON_HOURS`＝3 小时）

```sql
CREATE TABLE users (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  username      TEXT NOT NULL UNIQUE,          -- 登录名：学生=学号，教师=工号，admin=admin
  password_hash TEXT NOT NULL,                 -- bcrypt
  role          TEXT NOT NULL CHECK(role IN ('admin','teacher','student')),
  display_name  TEXT NOT NULL,
  is_active     INTEGER NOT NULL DEFAULT 1,    -- 0=停用，禁止登录
  must_change_password INTEGER NOT NULL DEFAULT 1,  -- 新建/重置后为 1：首登被强制改密，业务 API 在此之前一律 403
  password_updated_at  TEXT,                   -- 随机临时密码 7 天过期的锚点；NULL=不过期（统一/初始密码、已改密）
  created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE teacher_students (                -- 层 3「教师按 ID 手动添加的学生」：教师本人唯一写者（admin 只读）
                                               -- 0.3.2 起名单主体来自层 2 可教组别，本表只补"名单外的个别学生"
  teacher_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  student_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  PRIMARY KEY (teacher_id, student_id)
);

CREATE TABLE groups (                          -- 学生分组（班级/组），全站共享，管理员维护
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  name       TEXT NOT NULL UNIQUE,             -- 组名，≤50 字
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE group_members (                   -- 多组模型：一个学生可同时属多个组
  group_id   INTEGER NOT NULL REFERENCES groups(id) ON DELETE CASCADE,
  student_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  PRIMARY KEY (group_id, student_id)
);
CREATE INDEX idx_group_members_student ON group_members(student_id);

CREATE TABLE teacher_groups (                  -- 层 2「组-教师分配」：谁可教哪个班，admin 唯一写者
  teacher_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  group_id   INTEGER NOT NULL REFERENCES groups(id) ON DELETE CASCADE,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  PRIMARY KEY (teacher_id, group_id)
);
CREATE INDEX idx_teacher_groups_group ON teacher_groups(group_id);

CREATE TABLE teacher_subgroups (               -- 0.3.2 F1：教师自建子分组（名单之上的一层集合，同时用作发布受众）
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  teacher_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name       TEXT NOT NULL,                    -- ≤50 字，同一教师内唯一（跨教师可重名）
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE (teacher_id, name)
);
CREATE INDEX idx_teacher_subgroups_teacher ON teacher_subgroups(teacher_id);

CREATE TABLE teacher_subgroup_members (        -- 子分组 ↔ 学生；成员必须落在该教师「当前名单口径」内
  subgroup_id INTEGER NOT NULL REFERENCES teacher_subgroups(id) ON DELETE CASCADE,
  student_id  INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  PRIMARY KEY (subgroup_id, student_id)
);
CREATE INDEX idx_teacher_subgroup_members_student ON teacher_subgroup_members(student_id);

CREATE TABLE assignment_subgroups (             -- 场次受众选定的子分组，仅 audience_mode='subgroup' 时生效
  assignment_id INTEGER NOT NULL REFERENCES assignments(id) ON DELETE CASCADE,
  subgroup_id   INTEGER NOT NULL REFERENCES teacher_subgroups(id) ON DELETE CASCADE,
  PRIMARY KEY (assignment_id, subgroup_id)
);
CREATE INDEX idx_assignment_subgroups_subgroup ON assignment_subgroups(subgroup_id);

CREATE TABLE teacher_notices (                  -- 0.3.2 F1：教师端一次性提示（组别被撤销）；dismissed_at 非空即不再弹
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  teacher_id   INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  kind         TEXT NOT NULL,                  -- group_revoked
  payload      TEXT,                           -- JSON 文本：{groups:[{id,name,lost_count}],total_lost}
  created_at   TEXT NOT NULL DEFAULT (datetime('now')),
  dismissed_at TEXT
);
CREATE INDEX idx_teacher_notices_pending ON teacher_notices(teacher_id, dismissed_at);

CREATE TABLE audit_log (                       -- 只追加的操作台账：不提供任何 UPDATE/DELETE 路径
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  actor_id    INTEGER REFERENCES users(id),    -- 故意不设 CASCADE：日志不能被级联清除
  action      TEXT NOT NULL,                   -- 如 student_reset_pw / teacher_group_assign
  target_type TEXT NOT NULL,                   -- student / teacher / group / submission / user
  target_id   INTEGER,
  detail      TEXT,                            -- JSON 文本（中文不转义）
  created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_audit_log_actor ON audit_log(actor_id);
CREATE INDEX idx_audit_log_action ON audit_log(action);

CREATE TABLE problems (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  title           TEXT NOT NULL,
  description     TEXT NOT NULL,               -- Markdown 题面
  input_format    TEXT NOT NULL,
  output_format   TEXT NOT NULL,
  time_limit_ms   INTEGER NOT NULL DEFAULT 1000,
  memory_limit_mb INTEGER NOT NULL DEFAULT 256,
  compare_mode    TEXT NOT NULL DEFAULT 'trim' CHECK(compare_mode IN ('exact','trim','float')),
  float_eps       REAL,                        -- compare_mode='float' 时必填
  group_name      TEXT,                        -- 题库分组：自由文本，可空；列名不能叫 group（SQL 保留字）
  draft           TEXT,                        -- 题目草稿：ProblemDraft 的 JSON 文本，可空，每题目仅一份
  draft_saved_at  TEXT,                        -- 草稿保存时间，UTC ISO 字符串，可空
  created_by      INTEGER NOT NULL REFERENCES users(id),
  created_at      TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE test_cases (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  problem_id INTEGER NOT NULL REFERENCES problems(id) ON DELETE CASCADE,
  seq        INTEGER NOT NULL,                 -- 展示与判题顺序
  input      TEXT NOT NULL,
  expected   TEXT NOT NULL,
  is_sample  INTEGER NOT NULL DEFAULT 0,       -- 1=学生可见样例
  weight     INTEGER NOT NULL DEFAULT 1,       -- 分值权重
  UNIQUE(problem_id, seq)
);

CREATE TABLE assignments (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  title           TEXT NOT NULL,
  mode            TEXT NOT NULL CHECK(mode IN ('homework','test')),
  start_time      TEXT NOT NULL,
  end_time        TEXT NOT NULL,
  max_submissions INTEGER,                     -- NULL=不限
  score_policy    TEXT NOT NULL DEFAULT 'best' CHECK(score_policy IN ('best','last')),
  audience_mode   TEXT NOT NULL DEFAULT 'all' CHECK(audience_mode IN ('all','subgroup')),  -- 0.3.2 F1：受众来源（老数据 'all' 行为不变）
  released        INTEGER NOT NULL DEFAULT 0,  -- 测试模式放分开关
  released_at     TEXT,
  created_by      INTEGER NOT NULL REFERENCES users(id),
  created_at      TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE assignment_problems (
  assignment_id INTEGER NOT NULL REFERENCES assignments(id) ON DELETE CASCADE,
  problem_id    INTEGER NOT NULL REFERENCES problems(id),
  seq           INTEGER NOT NULL,
  full_score    REAL NOT NULL DEFAULT 100,
  PRIMARY KEY (assignment_id, problem_id)
);

CREATE TABLE submissions (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  assignment_id INTEGER NOT NULL REFERENCES assignments(id),
  problem_id    INTEGER NOT NULL REFERENCES problems(id),
  user_id       INTEGER NOT NULL REFERENCES users(id),
  code_text     TEXT NOT NULL,                 -- 已做 CRLF 归一化
  status        TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending','judging','done','failed')),
  verdict       TEXT,                          -- 提交级判定（7.6）
  score         REAL,                          -- 系统判分
  manual_score  REAL,                          -- 教师调分；有效分 = manual_score ?? score
  manual_score_updated_at TEXT,                -- 最后一次调分时刻；重判时与 manual_score 一起清空
  worker_id     TEXT,
  judge_log     TEXT,                          -- 系统级错误，仅教师/管理员可见
  submitted_at  TEXT NOT NULL DEFAULT (datetime('now')),
  judged_at     TEXT
);
CREATE INDEX idx_sub_pending ON submissions(status, id);
CREATE INDEX idx_sub_query   ON submissions(assignment_id, problem_id, user_id);

CREATE TABLE submission_results (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  submission_id INTEGER NOT NULL REFERENCES submissions(id) ON DELETE CASCADE,
  test_case_id  INTEGER NOT NULL REFERENCES test_cases(id),
  verdict       TEXT NOT NULL,
  time_ms       INTEGER NOT NULL,
  memory_kb     INTEGER NOT NULL,
  score         REAL NOT NULL,                 -- 该测试点得分
  UNIQUE(submission_id, test_case_id)
);

CREATE TABLE site_settings (                 -- 单行表，id 恒为 1，迁移时插入默认行
  id                 INTEGER PRIMARY KEY CHECK (id = 1),
  brand_color        TEXT NOT NULL DEFAULT '#0F6CBD',  -- 亮色/通用主题色 hex
  brand_color_dark   TEXT,                             -- 暗色专属主题色，NULL=沿用 brand_color；仅亮暗分图且暗图存在时生效
  brand_color_source TEXT NOT NULL DEFAULT 'manual',   -- manual=管理员手填 | image=跟随背景图自动提取
  bg_image_path      TEXT,                             -- 亮色/通用背景图，NULL=无；文件存 data/bg/
  bg_image_path_dark TEXT,                             -- 暗色背景图，仅 bg_dual=1 时参与渲染
  bg_dual            INTEGER NOT NULL DEFAULT 0,       -- 1=亮暗各用一张背景图
  bg_opacity         REAL NOT NULL DEFAULT 0.15 CHECK (bg_opacity >= 0 AND bg_opacity <= 1),
  audit_enabled      INTEGER NOT NULL DEFAULT 1,       -- 0.3.2 F5：关掉后 log_audit 直接 return，但绝不删除历史日志
  audit_retention_days INTEGER,                        -- 0.3.2 F5：保留天数 1–3650，NULL=永久保存；超期由 prune_expired 清理
  updated_by         INTEGER REFERENCES users(id),
  updated_at         TEXT NOT NULL DEFAULT (datetime('now'))
);
```

关于分组与题目草稿：

- **学生分组是"多组模型"**：一个学生可同时属于多个组，一个组可含多名学生，关系存 `group_members`。组由**管理员**创建、重命名、删除，**全站共享**（不按教师隔离，也不与 `teacher_students` 绑定关系挂钩）；教师**只读**组定义（只能看到全部组、不能增删改组本身），仅可调整**自己绑定学生**的成员关系（`POST /api/teacher/students/group_members`）。删组时成员关系随 `ON DELETE CASCADE` 一并删除；删学生同理。
- **`problems.group_name` 是自由文本**（可空，不建外键、不建枚举表，题库按 distinct 值筛选）。列名刻意不叫 `group`——**`group` 是 SQL 保留字**（`GROUP BY`），作列名要反引号包裹、易与语句混淆，一律用 `group_name`。表名 `groups` 无此问题（本机 SQLite 3.35.5 实测直接 `CREATE TABLE groups` 即可，不需引号）。
- **`problems.draft` / `draft_saved_at` 是题目编辑草稿**：`draft` 存 `ProblemDraft` 的 JSON 文本（题面字段，不含测试用例），`draft_saved_at` 为 UTC ISO 字符串；每个题目只保留一份草稿，新草稿覆盖旧草稿，教师保存题目（`PUT /api/teacher/problems/{id}`）成功后由服务端清空这两个字段。

初始化：Alembic 迁移建表（含 site_settings 默认行）；`scripts/init_admin.py` 创建 `admin/admin123`，并置 `must_change_password=1`——0.3.0 起首登强制改密对 admin/教师/学生一视同仁。

迁移：`0001_initial.py` 建全部基础表，`0002_dual_background.py` 给 `site_settings` 加 `brand_color_source`/`bg_image_path_dark`/`bg_dual`，`0003_groups_and_problem_group.py` 新建 `groups`、`group_members` 两表（含索引 `idx_group_members_student`），并给 `problems` 追加 `group_name`、`draft`、`draft_saved_at` 三列。本次新增 **`0004_dark_brand_color.py`**——给 `site_settings` 追加可空的 `brand_color_dark`（无 `server_default`，旧行取 NULL 即"暗色沿用亮色主题色"，行为与升级前一致）。手写、仿 0002；`render_as_batch=True` 已开，downgrade 的 `drop_column` 走批量模式；执行前先跑 `scripts/backup.sh` 备份，并在含旧数据的库上验证 upgrade/downgrade。本次新增 **`0005_password_audit_binding.py`**（0.3.0 批次 M6）——`users` 追加 `must_change_password`（NOT NULL DEFAULT 1）与 `password_updated_at`（可空），`submissions` 追加 `manual_score_updated_at`，新建 `teacher_groups`（组-教师分配，复合主键 + `idx_teacher_groups_group`）与只追加的 `audit_log`（+ actor/action 两个索引）；迁移显式 `UPDATE users SET must_change_password = 1`，即**存量账号（含 admin/教师）下次登录一律被强制改密**（用户拍板：系统尚未正式使用，存量多为测试数据，打扰成本≈0）。已在含真实数据的库副本（14 用户 / 46 提交）上跑通 upgrade→downgrade→upgrade，数据无损。0.3.2 批次再新增两支：**`0006_audit_settings.py`**——给 `site_settings` 追加 `audit_enabled`（NOT NULL DEFAULT 1）与 `audit_retention_days`（可空，NULL=永久）；**`0007_roster_and_subgroups.py`**——新建 `teacher_subgroups`/`teacher_subgroup_members`/`assignment_subgroups`/`teacher_notices` 四表，并给 `assignments` 追加 `audience_mode`（NOT NULL DEFAULT 'all' + `CHECK(audience_mode IN ('all','subgroup'))`）。SQLite 无法 ALTER 追加**表级** CHECK，且反射重建会丢掉既有的 `mode`/`score_policy` 约束，故 0007 对 `assignments` 走 `batch_alter_table(copy_from=...)` + `recreate="always"`，check 命名 `ck_assignments_audience_mode`，downgrade 先 drop 该 check 再 drop 列。两支迁移均在库副本上跑通 upgrade→head / downgrade→上一版 / 再 upgrade，13 行 assignments 全部保持 `'all'`（老数据行为与 0.3.1 一致）。

---

## 6. API 设计

### 6.1 通用约定

- 前缀 `/api`；除 login 外全部需要 `Authorization: Bearer <jwt>`。
- JWT 有效期 12 小时；payload：`{uid, role, exp}`。
- 成功返回 JSON 本体；错误返回 `{"code": "大写下划线码", "message": "中文说明"}`，HTTP 状态码语义化（401/403/404/409/413/415/422）。
- 教师接口除鉴权外还要校验"资源属于自己"（assignments.created_by / problems.created_by），否则 403。
- 代码体上限 64KB，超限 413。

### 6.2 接口清单

**认证**

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| POST | /api/auth/login | 匿名 | body `{username,password}` → `{token, user:{id,username,role,display_name,must_change_password}}`；停用账号 403 `USER_DISABLED`；`must_change_password=1` 且随机临时密码已超 7 天 → 403 `PASSWORD_EXPIRED`（坑 25） |
| GET | /api/auth/me | 全部 | 当前用户信息（带 `must_change_password`，供前端刷新后恢复强制改密拦截态） |
| POST | /api/auth/password | 全部 | 自助/强制改密 body `{old_password,new_password}`（新密码 **≥8 位**）；旧密码错 400 `BAD_OLD_PASSWORD`、违反 PW-03 规则 422 `PASSWORD_POLICY`、成功 204；成功后 `must_change_password=0`、写 `password_updated_at` 与审计 `user_change_password`。**0.3.2 F4 规则加严（中等档）**：≥8 位、不含空格、不等于用户名/姓名/旧密码/初始密码，**且必须同时含字母与数字、不能只由 ≤2 种字符重复组成、不能含 ≥5 位连续（升序或降序）字符、不在约 60 条常见弱密码表内**（`security.WEAK_PASSWORDS`，比较前 `lower()`）；前端 `components/passwordPolicy.ts` 与 `PASSWORD_RULES_TEXT` 逐字镜像，后端仍是唯一权威 |

**管理员**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST | /api/admin/teachers | 列表（含名单人数，`?q=` 模糊搜索）/ 创建 `{username,display_name}`（**不收密码**：统一初始密码 + 首登强制改密，审计 `teacher_create_pw`）；`TeacherOut` 带 `must_change_password` |
| PATCH | /api/admin/teachers/{id} | `{is_active}` 启用停用；审计 `user_is_active_change` |
| POST | /api/admin/teachers/{id}/reset_password | **无请求体**：生成 8 位随机临时密码，返回 `TempCredentialOut{student_id,username,display_name,temp_password,expires_at}`；置 `must_change_password=1` + `password_updated_at=now`（7 天过期锚点）；审计 `teacher_reset_pw` |
| GET | /api/admin/teachers/{id}/students | 该教师名单，**只读**（BD-06：admin 不持有层 3 写权限，原 `PUT` 已删除）。**0.3.2 F1 口径改为「可教组别成员并集 ∪ 手动添加」**，返回 `{students:[{id,username,display_name,source,group_names}]}`（`source=manual|group`，`group_names` 为含该生的可教组名）；撤销某组后该组学生立即从本视图消失（旧 `{student_ids}` 形态已废弃） |
| GET/PUT | /api/admin/teachers/{id}/groups | 层 2「组-教师分配」：`{teacher_id, group_ids}` / 全量替换 `{group_ids}`（组不存在 404），审计 `teacher_group_assign`（detail 记 added/removed）；教师可教的组由此唯一决定；**0.3.2 F1（决策 1e）**：保存前算差集，被移除的组与本次变更**同事务**写一条 `teacher_notices(kind='group_revoked')`（payload 记每组离开名单的人数与合计），该组学生立即离开教师名单与受众（历史提交与成绩仍保留） |
| GET/POST | /api/admin/students | 列表（含归属教师 + 所属分组 `groups:[{id,name}]`，一次联查构建；可选筛选 `?q=`（学号/姓名模糊）、`?group_id=`、`?must_change=0|1`，缺省时行为不变）/ 创建 `{username,display_name}`（**不收密码**，审计 `student_create_pw`）；`StudentOut` 带 `must_change_password`（LI-02；**0.3.1 起该「未改密」徽标仅存 API，前端管理表不再展示**） |
| POST | /api/admin/students/import | multipart 导入，**支持 `.xlsx`/`.txt`/`.csv` 三种格式**，列格式 `学生ID\|姓名\|组别\|教师`（第 4 列可选）：`.xlsx` 用 openpyxl（`read_only=True, data_only=True`）取第一个 sheet 前 4 列；`.txt` 行内含 `\|` 则按 `\|` 切，否则按 `,`/`，` 兜底；`.csv` 保留逗号语义，第 3 列组别可省（两列仍合法）。组别列可空，多组分隔符 `、，,；;/`；组不存在则自动创建；首行首格为 `学号`/`学生ID` 视为表头跳过；**空行静默跳过**（不再计入失败行——相对旧实现的行为微调）；学号已存在仍记失败「学号重复」，不增量补组；新建学生的**初始密码统一为 `12345678`**（整批只算一次 bcrypt）+ `must_change_password=1`、`password_updated_at=NULL`，重复学号不会覆盖已有账号的密码，逐生审计 `student_create_pw`、汇总审计 `student_import`。**第 4 列「教师」**（IM-01）：按 `username` 精确匹配 role=teacher，分隔符同组别列；教师名不存在 → 失败行（`教师不存在：X`）；写了教师但该行无组别 → 失败行（`教师列需与组别列同时提供`）；校验通过的行给每个（教师, 组）幂等写入 **`teacher_groups`（层 2）**，**绝不写 `teacher_students`**（名单由教师自己拉）。txt/csv 的「第 4 列起并入教师列」（原为第 3 列起并入组别），**旧三列文件行为完全不变**；**仅行本身合法时才解析组别**，失败行无任何副作用，整批单事务 commit；编码沿用 utf-8-sig/gbk。返回 `{success_count, failures:[{line,content,username,reason}]}`。文件校验按 **后缀 → 大小 → 空文件** 的顺序判：后缀白名单外 415 `UNSUPPORTED_FILE_TYPE`、大小 413 `FILE_TOO_LARGE`（**用 `>` 比较，恰好 5MB 合法**）、空文件 422 `EMPTY_FILE`（故一个空的 `.pdf` 先命中后缀，报 **415** 而非 422）；解析阶段再报 422 `INVALID_XLSX`（xlsx 无法解析，提示另存为标准 .xlsx）、422 `INVALID_CSV_ENCODING`（文本编码无法识别，提示用 UTF-8 或 GBK） |
| PATCH | /api/admin/students/{id} | `{is_active}`；审计 `user_is_active_change` |
| POST | /api/admin/students/{id}/reset_password | **无请求体**：随机 8 位临时密码，响应与副作用同教师端（`TempCredentialOut` + 审计 `student_reset_pw`） |
| POST | /api/admin/students/group_members | 批量加入/移出分组 `{student_ids[],group_ids[],action:"add"\|"remove"}`；**先全量校验**（学生 role=student、组存在）后写入，单事务，**不部分成功**；返回 `GroupMembershipOut` = `{success_count}`，其中 **success_count 是实际写入/删除的「学生×组」条数**（对已在组内的学生重复 add 得 0，不算失败）；审计 `group_member_change`（detail 记 `action/group_ids/student_ids/changed`）；**admin 是层 1 的唯一写者**（GR-01） |
| POST | /api/admin/students/batch_reset_password | `{student_ids[],mode:"unified"\|"random"}` → `BatchResetResultOut{mode,count,credentials:[TempCredentialOut]}`（PW-06/09）。**unified**：全员回到统一初始密码，整批**只算一次 bcrypt**（坑 24）、`password_updated_at=NULL` 即不过期、明细里 `expires_at=null`；**random**：逐生互不相同的 8 位随机密码，人数 >`BATCH_RESET_ASK_THRESHOLD`(20) 时走 `hash_password_many` 线程池（`BCRYPT_PARALLEL_WORKERS`=8，bcrypt 释放 GIL）但仍单事务同步返回，全员共用一个过期锚点。审计 `student_batch_reset_pw`（detail 记 `mode`/`count`）。前端凭明细拼 CSV（坑 22/26） |
| POST | /api/admin/students/batch_active | `{student_ids[],is_active}` 批量启用/停用；返回 `{success:true}`（**无计数字段**）；**逐人一条**审计 `user_is_active_change` |
| GET | /api/admin/audit_logs | 审计查看（AU-05，只读）：`?action=&target_type=&limit=50(0~200)&offset=0` → `{items:[{id,actor_id,actor_name,action,target_type,target_id,detail,created_at}], total}`，按 `created_at DESC, id DESC`；`actor_name` 为 `姓名(用户名)` 且一次批量查出；`detail` 解析失败降级为 null；**系统不提供任何修改/删除审计的接口**。**0.3.2 F5**：每条多出 `action_label`/`target_label` 中文名（由 `services/audit.py` 的 `AUDIT_ACTION_LABELS`/`AUDIT_TARGET_LABELS` 单点下发，页面与 Excel 导出共用一份，避免中英两套字典漂移）；`audit_enabled=0` 时本接口返回 403 `AUDIT_DISABLED`（前端隐藏导航不是安全边界） |
| GET | /api/admin/audit_logs/export | 0.3.2 F5：按筛选导出 xlsx（`?action=&target_type=&start=&end=`），列为 `时间\|操作人\|工号\|动作\|对象类型\|对象 ID\|详情 JSON`（动作/对象用上表中文名）；`StreamingResponse` + RFC5987 文件名；遵守保留期；超过 50000 行 → 422 `AUDIT_EXPORT_TOO_LARGE`（提示先加筛选条件） |
| GET/POST | /api/admin/groups | 分组列表（`{id,name,member_count}`）/ 新建 `{name}`（≤50 字）；重名 409 `GROUP_NAME_EXISTS`（捕获 IntegrityError，防并发重名） |
| PATCH | /api/admin/groups/{id} | 重命名 `{name}`；不存在 404 `GROUP_NOT_FOUND`、重名 409 `GROUP_NAME_EXISTS`；审计 `group_update`（detail 记 old/new） |
| DELETE | /api/admin/groups/{id} | 删除分组；不存在 404 `GROUP_NOT_FOUND`；其 `group_members` 成员关系级联删除；审计 `group_delete` |

**教师**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST | /api/teacher/problems | 自己的题库列表（含 `group_name`）/ 建题 `{title,description,input_format,output_format,time_limit_ms,memory_limit_mb,compare_mode,float_eps,group_name}`，`group_name` 为可空自由文本 |
| GET/PUT/DELETE | /api/teacher/problems/{id} | 详情（含全部用例，响应带 `draft`、`draft_saved_at`）/ 修改（**保存成功后服务端顺带清空 `draft` 与 `draft_saved_at`**）/ 删除（已被场次引用则 409，草稿随题目行一并消失） |
| POST | /api/teacher/problems/{id}/cases | 新增用例 `{seq,input,expected,is_sample,weight}` |
| PUT/DELETE | /api/teacher/cases/{case_id} | 修改 / 删除用例 |
| PUT | /api/teacher/problems/{id}/draft | 保存题目草稿：body 为 `ProblemDraft`（题面各字段，不含用例），写入 `problems.draft`（JSON）与 `draft_saved_at`（UTC ISO）；**新草稿直接覆盖旧草稿**（每题目只有一份）；归属校验同 `PUT /problems/{id}` |
| DELETE | /api/teacher/problems/{id}/draft | 清除该题草稿（`draft`、`draft_saved_at` 置空）；教师在前端恢复提示里选「放弃」时调用 |
| GET/POST | /api/teacher/assignments | 场次列表 / 发布 `{title,mode,start_time,end_time,max_submissions,score_policy,problems:[{problem_id,seq,full_score}]}`。**0.3.2 F1（决策 1c）新增受众字段** `audience_mode:"all"\|"subgroup"` 与 `subgroup_ids:[]`（每个子分组须属于该教师，否则 403/404）；`all` 即全部名单（老数据默认） |
| GET/PUT | /api/teacher/assignments/{id} | 详情 / 修改（仅开始前的字段可改；已有提交后禁止改题单，409） |
| POST | /api/teacher/assignments/{id}/release | 放分：`released=1, released_at=now`；仅 mode=test 且已过 end_time |
| GET | /api/teacher/assignments/{id}/overview | `{total_students, submitted_students, per_problem:[{problem_id,title,submit_count,pass_rate,avg_effective_score}], histogram:[{range,count}]}` |
| GET | /api/teacher/assignments/{id}/students | 逐学生行：`{student_id,username(学号),name,submitted_count,best_effective_score,total_score,problem_scores:[{problem_id,seq,title,full_score,effective_score}],last_submitted_at}`，含未交者（0 值）；`best_effective_score` 语义是**最高单题分**（沿用历史字段名，口径未变），SC-01 的「本场总分」是 `total_score` = 题单内各题有效分之和（题单外的提交计入 best、不计入 total）；`problem_scores` 按题单 seq 排序、未提交题 `effective_score=null`；`username` 供导出与前端「学号」列使用 |
| GET | /api/teacher/assignments/{id}/export | 导出本场次逐学生成绩 xlsx（`?tz_offset=<分钟偏移>`）：openpyxl 生成、`StreamingResponse` 流式下载；表头 `学号\|姓名\|提交次数\|最高单题分\|总分\|<每题得分列…>\|最后提交时间`（SC-02：每题一列、列头=题目标题、顺序=题单 seq，未提交的题留空），sheet 名取场次标题；时间按 `tz_offset` 由库内 UTC 转本地（坑 9）；归属校验同 `/students`；`Content-Disposition` 用 RFC5987 `filename*=UTF-8''` 并带 ASCII 兜底 |
| GET | /api/teacher/submissions/{id} | 提交详情：代码 + 逐用例结果（教师全可见） |
| POST | /api/teacher/submissions/{id}/rejudge | 置回 pending、清空 results |
| PATCH | /api/teacher/submissions/{id}/score | `{manual_score}`，null 表示撤销调分 |
| POST | /api/teacher/submissions/{id}/rejudge | 置回 pending、清空 results，并清 `manual_score` 与 `manual_score_updated_at`（SC-04：旧调分不得顶掉新判结果） |
| PATCH | /api/teacher/submissions/{id}/score | `{manual_score}`，null 表示撤销调分（仍合法）；非 null 须落在 `0 ~ 该题 full_score`（经 `assignment_problems` 联查），越界或未配置分值 422 `MANUAL_SCORE_OUT_OF_RANGE`（SC-03）；成功写 `manual_score_updated_at` 并审计 `score_manual_adjust` |
| GET | /api/teacher/students | 我的名单：`{id,username,display_name,is_active,must_change_password,groups:[{id,name}],source}`，可选 `?q=`（学号/姓名模糊，缺省行为不变）。**0.3.2 F1**：数据源 `stats.bound_students` 改为「可教组成员 ∪ 手动添加」并集，新增 `source=manual|group` 供前端判定「移出手动添加」是否可用；分组一次联查构建 |
| GET | /api/teacher/classes | 我可教的班级及成员（层 1 只读视图，教师端**唯一**的组入口）：`[{id,name,member_count,students:[{id,username,display_name,is_active,bound}]}]`，按 `teacher_groups`（层 2）过滤、组名排序；**停用学生照常返回**由 `is_active` 标注；`bound` 表示已在自己名单里；无可教组时返回 `[]`（BD-03/GR-02）。**0.3.2 F1（决策 1b）起教师端不再从这里拉人**，本接口降级为只读组别徽标展示（`bound` 已无消费方） |
| POST | /api/teacher/students/bind_from_class | 主路径拉名单（层 3）：`{student_ids[]}` → `{success_count}`（实际新增，幂等）；**任一** id 不属于我可教的组 → 整批 403 `STUDENT_NOT_IN_CLASS` 且零写入；审计 `teacher_student_bind`（detail 记 `source:"class"`） |
| POST | /api/teacher/students/bind | 兜底按学生 id 拉入名单（转学生/旁听等暂不在组场景）：只接受 role=student 且 `is_active=1`，否则整批 422 `INVALID_STUDENT_IDS`；**响应不含学生明细**，避免变成全量名册（BD-04）；审计同上 `source:"manual"`。**0.3.2 F1（决策 1d）保留本操作并写入审计**，管理端审计页中文名改为「手动添加学生进名单」 |
| POST | /api/teacher/students/unbind | 从自己名单移出 `{student_ids[]}` → `{success_count}`；不在名单里的静默忽略（幂等）；历史提交与成绩保留，教师统计仍可见（BD-05）；审计 `teacher_student_unbind`。**0.3.2 F1 语义收窄**：只能移走层 3 手动添加的学生，对组别派生的学生返回 422 `ROSTER_DERIVED_STUDENT` 并列出 id（这类学生只能由管理员撤销组别来移除） |
| GET/POST | /api/teacher/subgroups | 0.3.2 F1：我的子分组列表 `[{id,name,created_at,member_count,student_ids}]`（成员数按当前名单口径）/ 新建 `{name}`（≤50 字，同教师内重名 409 `SUBGROUP_NAME_EXISTS`）；审计 `subgroup_create` |
| PATCH/DELETE | /api/teacher/subgroups/{id} | 重命名 `{name}`（审计 `subgroup_update`）/ 删除（审计 `subgroup_delete`）。**被任何场次引用时 409 `SUBGROUP_IN_USE`**（消息列出场次标题），绝不自动把受众放宽到全部名单 |
| PUT | /api/teacher/subgroups/{id}/students | 全量替换子分组成员 `{student_ids}`（空列表=清空）；任一 id 不在当前名单口径内 → 整批 422 `INVALID_STUDENT_IDS`；审计 `subgroup_member_change`（detail 记 added/removed） |
| GET | /api/teacher/notices/pending | 0.3.2 F1：待确认的一次性提示 `[{id,kind,payload,created_at}]`（当前只有 `kind='group_revoked'`，payload 见 6.2 组别分配行） |
| POST | /api/teacher/notices/{id}/dismiss | 标记已读（写 `dismissed_at`），教师端弹窗点「知道了」后调用；已读的不再出现在 pending |
| GET | /api/teacher/assignments/{id}/students/{sid}/problems | 0.3.2 F6：逐学生**按题**调分数据 `[{problem_id,seq,title,full_score,submission_count,latest_submission_id,score,manual_score,effective_score}]`；每题有效分与逐学生表同口径（`scoring.aggregate_scores`），`latest_submission_id` **按题各取一条**（按 `(submitted_at,id)` 取最新），是「多题场次只能调一道题」的修复落点 |

> **红线（权限）**：**教师端没有任何密码重置接口**——不存在 `/api/teacher/students/reset_password`、`/api/teacher/students/batch_reset_password`、`/api/teacher/*/reset_password` 之类路由，`api/teacher.py` 亦不 import 任何密码能力（`hash_password` 等）。重置密码（含批量）只有管理员一侧的接口（见上表），教师改自己的密码只走 `POST /api/auth/password`。前端教师页同样不得出现任何密码相关按钮；API 测试须断言教师 token 调上述路径返回 404/405。
>
> **红线（归属，0.3.0）**：组别（层 1 `groups`/`group_members`）与组-教师分配（层 2 `teacher_groups`）的**唯一写者是 admin**；`GET /api/teacher/groups` 与 `POST /api/teacher/students/group_members` 已删除（教师 token 调过去 404/405），教师可见的组**只经 `/api/teacher/classes`**，不再暴露不可教组的存在。层 3 `teacher_students`（教师名单）唯一写者是教师本人，admin 侧只保留只读 GET。三层互不级联增删（坑 27）。

**学生**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/student/assignments | 我的场次：`now>=start_time` **且我在该场次受众内**（0.3.2 F1：判定改为「我在 `audience_students` 里」，不再看教师绑定关系）；附 `state: ongoing\|ending\|ended`（0.3.2 F7）与我的有效分汇总 |
| GET | /api/student/assignments/{id} | 场次详情：题单 + 每题我的有效分（按 6.4 可见性脱敏）；可见性判据同列表（不在受众内即 404），`state` 同样三态 |
| GET | /api/student/assignments/{id}/problems/{pid} | 题面 + **样例用例** + 我的提交摘要列表 |
| POST | /api/student/assignments/{id}/problems/{pid}/submissions | `{code_text}`；校验窗口/次数/受众；返回 submission id |
| GET | /api/student/submissions/{id} | 提交详情（按 6.4 脱敏） |

**站点设置**

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| GET | /api/settings | 匿名 | `{brand_color, brand_color_dark, brand_color_source, bg_image_url, bg_image_url_dark, bg_dual, bg_opacity, audit_enabled}`；登录后页面用全部项，登录页只用 `brand_color` 生成主题与动态背景。**0.3.2 F5 只多暴露 `audit_enabled` 一个布尔**（前端据此决定审计导航与路由是否放行），保留天数与日志内容不下发给未认证访客 |
| PUT | /api/admin/settings | admin | `{brand_color?, brand_color_dark?, brand_color_source?, bg_dual?, bg_opacity?}`；两个主题色都校验 hex 格式、来源枚举与 0–1 范围；字段缺省即不改动（`brand_color_dark` 无法置回 NULL，靠"暗图不存在则不生效"覆盖）。**0.3.2 F5 新增** `audit_enabled?`、`audit_retention_days?`（1–3650，可传 null 表示永久；越界 422 `AUDIT_RETENTION_INVALID`）；写入后失效进程内审计开关缓存，改保留期时顺带跑一次 `prune_expired` |
| GET | /api/admin/settings | admin | 0.3.2 F5：`{…公开项, audit_retention_days}`——保留天数只在管理端点下发（公开端点不给），设置页据此回填输入框；页面的「保存」走的仍是上面的 PUT |
| POST | /api/admin/settings/bg_image | admin | multipart 上传（≤5MB，jpg/png/webp），`?mode=light\|dark` 指定槽位，存 `data/bg/`，返回该槽位 url（形如 `/api/settings/bg_image?v=<文件名>`，暗色槽位额外带 `&mode=dark`） |
| GET | /api/settings/bg_image | 匿名 | `?mode=light\|dark` 返回对应槽位文件；`v` 与当前文件一致时响应 `Cache-Control: public, max-age=31536000, immutable`，不一致时 `no-store` |
| DELETE | /api/admin/settings/bg_image | admin | `?mode=light\|dark` 清除对应槽位（删文件） |

### 6.3 提交校验顺序（POST submissions）

1. 场次存在且 `created_by` 是我的绑定教师之一，否则 404；
2. `start_time <= now <= end_time`，否则 409 `WINDOW_CLOSED`；
3. 该场次该题我的提交数 < `max_submissions`（NULL 不限），否则 409 `SUBMIT_LIMIT`；
4. 代码非空且 ≤64KB；CRLF→LF 归一化后落库。

### 6.4 反馈可见性（services/visibility.py，权威伪代码）【自写】

```python
def can_see_detail(user, assignment):
    if user.role in ("admin", "teacher"):
        return True                      # teacher 侧另校验归属
    return assignment.mode == "homework" or assignment.released == 1
```

- 不可见时，学生端的提交详情/列表只返回：`{id, submitted_at, status_text}`，其中 `status_text` 为 `判题中` / `已判题（待放出）`；**不含** verdict、score、逐点结果。
- 可见时返回完整：提交级 verdict、score、逐用例 `{seq, is_sample, verdict, time_ms, score}`（隐藏用例不返回 input/expected 原文）。

---

## 7. 判题系统设计

### 7.1 进程模型

三个独立运行单元：

1. **web**：只写 `status='pending'` 的提交行，不判题。
2. **judge worker**：领任务、编排判题流程；**自身不执行学生二进制**，一切执行经 HTTP 委托 go-judge → worker 可在 Windows 开发机运行。
3. **go-judge 服务**（开源）：真正执行不可信代码的沙箱。仅暴露在 compose/本机内网，只允许 worker 访问。

### 7.2 状态机与领任务

```
pending --领任务--> judging --成功--> done
                          \-- worker 内部异常 --> failed（judge_log 记录原因）
```

领任务 SQL（原子抢占，防多 worker 重复判）：

```sql
UPDATE submissions
   SET status='judging', worker_id=?
 WHERE id = (SELECT id FROM submissions WHERE status='pending' ORDER BY id LIMIT 1)
RETURNING *;
```

取不到行则 sleep 1s 重试。`failed` 的提交教师可重判（rejudge 接口）。

### 7.3 适配器接口（judge/gojudge.py）

worker 只依赖以下抽象，go-judge 的字段名/单位/版本差异全部收敛在适配器内：

```python
@dataclass
class RunResult:
    time_ms: int
    memory_kb: int
    exit_code: int | None
    signal: int | None
    stdout: bytes      # 已按 output 上限截断
    stderr: bytes
    status: str        # 适配器归一化：ok | time_limit | memory_limit | runtime_error | output_limit

def run(argv: list[str], stdin_data: bytes, *,
        time_limit_ms: int, memory_limit_mb: int,
        proc_limit: int = 64, output_limit_mb: int = 1,
        copy_in: dict[str, bytes] | None = None) -> RunResult: ...
```

- 编译 = `run(["gcc","-std=c11","-O2","-o","main","main.c","-lm"], ...)`，time_limit 10s、memory 512MB；非零退出 → 提交级 `CE`，stderr 截断 4KB 给学生看。
- 每用例运行 = `run(["./main"], stdin=用例输入, copy_in={"main": 编译产物})`，limits 取题目配置。
- worker 启动时调 go-judge 健康检查接口，不通则拒绝启动并报错（避免静默假判）。

### 7.4 go-judge 资源限制表（请求参数语义）

| 资源 | 限制 | 说明 |
|---|---|---|
| CPU 时间 | `time_limit_ms` | 毫秒级，go-judge 原生支持 |
| 内存 | `memory_limit_mb` | 超限归一化为 memory_limit |
| 进程数 | 64 | 防 fork 炸弹 |
| 输出大小 | 1MB | 截断，防 bigout 撑爆 |
| 网络 | 无 | go-judge 隔离环境默认无网络 |
| 文件系统 | 每次运行独立临时根 | copyIn 只注入必需文件 |

- 镜像与端口（已核实）：镜像 `criyle/go-judge:v1.12.3`，REST API 端口 **5050**（5051 为 gRPC、默认关闭）；docker 运行需 `--privileged --shm-size=256m`。本机开发把宿主机侧发布端口交给 `start-local.ps1` 决定：默认 5050，落在 Windows 开机保留段内时自动顺延并写入 `backend/data/dev-ports.json`；服务器不受影响。
- **M4 部署演练结论（2026-09-18 WSL2 实测，Ubuntu 24.04 + Docker 29）**：① 官方镜像不含 gcc，C 编译会 CE——compose 改用派生镜像 `docker/Dockerfile.gojudge`（基座 + apt 装 gcc/libc6-dev，Debian 13）；② cgroup v2 宿主上容器内独立 cgroupns 导致 go-judge 启动即 fatal（`cgroup path is empty`），**首选解法是 compose 服务级 `cgroup: host`**（compose-spec 官方键，见 `05-services.md` 与 JSON schema；2026-09-19 复核更正——原记"compose 不支持 cgroupns 键"属键名找错，`cgroupns_mode` 才是不合法的那个），仅当 compose 版本过旧不认该键时才退回 dockerd 全局 `--default-cgroupns-mode=host`（见《部署操作指南.md》§4.2）；注：`/sys/fs/cgroup:/sys/fs/cgroup:rbind,rw` 挂载单独不足以绕过，实测加了它仍崩溃循环，保留无害；③ judge 网络必须 `internal: true`（否则沙箱可出外网，违反 7.7 安全清单）；④ compose 的 web 容器同时托管前端静态（Dockerfile 多阶段构建，`STATIC_DIR` 挂载 dist）。
- /run 请求字段（已核实，源自 master `cmd/go-judge/model/model.go`）：`cmd[].args/env/files[{content|fileId,name,max}]`、`cpuLimit`（纳秒）、`memoryLimit`（字节）、`procLimit`、`clockLimit`、`strictMemoryLimit`、`copyIn{文件名:{content|fileId}}`、`copyOut/copyOutCached/copyOutMax`、`tty`；响应 `results[].status`（字符串，如 `Accepted`/`Time Limit Exceeded`/`Memory Limit Exceeded`/`Runtime Error`/`Output Limit Exceeded`）、`exitStatus`、`time`（纳秒）、`memory`（字节）、`files`。stdin 经 `cmd[].files` 注入，stdout/stderr 用 `files:[{name:"stdout",max:上限}]` 声明收集；编译产物用 `copyOut:["main"]` 收集、运行时经 `copyIn` 注入。健康检查 `GET /version`；可选 `-auth-token` 启用令牌鉴权。
- compose 中**固定镜像版本 tag**（禁 latest），防上游漂移。

### 7.5 备选：Judge0 CE

仅当 go-judge 在目标环境经评估不可用（内核/权限受限）时启用：worker 适配器换实现为 Judge0 REST（language_id=C、expected_output 由其比对），此时 trim/float 语义退化为 exact+自行后比对——**启用前必须经用户确认并更新本文档**。默认不装。

### 7.6 判定与计分

单用例判定顺序（基于 RunResult.status）：`time_limit`→TLE；`memory_limit`→MLE；`runtime_error`→RE；`ok`→进入比对→AC/WA；`output_limit`→WA（并记 judge_log）。

比对（compare.py）【自写】：

- `exact`：字节级相等。
- `trim`：双方统一 LF；逐行 `rstrip()`；去掉末尾全部空行后逐行比较。
- `float`：按空白分词；两 token 都能解析为浮点时按 `abs(a-b) <= eps` 判等，否则字符串相等；token 数必须相同。

计分：

```
用例得分 = full_score * weight / sum(weights)   当且仅当该用例 AC，否则 0
submission.score = round(sum(用例得分), 1)
提交级 verdict = 全 AC ? 'AC' : 按 seq 顺序第一个非 AC 用例的判定
有效分 = manual_score ?? score；场次内按 score_policy 取 best/last
```

### 7.7 安全清单（判题部分）

- 学生代码视为**完全不可信**：web/worker 进程任何情况下不得直接执行学生二进制；唯一执行入口是 go-judge。
- go-judge 按 README 的权限要求运行（容器/能力集），与 web 隔离在不同容器；仅内网可达；若所装版本支持访问令牌则启用并走环境变量。
- 7.4 限制全量启用；输出/进程/内存超限均有归一化判定，不会拖垮服务。
- 判题产物（代码、输出）不进入任何学生可见接口，除 6.4 允许部分。

---

## 8. 前端设计（Fluent 2）

### 8.1 视觉规范

- `FluentProvider` 包裹全站；**暗色模式默认跟随系统 `prefers-color-scheme`，右上角手动切换覆盖并持久化**（localStorage）。
- **暗色模式全适配**：所有页面/组件（自定义背景、recharts 轴与网格、CodeMirror 编辑器主题、判定徽章、背景图层）取色于主题令牌或主题上下文，禁止硬编码 hex/rgb（见坑 12）；M3 验收含全路由暗色走查。
- 顶导航左侧 Logo 只有主标题「青蓝」（品牌色，走 Fluent 标题令牌）；标语「青蓝 QingLan · C 语言练习与测评平台」放在**页脚**，不在 Logo 下方。
- **主题色管理员可调**：前端启动时拉取 `/api/settings`，用 **chroma-js** 对品牌色插值生成 16 阶 brandRamp，经 Fluent v9 `createLightTheme/createDarkTheme` 构建亮/暗主题；默认 `#0F6CBD`。亮暗两套主题的取色由 `theme.ts:resolveBrandColors()` 决定：亮色恒用 `brand_color`，暗色只在**亮暗分图且暗图已上传**时用 `brand_color_dark`（为空则沿用 `brand_color`），该判定与 `resolveBgUrl()` 的选图规则一一对应，避免"背景回退了、主题色还留着"的错位。主题 API 名称以所装版本官方文档为准（同 go-judge 约定，核实后回写本文档）。
- **品牌色阶方向（易错，务必守住）**：Fluent 的 16 阶约定是 **step 10 最深 → step 160 最浅**，`brand[80]` 恒等于管理员设定的原色。`createBrandRamp()` 的插值域必须是 `[darkEnd, base, lightEnd]`（`darkEnd = mix(base, black, .78)`、`lightEnd = mix(base, white, .94)`）。方向写反不会报错，但会静默改变一大批令牌：亮色下 `colorBrandStroke2Contrast = brand[140]` 会从浅蓝变成近黑（Spinner 圆环底色）、`colorBrandBackground2 = brand[160]` 会从近白变成近黑、`*Pressed` 档反而变浅。
- **文本选区**：浏览器默认选区用系统 Highlight 色，在浅色强调色下是一整块不透明白，会盖掉背景图。`index.css` 统一改为 `::selection { background-color: var(--ql-selection); color: inherit }`，半透明品牌色由 `Layout` 写到 `:root`（放 `:root` 而非组件内，`Dialog`/`Menu` 等 portal 内容才取得到）。
- **加载动画**：全站 loading 统一用 `StateViews.LoadingView`（Fluent `Spinner`）。圆环轨道/弧线颜色由品牌色阶令牌决定；标签文字 Fluent 默认不写 `color`、会继承近黑的 `colorNeutralForeground1`，`index.css` 里以 `.fui-Spinner__label { color: var(--colorNeutralForeground3) }` 统一改成中性灰。
- **页内返回链接统一写法**：页面首尾的「← 返回 X」一律是 `Caption1` 包 `Button appearance="subtle" size="small"` + `onClick={() => navigate(...)}`，**不要用带品牌色的 `Link`**（蓝色下划线文字与「灰底 hover 按钮」是两套观感，混用会被走查判为不齐）。代价是没有真实 `href`（不能中键/新标签打开），全站已统一接受。另：`react-router` 的相对 `to=".."` 在 `/student/submissions/:sid` 这类扁平路由下实测解析到 `/`（用 `MemoryRouter` + `renderToStaticMarkup` 渲染出 `<a href>` 验证），回跳要么写绝对路径，要么 `navigate(-1)`。
- **页面标题规范**：页面级标题一律用共享组件 `components/PageHeader`（`title` / `subtitle` / `actions`），副标题**必须在大标题下方独占一行**、右侧操作区自动换行。原因：Fluent `Text` 根样式是 `display: inline`，`<Text as="h2">` 后紧跟 `Caption1` 会渲染在同一行。`Forbidden` / `NotFound` / `Login` 这类居中式提示页不套该组件。
- **背景图与透明度**：管理员上传的图存 `data/bg/`；前端以独立 fixed 层渲染（z-index 低于全部内容），`opacity` 取 `bg_opacity`；无图或透明度为 0 时为纯色背景（`resolveBgUrl()` 统一判定，`Layout` 与 `BackgroundLayers` 共用）。**登录页不使用该图**，改用独立背景组件（见下条）。**mica 近似**：页面根背景用主题令牌色的两段渐变（亮/暗各一套），固定不滚动，半透明透出背景图；**acrylic 近似**：顶导航 `backdrop-filter: blur(20px) saturate(150%)` + 主题令牌半透明底色；**设置了自定义背景图时页面卡片同样启用毛玻璃**——`Layout` 根节点挂 `.ql-has-bg`，`index.css` 给 `.fui-Card` 加半透明底（`--ql-surface`，中性背景 72% 不透明度）+ `backdrop-filter: blur(60px) saturate(150%)`（对齐 Win11 Mica/Acrylic：背景图彻底化成色雾，只留色调与明暗）；无背景图时卡片保持不透明。
- **登录页专属动态背景**：`LoginBackdrop` 单一 fixed 层（z-index -1，不透明底色）＝ 中性背景色到"中性背景×品牌色"混色的斜向渐变 + 三颗缓慢漂移的品牌色光斑（`radial-gradient` + `blur`，色相仅 ±12°，保证主体色仍是站点品牌色）；颜色全部由主题令牌经 chroma-js 推导，亮/暗模式各自取色；光斑尺寸用 `vmax`，窄屏（≤640px）降模糊半径，`prefers-reduced-motion: reduce` 时停动画。
- **亮 / 暗独立背景图**：管理员开关 `bg_dual`。关闭时两个模式共用 `bg_image_path`（已有数据不失效）；打开后亮色用 `bg_image_path`、暗色用 `bg_image_path_dark`，暗色槽位为空则回退主图。前端 `BackgroundLayers` 按 `useThemeMode().isDark` 选 URL；开关切换即时预览，点「保存」后落库。
- **主题色跟随背景图**：开关 `brand_color_source=image`。每次上传新背景图（或打开该开关）时，前端取图 → `extractBrandColor()`（Canvas 采样 + 色相分桶投票，忽略近黑/近白/灰，chroma-js 把中选色校正到可用明度/饱和度）→ **按上传的槽位写入对应字段**（亮图→`brand_color`，暗图→`brand_color_dark`，两者互不覆盖）；手动改品牌色即回落 `manual`。打开该开关时若已启用亮暗分图，会**两张图各提取一次**。提取结果在消息条上给出「撤销取色」可回退到上一颜色（一次涉及两个槽位时一起回退）；灰度图等提不出色时保留原色并提示。管理页只有在使用两张背景图时才显示「暗色模式品牌色」取色器，避免多出一个平时不生效的输入项。
- **超 5MB 的图**：前端不再直接报错，改用 Fluent `Dialog`（标题 + 正文 + 取消/主操作）询问是否压缩；确认后用 `browser-image-compression` 在浏览器本地压到最长边 2560px、≤2MB（PNG/WebP 压不到目标时退化为 JPEG 并同步扩展名）；压缩后仍 >5MB 则提示换图、不上传。服务端 5MB 限制与格式校验保持不变。
- **强制改密拦截（PW-02）的前端形态**：`components/Guard.tsx` 的 `RequirePasswordChanged` 是**路由级 gate**（不是可关闭的弹窗），包住 `/`、`/403`、`/account`、三角色全部业务路由与 `*`；`/change-password` 故意挂在 `RequireAuth` 之内、gate 之外（既要已登录，又要在拦截期可达）。登录态**不能直接读 localStorage**——它不是响应式数据源，改密成功或后端回 403 时守卫不会重新渲染；`api/client.ts` 因此加了一层带订阅的进程内缓存，守卫经 `useSyncExternalStore` 读它，gate 挂载时再用 `/auth/me` 校准（本地可能存着过期的 `false`），任意业务接口回 `403 MUST_CHANGE_PASSWORD` 时把标志补回去。**拦截期收起导航页签、头像菜单项改文案改指向改密页**，不给任何"跳过"入口。
- **凭证类敏感文本（PW-09）只有一个实现**：`components/credentialCsv.ts`。逐格加引号并按 RFC4180 把 `"` 翻倍转义；**必须带 UTF-8 BOM**（`String.fromCharCode(0xfeff)`，别用肉眼不可见的字面量），否则 Excel 双击打开中文列头按本地代码页解码成乱码；下载走 `Blob + URL.createObjectURL + a.download`，**revoke 延迟 2s**（立即 revoke 时 Firefox 拿不到数据），绝不 `<a href>` 直链（坑 22/26）。`expires_at` 是 naive UTC 串，只能经 `components/time.ts`（对 `YYYY-MM-DD HH:mm:ss` 形态自动补 `Z`）转本地，直接 `dayjs(x)`/`new Date(x)` 会按本地时间解释、整体差一个时区。复制优先 `navigator.clipboard`、失败回退 `execCommand`（校内 http 局域网直连不是安全上下文，前者不可用）。
- **密码规则单一来源**：`components/passwordPolicy.ts` 与后端 `security.validate_new_password` 同规则，改密页与账号页共用，避免两处预校验漂移。统一初始密码常量只用于拼「导出初始密码表」的列值与本地预校验，**界面文案一律写"统一初始密码"而不打印明文**。
- 圆角、间距、字号、阴影一律用 Fluent 设计令牌（`tokens.borderRadiusMedium` 等），禁止手写魔法值。
- 字体：Segoe UI Variable（系统回退 Segoe UI / 微软雅黑）。
- 图标一律 `@fluentui/react-icons`，不自绘 SVG。
- **居中式提示页（404 / 403）**：`NotFound` / `Forbidden` 这类页面**不套 `PageHeader`**（见上面「页面标题规范」的例外）；颜色一律取 `useTheme()` 令牌（项目内从 `src/appTheme.ts` 导出），不得写 hex/rgb；卡片毛玻璃**不需要页面自己处理**——`Layout` 根节点挂了 `.ql-has-bg` 后 `index.css` 的 `.ql-has-bg .fui-Card` 自动生效，**禁止给 `Card` 传 `backgroundColor`**（会盖掉那层半透明底与 blur）。页面形态：居中 `Card`（`maxWidth: 560`、内边距 XXXL、`minHeight: min(520px, calc(100vh - 16rem))`）+ 品牌色圆形图标块 + 巨型状态码 + 主/次两个返回动作；注意 `Text` 上的居中要显式设置（见下方「对齐像素自查」根因①）。**状态码要精确落在卡片垂直中线上**：卡片用 `grid-template-rows: 1fr auto 1fr`，图标块放进上轨道并 `align-self: end`、文案+按钮放进下轨道并 `align-self: start`，两条轨道永远等分剩余空间，所以中间那行（状态码）必然居中；上下各自的 8px 外边距要对称，且必须显式 `gap: 0`（`Card` 自带 12px gap，会和外边距叠成 20px）。说明文案不要设 `maxWidth`（`34ch` 在 19 个汉字时会折出孤零零一行尾）。
- **DataGrid 多选只用声明式 props**：`selectionMode="multiselect"` + `getRowId` + `selectedItems` + `onSelectionChange` 四个属性自己管状态；本项目实装的 `@fluentui/react-components` 是 **9.74.7**，**没有** `useDataGridSelection` 这类旧 headless API，不要照旧教程去 import hook。选择状态类型必须是 `Set<TableRowId>`（`TableRowId = string | number`），写成 `Set<number>` 会在 `onSelectionChange` 赋值处报 TS2345。每次重新拉数据后必须清空选择，否则会残留已不可见行的选中态。
- **`BulkActionBar` 保持中性**：按钮（含文案、图标、回调）全部由调用方 props 注入，组件本身**不得出现任何密码相关字样**——教师页复用它时天然拿不到"重置密码"入口，这是 6.2 红线的前端一侧保障；选中数为 0 时整条不渲染。
- **全局 Fluent 覆盖写在 `index.css`，用「双类名」提特异性**：`@fluentui/react-components` 的样式是 Griffel 运行时注入的原子类（特异性 0,1,0），且注入顺序在 `index.css` 之后——同特异性会被组件库赢，所以覆盖必须写成 `.fui-X.fui-X` 或 `.fui-X .fui-X__slot`（0,2,0）。当前三条全局覆盖：① `.fui-MessageBar.fui-MessageBar { border-width: 0 }`——MessageBar 默认画 1px 描边，`intent` 变体还会把 `border-color` 换成状态色，全站提示框一律去边框；② 表头**不做居中**——`DataGrid` 会把列拉伸到远宽于内容（实测列约 179 CSS px、内容最宽约 62px），只居中表头会让表头相对数据恒定右飘约 66px，视觉上更歪。表头与内容一律共用左轴，唯一的破绽是操作列：单元格以 `Button` 开头，按钮自带 `spacingHorizontalS` 水平内边距，图标墨迹会比表头右移同样多的量，故用 `.fui-DataGridCell > .fui-Button:first-child, .fui-DataGridCell > div:first-child > .fui-Button:first-child { margin-inline-start: calc(-1 * var(--spacingHorizontalS)) }` 把首个按钮左移等量补偿；③ `.fui-Badge.ql-badge-status { min-width: 56px }`——状态类标识（启用/已停用/已放出/未放出）字数不同，不给最小宽度会在同一列里参差。**注意 `--spacing*` / `--color*` 这些 Fluent 令牌只注入在 `FluentProvider` 容器上、`:root` 取不到**，所以引用令牌的 `index.css` 规则必须作用在 Provider 内的元素上，写页面级/根级选择器会静默失效（`var` 解析失败 → 整条声明按初始值算，不报错）。
- **标识（Badge）统一 `size="large"`**：24px 高、12px 字号、6px 横向内边距。`small` 只有 16px/10px，在表格里偏小；同一行里的 outline/tint/实色标识必须同尺寸，`--status` 变体再叠 56px 等宽。
- **危险操作按钮固定红系**：删除确认等破坏性操作用共享 `components/dangerStyles.ts`（`solid` 实心红 / `outline` 红描边），基准色取 `colorPaletteRedBackground3`（亮暗同值 #d13438），hover/active 用 `color-mix(in srgb, <令牌> N%, black)` 加深，**不随管理员品牌色变化**。
- **对齐像素自查（新建组件/调间距必做，代码对齐 ≠ 观感对齐）**：以截图渲染出的「墨迹」为准确认。方法：node-repl 用 `zlib.inflateSync` 写 ~40 行纯 JS PNG 解码器（解析 IHDR/IDAT → inflate → 逐行 unfilter，零依赖），按 y 带扫描每行文字/元素的墨迹最左边缘（RGB 均值 <100 的最左 x），取 span 求中心与容器中心比对；截图是设备像素，**÷ DPR（本机 1.25/1.5）得 CSS px，偏差 >2px 必查根因**。已踩过的观感错位根因：① Fluent v9 `Text` 自带 `textAlign:'start'`，父级的 `center` 传不进来，必须在 Text 自身设；② 行首全角引号「的字形留白 ≈1em 造成视觉缩进，改写文案避开、别用 CSS 硬拗；③ 小号 `Tab` 自带 `spacingHorizontalMNudge`（10px）水平内边距，与 PageHeader 标题对齐用负 margin 抵消（具体值从 `@fluentui/react-tabs/lib/components/*/useTabStyles.styles.js` 里 grep `padding:var(` 核实，勿凭记忆）；④ Griffel 禁 CSS shorthand（`borderColor` 必须拆四边 longhand，否则 tsc 报 TS2322）；⑤ 双主题 hover 色无现成令牌时用 `color-mix` 在基准令牌上加深；⑥ **同一 CSS grid 行里只给某个 `Field` 传 `hint`，会把同排其他 `Field` 的控件撑高**——`Field` 根样式是 `display: grid`，`align-content` 默认 `normal`（对 grid 即 `stretch`）会把高出来的部分平摊给自动行，实测标签行与控件行各 +9px（下拉框 32px → 43px、顶边比旁边两个框高 9px），三个框既不等高也不齐平。说明文案要么同排 `Field` 都给，要么收进 `Dropdown` 的 `Option`（`Option` 传自定义节点时必须同时给 `text`，否则 tsc 报缺 `text`；两行选项外面包一层 `flexDirection: column`，`Option` 自身无固定高度、也不 `nowrap`，会自然长高）。
- **管理端说明文字是纯 JSX，Markdown 语法不生效**：`**加粗**`、`# 标题`、`- 列表` 直接写进 JSX 文本会**按字面显示**（`react-markdown` 只挂在学生端题面，见 8.3）。要着重就用 `<Text weight="bold">…</Text>`。这条是 0.3.2 反馈「可教组别说明里的 `**` 没渲染」的根因，新增说明文案照此办理。

### 8.2 路由表

| 路径 | 页面 | 角色 |
|---|---|---|
| /login | 登录（专属品牌色动态背景，不显示站点背景图） | 匿名 |
| /change-password | 强制/自助改密页：`must_change_password=1` 时全站唯一可达的业务页，改完按角色回首页（PW-02/03） | 全部 |
| /account | 账号设置：改自己的密码（头像菜单 →「账号设置」入口；规则与 `/change-password` 同源） | 全部 |
| /student/assignments | 我的场次列表 | student |
| /student/assignments/:id | 场次详情（题单+我的成绩列） | student |
| /student/assignments/:id/problems/:pid | 题目详情：题面、样例、提交框、我的历史 | student |
| /student/submissions/:sid | 提交详情（逐点判定） | student |
| /teacher/problems | 题库列表（关键词搜索、按 `group_name` 分组筛选、行内删除） | teacher |
| /teacher/problems/:id | 题目编辑（含用例编辑器、样例标记、草稿：存为草稿 / 重进询问「是否使用草稿」，按钮「是 / 放弃」） | teacher |
| /teacher/assignments | 发布（页内 Tab：全部/作业/考试；教师侧 mode 文案统一叫「考试」，学生端仍叫「测试」） | teacher |
| /teacher/assignments/new | 发布页（模式/时间窗/选题/次数/计分） | teacher |
| /teacher/assignments/:id | 场次总览（统计卡片+每题通过率+分数分布） | teacher |
| /teacher/assignments/:id/students | 逐学生表（含未交、学号列、「最高单题分」与「总分」列）+「导出 Excel」 | teacher |
| /teacher/assignments/:id/students/:sid | 0.3.2 F6：逐学生**按题**调分页——题号/标题（链到题目）、满分、提交次数、系统分、教师调分、有效分，行内 `NumberInput` + 保存/撤销，另有「查看最新提交」 | teacher |
| /teacher/submissions/:sid | 提交下钻（代码+逐点+调分+重判按钮；调分越界即时报错；**入口=逐学生按题页的「查看最新提交」**；0.3.2 F6 已删掉页面底部那个冗余「← 返回」） | teacher |
| /teacher/students | 我的学生（0.3.2 F1：左列自建**子分组**〔新建/改名/删除/移入移出〕，右列名单成员——进页面即全部可教学生，**「从班级拉学生」已下线**；保留按学号添加与「移出手动添加」、`?q=` 搜索；子分组同时用于发布受众；可教组别降级为只读徽标。**全页无层 1 组别编辑控件、无密码操作**，GR-04） | teacher |
| /admin/teachers | 教师管理（搜索、建号不填密码、重置密码出随机临时密码） | admin |
| /admin/teachers/:id | 教师详情（0.3.2 F1）：**可教组别**＝已分配组列表（组名+人数+「移除」）＋「添加组」弹窗勾选，保存仍是全量替换；**名单改为矩阵 chip**（`小明(ID:01)`，Tooltip 给学号与来源，手动添加者带淡色角标）＋ 姓名/学号搜索 ＋ 重置密码 | admin |
| /admin/students | 学生管理：xlsx/txt/csv 四列导入、多选批量（加入/移出分组、批量重置密码〔>20 人询问统一/随机〕、批量启停）、分组管理（新建/重命名/删除）、单个重置密码与停用、`?q=`/组别筛选（「改密状态」列与筛选自 0.3.1 起移除）、「导出初始密码表」 | admin |
| /admin/audit | 审计日志（AU-06）：时间/操作者/动作/对象/详情表格（**动作与对象的中文名由后端下发**，0.3.2 F5），按 action 与 target_type 筛选 + 加载更多 + **导出 Excel**；`audit_enabled=0` 时本路由重定向到 `/admin/settings` 且导航项不渲染；**无任何修改/删除入口** | admin |
| /admin/settings | **系统设置**（0.3.2 F5 起由「主题设置」更名，导航标签同步）：主题色（亮/暗两份，含"从背景图提取"开关）、背景图上传/清除（亮暗独立开关 + 两个槽位）、透明度滑杆，**外加一张「审计日志」卡片**（启用开关 + 保留天数〔留空=永久〕+ 关闭不删历史的说明 + 跳转审计页） | admin |

路由守卫：无 token → /login；角色不符 → 403 页；**`must_change_password=1` → 除 `/change-password`（与退出）外一律重定向到它**（PW-02 前端侧，与后端 `get_current_user` 的白名单两道并行，缺一不可）。重置/批量重置的凭证 Dialog 与 CSV 全部由前端用响应 JSON 拼装（坑 22/26）。

### 8.3 关键交互与组件选型

- 提交后跳转提交详情页，**每 2s 轮询**直到 `status != pending/judging`（M1 方案；SSE 留待后续）。
- 判定徽章颜色：AC 绿 / WA 红 / TLE 橙 / MLE 紫 / RE 棕 / CE 灰（Fluent 语义色令牌）。
- 代码提交框/代码查看：**@uiw/react-codemirror**（C 模式；提交框可编辑、详情只读）。
- 题面渲染：**react-markdown**（默认不渲染原始 HTML，天然防 XSS）。
- 分数分布：**recharts** 柱状图，配色取 Fluent 主题令牌。
- 时间显示：**dayjs** 统一格式化。
- 主题设置页：原生 `input[type=color]` 取色器（共用 `BrandColorField`；启用亮暗分图且已传暗图时多出一个「暗色模式品牌色」取色器）+ Fluent Slider 透明度条 + 图片上传（带预览与清除）；修改即时全站预览，保存落库；其他用户下次加载生效。

---

## 9. 里程碑与验收标准

> 每个里程碑 = 一次或数次 AI 会话。任务清单按序执行；验收标准逐条过。

### M1 可跑核心（后端骨架 + 判题流水线 + 前端壳）

任务清单：
1. 仓库初始化、目录结构（第 4 章）、.gitignore、requirements/package 依赖落盘；Alembic 初始化。
2. core（config/db/security）+ models 全表 + 首个迁移 + init_admin 脚本。
3. auth 接口 + 角色依赖。
4. 教师：problems/cases 的 CRUD 接口。
5. 学生：提交接口（先不做场次，M1 用"直接对题提交"的临时路由 `/api/dev/submit/{problem_id}`，M2 删除）+ 提交查询接口。
6. **核实 go-judge**：按官方 README 拉镜像、起容器、curl 冒烟一次编译+运行；把镜像名/端口/请求字段核实结果回写本文档 3.2/7.3/7.4。随后实现 gojudge 适配器 + compare + worker 全链路。
7. 前端：Fluent 壳（登录、布局、主题切换）+ 学生题目页（题面/样例/CodeMirror 提交/轮询结果）+ 教师极简建题页。
8. tests/fixtures 全部样例程序（第 10 章）。

验收标准（逐条贴结果）：
- [ ] `uvicorn` 启动无报错；`/docs` 可见全部 M1 接口；`alembic upgrade head` 幂等可重跑。
- [ ] go-judge 容器健康；worker 启动健康检查通过。
- [ ] admin 登录成功；错误密码 401。
- [ ] 教师建题 + 3 个用例（1 样例 2 隐藏）。
- [ ] 提交 `ac.c` → 轮询到 `done`，verdict=AC，score=满分，逐点全 AC。
- [ ] 提交 `wa.c` → score 按通过用例权重折算正确。
- [ ] 提交 `tle.c`（time_limit=500ms）→ 3.5s 内返回 TLE；go-judge 无残留进程。
- [ ] 提交 `mle.c` → MLE。
- [ ] 提交 `re.c` → RE；`ce.c` → CE 且学生能看到编译错误文本。
- [ ] 提交 `fork_bomb.c` → 判 RE/TLE 之一，**go-judge 与 worker 存活、宿主机无异常**。
- [ ] 提交 `bigout.c` → 正常返回（输出被截断），服务内存无暴涨。
- [ ] 前端：登录 → 看到题面与样例 → CodeMirror 粘贴代码提交 → 看到逐点判定；亮暗主题切换正常。

### M2 双模式与放分

任务清单：
1. assignments/assignment_problems 接口（教师发布/修改）。
2. 学生场次接口族（6.2）+ 6.3 校验顺序 + 6.4 可见性。
3. release 接口；rejudge 接口。
4. CRLF 归一化（提交与用例录入两侧）。
5. 管理员绑定接口（teacher_students GET/PUT）——M2 只做接口 + Swagger 验证，UI 放 M3。
6. 前端：学生场次列表/详情/题目页改造为走场次路由；测试模式下结果脱敏展示。
7. 删除 M1 临时路由 `/api/dev/submit/*`。

验收标准：
- [ ] 作业模式：提交后即时看到逐点详情。
- [ ] 测试模式：提交后学生只看到"已判题（待放出）"；教师 overview 能看到分数。
- [ ] 测试过 end_time 后学生提交 → 409 WINDOW_CLOSED；教师 release 后学生可见详情；未过 end_time 调 release → 409。
- [ ] max_submissions=2 时第 3 次提交 → 409 SUBMIT_LIMIT。
- [ ] 未绑定该教师的学生访问场次 → 404。
- [ ] Windows 记事本存的 CRLF 代码提交后正常 AC（不出现 CE/WA）。
- [ ] score_policy=last 时有效分取最后一次；best 时取最高（构造两次不同分提交验证）。

### M3 教师端统计 + 管理员端 UI

任务清单：
1. stats 服务 + overview/students 接口。
2. 调分接口 + 有效分联动（overview、学生端同步变化）。
3. 管理员 UI：教师管理、绑定勾选、学生管理、导入学生（当时为 CSV 两列，v2.0 已扩展为三格式三列，见 M5）、重置密码。
4. 教师 UI：总览页（统计卡片、每题通过率、recharts 分数分布）、逐学生页、下钻页（调分/重判）。
5. 前端细节打磨：空状态、加载态、错误提示全走 Fluent 组件。
6. 站点设置：site_settings 接口族 + 管理员主题设置页（主题色/背景图/透明度）+ 前端运行时主题生成与背景图层（登录页用其独立的 `LoginBackdrop`）。

验收标准：
- [ ] 3 学生 2 题场景下，overview 数字与手工计算一致（含未交者计入 total）。
- [ ] 调分后：学生端有效分、overview 平均分、逐学生表三处同步变化；撤销调分恢复。
- [ ] 重判后结果与首次一致（幂等）。
- [ ] CSV 导入含 1 行坏数据：好行入库、坏行在响应中列出原因。（v2.0 已扩展为 xlsx/txt/csv **三格式三列** `学生ID|姓名|组别`，见 M5）
- [ ] 绑定取消后，该教师场次列表对相应学生立即消失。
- [ ] 管理员改主题色 → 全站（按钮、焦点环、徽章、图表）即时换色，刷新后保持，三种角色均可见。
- [ ] 管理员上传背景图并设透明度 0.2 → 所有登录后页面按该透明度显示；透明度设 0 或删除图片恢复纯色背景。
- [ ] 背景图连续换图：上传图 A → 清除 → 上传图 B，页面（预览与整页背景）立即显示 B；刷新后仍是 B。
- [ ] 登录页背景为品牌色动态背景，且不受背景图/透明度影响；亮色、暗色、手机窄屏三态均正常且无横向滚动。
- [ ] 上传 >5MB 的图 → 弹 Fluent 压缩确认框；确认后在浏览器本地压到 ≤2MB 且上传成功（消息条显示压缩前后体积），取消则不上传。
- [ ] 打开「亮色/暗色使用不同背景图」并分别上传两张 → 切亮/暗主题各显示各的；只上传一张时两个模式都用它。
- [ ] 打开「主题色从背景图提取」→ 换图后主题色跟随变化（可「撤销取色」回退）；手动改品牌色后该开关自动关闭。
- [ ] 亮暗分图 + 抽色开关：分别上传两张图后，切亮/暗模式**主题色跟着各自背景图变**（按钮、链接、焦点环、选区色都随之切换）；关掉「亮暗分图」后暗色回用亮色主题色；「撤销取色」一次回退两个槽位。
- [ ] 头像菜单 →「账号设置」→ 改密码：旧密码错给 400 提示、两次不一致被前端拦截、成功后新密码可登录。
- [ ] 暗色模式走查全部路由：无硬编码白底/黑字块；CodeMirror、recharts、背景图层、acrylic 导航均正确适配。
- [ ] 品牌色阶方向：亮色主题下加载动画圆环轨道为**浅蓝**（非近黑）、弧线仍为 `brand_color` 原色、标签文字为中性灰；主按钮 hover/pressed 比常态更深；浅蓝徽章底色不为近黑。
- [ ] 任意页面用鼠标划选正文与副标题：选区底色半透明、能透出下层背景，文字始终可读（含 Dialog 弹层内文本）。
- [ ] 设置自定义背景图后：页面卡片呈毛玻璃（背景图被高斯模糊），顶导航与卡片风格一致；把透明度拖到 0 或清除背景图后卡片回到不透明实色。
- [ ] 页面标题规范：所有页面副标题都在大标题**下方独占一行**、左对齐；右侧操作按钮在无副标题页仍与标题同行；窄屏（≤640px）标题区自动换行、不产生横向滚动。

### M4 部署与运维

任务清单：
1. Dockerfile.web / Dockerfile.judge / compose.yml（三服务：web、worker、go-judge；go-judge 仅内网、固定版本 tag、按 README 权限配置）。
2. 数据卷：`backend/data` 挂载；备份脚本（sqlite `.backup` + data 目录 tar，含 cron 示例）。
3. 运行与部署 runbook = 仓库根《部署操作指南.md》（面向零基础；第一部分为本机 Windows 开发运行含一键脚本，第二部分为 Linux 服务器分步部署，含 FAQ、备份与恢复演练、命令速查表）。
4. 账号批量导入 runbook；初始密码策略说明。
5. 运维脚本与模板：`.env.example`、`scripts/backup.sh`（保留 7 份 + cron 示例）、`scripts/reset_password.py`。

验收标准：
- [ ] 干净 Linux 机器按 runbook 从零部署成功，浏览器可登录。
- [ ] `docker compose ps` 三容器健康；go-judge 容器 `ping 8.8.8.8` 不通（断网验证）；web 容器 curl go-judge 不通（仅 worker 可达）。
- [ ] 备份脚本执行一次并成功恢复到一个临时实例验证。

### M5 分组、效率与体验增强（v2.0 / 0.2.0 批次，已完成）

任务清单：
1. 404 页按 Fluent Design 2 重写：居中式提示页、卡片毛玻璃、配色全走主题令牌。
2. 学生分组落地为**多组模型**：Alembic 0003 建 `groups` / `group_members`，`problems` 加 `group_name`、`draft`、`draft_saved_at`；服务层 `services/groups.py`（成员映射、自动建组、幂等增删、单事务）。
3. 管理员端：分组 CRUD（重名 409）、学生批量加入/移出分组、批量重置密码（只算一次 bcrypt 复用）、批量启停；`/admin/students` 响应带 `groups`。
4. 管理员/教师学生页多选批量条 `BulkActionBar`（按钮由调用方注入）。
5. 教师端新增「我的学生」页 `/teacher/students`：批量调整分组（先校验绑定关系，越权整批 403）。
6. 教师导出场次成绩 xlsx（`services/export.py` + `GET /api/teacher/assignments/{id}/export?tz_offset=`，openpyxl 流式下载）。
7. 题库：关键词搜索、`group_name` 自由文本分组与筛选、行内删除（被场次引用 409）。
8. 导入升级到 xlsx/txt/csv 三列 `学生ID|姓名|组别`（不存在组自动创建、空行静默跳过、失败行显示学号）+ 题目编辑草稿存后端（`problems.draft`，离开时询问存草稿、重进时询问恢复、保存即清草稿）；「场次管理」改名「发布」+ 页内 Tab（全部/作业/考试），教师侧 mode 文案统一「考试」。

验收标准（2026-09-20 逐条执行）：
- [x] 后端 `pytest tests/` → **207 passed, 1 skipped**；其中新增 `tests/test_groups_api.py` 44 例、`tests/test_import_export.py` 48 例。
- [x] Alembic **0003** 在含真实数据（14 用户 / 20 题目）的库副本上 upgrade → downgrade → upgrade 可逆，数据无损。
- [x] 教师 token 调任意 `reset_password` 路径 → 404；教师对未绑定学生批量分组 → 403 `STUDENT_NOT_BOUND` 且查库确认 `group_members` 零变化。
- [x] 批量 add 幂等（第二次 `success_count=0`、无重复行）；组重名 409 `GROUP_NAME_EXISTS`；批量重置 N 人只调用 1 次 bcrypt。
- [x] 导入三格式实测：`学生ID|姓名|组别`、多组分隔符、不存在组自动建、空行静默跳过、3 行样本成功 2 / 失败 1 且失败行显示学号（`username`）。
- [x] 导出 xlsx 浏览器实跑：请求带 `?tz_offset=480`，返回 xlsx MIME 与 `filename*=UTF-8''` 中文名，表内时间为本地时区。
- [x] 草稿全链路：改表单 → 返回题库弹「取消/直接退出/存为草稿并退出」→ 重进弹「检测到未保存的草稿，是否使用草稿内容？」（说明只给本地时区的保存时间，按钮「放弃 / 是」）→ 保存题目即清草稿。
- [x] 前端 `npm run build`（含 `tsc --noEmit`）0 错误；浏览器走查确认教师学生页全文无「密码」字样、多选过滤后选中不错位、分组管理对话框成员计数正确、404 页在自定义背景图下呈毛玻璃卡片。
- [x] 暗色模式全路由复核：以计算样式判定（读 `FluentProvider` 上的 `--colorNeutralBackground1`，暗色为 `#292929`），逐路由扫描 `main` 内可见元素，无近白不透明块、无近黑正文；覆盖 admin/teacher/student 各页与 404、403、账号设置。两处初判「缺陷」经核实为 Fluent 自身产物，非问题：`fui-Tab__content--reserved-space`（`visibility: hidden` 的占位测量副本）、`input.fui-Slider__input`（`opacity: 0` 的原生控件，视觉由外层 `.fui-Slider` 绘制）。

### M6 权限与密码体系（0.3.0 批次，已完成）

> **实施依据**：仓库根**《权限与密码体系修改意见.md》（v1.1 审查修订版）**，编号规则 PW / AU / BD / LI / SC / GR / IM / DM / QA 与附录 A 机器可读策略块为权威；本批次范围内它与主文档 §5/§6.2 现状描述冲突时**以修改意见为准**，批次完成后按 §0.1 规则把最终状态回写主文档（§1.3/§5/§6.2/§11/§12 与坑 24 改写）。
>
> **本批次版本号固定 `0.3.0`**：无论产生多少 commit，`frontend/package.json` 的 version 一律保持 0.3.0；每个独立更新提交一个本地 commit、**不推送**（§0.1 规则 8）。

2026-09-21 审查结论（已回写修改意见 v1.1，此处摘要备查）：

- 初稿引用的现状代码位置**逐条核实无误**：教师改组接口（teacher.py:472-483）、admin 直绑接口（admin.py:183）、调分无边界（schemas.py:324）、rejudge 不清 manual_score（teacher.py:415-432）、best_effective_score 为最高单题分（stats.py:113）、导出固定 5 列、bcrypt cost=12、迁移下一号 0005。
- 修复的规格缺口：① PW-05 的 7 天过期原本无数据支撑 → DM-05 `password_updated_at` 升为 P0 必选（过期锚点，NULL=不过期）；② PW-08 字符集实为 **56** 字符（JSON 块多排除了小写 o，以 JSON 为准）；③ IM-01"教师列"初稿自相矛盾（校验后无效果）→ 用户拍板改为**写入 teacher_groups**，教师列无组别同行的按失败行处理；④ PW-06 逐生随机密码与坑 24"整批一次 bcrypt"冲突 → 阈值 20：≤20 串行随机，>20 前端询问"统一 12345678 / 各自随机"，随机且 >20 走线程池（8 workers）并行；⑤ 审计只有 API 没有页面，实际用不上 → 新增 AU-06 admin 审计页。
- 用户拍板：PW 方案**同等适用于教师账号**（新建教师初始密码也是 12345678、admin 重置教师也返回随机 8 位、`AccountCreate` 移除 password 字段、种子 admin 首登强制改密）；**存量账号迁移后一律 `must_change_password=1`**；新增 PW-09 凭证导出（重置响应返回 JSON 明细、前端拼 CSV，学生列表可导出初始密码表）。
- 实施注意：PW-02 后端拦截必须放行 `/auth/login`、`/auth/me`、`/auth/password`、`/api/settings*`（改密接口也走 `get_current_user`，直接拦会锁死自己）；SC-03 的 `null`=撤销调分必须保留；BD-06 只删 PUT，`GET /admin/teachers/{id}/students` 保留只读；`GET /api/teacher/groups` 随 GR-02 一并废弃（教师可见组以 `/teacher/classes` 为准）。
- 测试影响面：`test_groups_api.py` 中教师 group_members 4 例、批量重置单 hash 2 例、PUT teachers/students 相关用例将失效，按新规则重写。

任务清单（按依赖顺序）：

1. Alembic 迁移 `0005`（DM-01~05：must_change_password、password_updated_at、audit_log、teacher_groups、manual_score_updated_at；存量行全部置 must_change=1）+ models/schemas 同步；库副本上验证 upgrade→downgrade→upgrade 可逆。
2. 密码与审计基础设施：`generate_temp_password()`（56 字符集）、`validate_new_password()`（PW-03）、`services/audit.py::log_audit()`、强制改密拦截依赖（含白名单）、config 阈值常量。
3. auth：登录响应/`UserOut` 加 `must_change_password`、PASSWORD_EXPIRED 判定、`/auth/password` 新规则 + 成功置 0 + 审计。
4. admin：建学生/教师去 password 字段（初始 12345678 + 置标志 + 审计）；单/批量重置改造（随机/统一、阈值询问、并行哈希、明细响应）；删 `PUT /admin/teachers/{id}/students`；新增 `GET/PUT /admin/teachers/{tid}/groups` + 审计；`GET /admin/audit_logs`；列表 `?q=&group_id=&must_change=`；组员/启停操作补审计。
5. teacher：新增 `GET /classes`、`POST /students/bind_from_class`、`/students/bind`、`/students/unbind` + 审计；删 `POST /students/group_members` 与 `GET /groups`；`GET /students` 加 `?q=`；rejudge 清 manual_score（SC-04）；调分边界 + 审计（SC-03）。
6. 成绩：`student_rows` 加 `total_score` 与每题有效分（SC-01）；导出表头改"最高单题分"+ 新增"总分"+ 动态每题列（SC-02）。
7. 导入：第 4 列"教师"（写 teacher_groups、失败行规则）+ 初始密码改 12345678 + 审计（IM-01/02）。
8. 前端：`/change-password` 强制改密页 + Guard 拦截；重置结果展示/CSV 下载；批量重置询问 Dialog；学生列表"导出初始密码表"与"未改密"徽标、搜索框；admin"教师-组分配"界面（diff 确认）、审计日志页 `/admin/audit`；教师端"从班级拉学生"界面、移除组别编辑入口；创建表单去密码框；`api/index.ts` 清理废弃端点；version 置 0.3.0。
9. 测试：重写失效用例 + 新增密码流程/审计/三层绑定/调分边界/导入教师列测试；`pytest` 全量回归、`npm run build` 通过。
10. 文档回写：§1.3 角色表、§5 DDL 与迁移段、§6.2 接口表、§11 安全清单、坑 24 改写（批量重置不再是"只算一次 bcrypt"一条路）+ 新增坑（强制改密白名单、凭证 CSV 走前端拼装）；修改意见各规则 status 更新为 done。

验收标准：执行《权限与密码体系修改意见.md》§11 **QA-01~QA-21** 逐条贴结果，另加：

- [x] `alembic upgrade head` 在含真实数据的库副本上可逆（upgrade→downgrade→upgrade），存量账号首登全部被强制改密。**证据**：`cg.db.bak-20260920-mig0004` 副本（14 用户 / 46 提交）0003→0005→0004→0005 全程数据无损、`audit_log`/`teacher_groups` 与两枚索引存在、14/14 账号 `must_change_password=1`；本机开发库已升到 0005（备份 `cg.db.bak-20260921-mig0005`），实跑 `POST /api/auth/login`（admin/admin123）响应 `must_change_password: true`。
- [x] `pytest tests/` 全量通过；`npm run build`（含 tsc）0 错误。**证据**：M6 实施当轮 `290 passed, 1 skipped`（M6 前为 207 passed / 1 skipped，新增 `test_password_flow.py` / `test_admin_password_audit_api.py` / `test_teacher_binding_api.py` 并按新契约重写失效用例）；**2026-09-21 补 `tests/test_score_adjust_rejudge.py`（QA-11/12）后全量 `306 passed, 1 skipped`**；`npm run build` → 3290 modules transformed、built in 7.71s、tsc 无错（>500kB chunk 警告为既有状况）。
- [x] 暗色模式走查新页面（改密页/审计页/组分配界面/拉学生界面）无硬编码色。**静态核查**：`ChangePassword.tsx` / `AuditLogPage.tsx` / `CredentialDialog.tsx` / `TeacherDetail.tsx` / `teacher/StudentList.tsx` 内 `#rrggbb` 与 `rgb(` 命中数均为 0，含「未改密」徽标也走 `t.colorPaletteDarkOrangeForeground1` 令牌。**浏览器逐页走查已于 2026-09-21 补做完成**（隔离快照库 + 8010 后端 + 临时 vite 45173，见下条取证）。
- [x] `frontend/package.json` version=0.3.0。**证据**：npm 输出包名即 `qinglan-frontend@0.3.0`；本批次 commit 均为本地提交、未推送远端。

M6 已产出的实测结论（QA 逐条结果的补充取证）：

| 项 | 结论 |
|---|---|
| QA-01/18 强制改密拦截 | 真实服务（新代码起在 127.0.0.1:8010、连本机已迁移库）实测：登录后 `/api/admin/students`、`/api/admin/audit_logs`、`/api/teacher/classes` 全部 **403**，`/api/auth/me` **200** 带标志、匿名 `/api/settings` **200** —— 白名单形状正确（坑 25）。种子 admin 首登同样被强制改密 |
| QA-02/05/16 密码流程 | `tests/test_password_flow.py` **27 例全绿**：56 字符集与长度、批量唯一性、`PASSWORD_POLICY`（<8 位 / 含空格 / 等于用户名 / 等于姓名 / 等于旧密码 / 等于初始密码）、改密后业务放行、旧随机密码再登录 401、`password_updated_at` 锚点与审计 `user_change_password`、8 天前锚点 → 403 `PASSWORD_EXPIRED`、`password_updated_at=NULL`（统一/初始密码）不受 7 天限制、`must_change=0` 的旧锚点不误判 |
| QA-17 大批量并行哈希 | **500 人 `mode=random` 实测 11.4s**（验收线 <60s），500 个密码互不相同且均 8 位，全员 `must_change_password=1`、`password_updated_at` 落锚点、`expires_at = +7 天`；`mode=unified` 由测试用 monkeypatch 计数守住「整批只算一次 bcrypt」 |
| QA-13/19 红线 | 静态核查：前端全库对 `POST /teacher/students/group_members`、`GET /teacher/groups` 无任何调用（仅注释提及），`api/index.ts` 不再导出废弃函数；audit 相关只有一个 `GET /admin/audit_logs`，不存在任何修改/删除审计的入口 |
| QA-10 口径 | `stats.student_rows` 的 `total_score` = 题单内各题有效分之和，`best_effective_score` 保持「最高单题分」原算法与口径（含已移出题单的提交，仍不计入总分）；导出实测表头 `学号\|姓名\|提交次数\|最高单题分\|总分\|<每题标题…>\|最后提交时间`，未提交的题读回为 `None`（坑 23）。**浏览器复核**：逐学生成绩页 12 列表头与题单 seq 一致，未作答的每题格渲染为**空 cell**（不是 `—`），行列对齐无错位 |
| QA-11/12 调分与重判 | 新增 `tests/test_score_adjust_rejudge.py` **16 例**：`0/100/99.99` 边界放行、`-5/-0.01/100.01/101` → 422 `MANUAL_SCORE_OUT_OF_RANGE` 且库里两列与审计全零变化、题已被移出场次（无 `assignment_problems` 行）→ 422「未在本场次中配置分值」、`null` **与省略字段**都撤销且 `manual_score_updated_at` 一起回 NULL、二次调分审计 `old` 取上一次值、他人场次 403 / 提交不存在 404、rejudge 后 `manual_score`+锚点+`worker_id`+`SubmissionResult` 全部清空并回到 `pending`、重判后重新按新边界校验。**浏览器侧另记**：提交详情页用 ±箭头 递增到 1 → 点「保存」→「调分已保存」、教师调分=1、有效分由 0 变 1、「撤销调分」按钮出现 → 点它 →「已撤销调分，恢复系统判分」、回「未调分」/有效分 0、按钮消失；库里对应 3 条 `score_manual_adjust`（`old/new` = null→1、1→1、1→null，`target_type=submission`、`target_id` 与 `actor_id` 正确），终态两列均 NULL。**但手输数值不能生效**，见「本批次遗留」第 4 条 |
| QA-01/02/03/04/06/07/08/09/13/17/19/20 浏览器走查 | 2026-09-21 在隔离快照库（`backend/.tmp-verify/cg.db`，`DATA_DIR` 指过去）+ 8010 后端 + 临时 vite 45173 上逐条走过：强制改密拦截（业务页 URL 被改写为 `#/change-password`，按钮「确认修改并继续」，无跳过入口）；`12345678` 作新密码 → 页面内 MessageBar 报「密码不能与学号/用户名、姓名、旧密码或初始密码相同」且不放行；改密成功后 `must_change_password=false`、全站可用，自助改密页文案为「自助修改密码…」（按钮「保存新密码」，与强制页是同一组件的两种形态）；单个重置返回随机 8 位 + 有效期，**该随机密码随后真的能登录**（200）并立刻被强制改密；批量 33 人 → 弹「A. 统一重置 / B. 各自随机（33 人）」，选 B 后逐行明细 + 复制/下载 CSV，CSV 表头 `学号,姓名,临时密码,有效期`、33 行与屏显一致、**不发任何新请求**；「导出初始密码表」同样纯前端（`学号,姓名,初始密码,有效期=不过期`）；学生列表 `?must_change=1` 让 35→34 行、徽标只剩「未改密」，`?q=批量生1&group_id=102` 组合过滤命中 11 行/0 行；教师详情只读名单 + 「原勾选学生绑定教师的写入口已随 BD-06 删除」，可教组别保存前弹 diff 确认（新增 1 个组别「走查三组」/移除 0 个）；教师「我的学生」只有「从班级拉学生 / 按学生 ID 添加」两个入口、无任何组别编辑控件，拉人面板只列可教班、已在名单的置灰标「已在名单」、停用的仍可勾，全选本班 26 人 → `POST /teacher/students/bind_from_class` 名单 3→29；审计页由本轮操作自然攒到 53 条（启停 24 人 → 逐人 48 条），初始 `limit=50&offset=0` 显示「加载更多（3 条）」→ 点完变「已加载全部」，`?action=teacher_group_assign` 精确筛出 1 条，页面上没有任何改/删审计的按钮 |
| QA-21 暗色逐页扫描 | 暗色（`--colorNeutralBackground1=#292929`）下逐页扫「不透明近白背景块 / 近黑直接文本 / 文字与祖先背景对比度 <55」三类缺陷，覆盖强制改密页、凭证对话框、学生管理、教师详情（组分配）、审计页、教师我的学生、逐学生成绩页：**0 命中**；亮色（`#ffffff`）下对学生管理/审计页/教师详情再扫一遍同样 0 命中。**两个取证陷阱**：① 前端是 **HashRouter**，`navigate` 到 `/admin/students` 只会落到默认页，深链必须写 `/#/admin/students`；② 教师调分框（`components/NumberInput.tsx` 包 Fluent `SpinButton`）的 React state **只被 ±箭头的 onChange 驱动**：合成 `input`/`change` 事件与真实键入结果一样——数字显示在框里，失焦即被清空、state 仍 null，点保存恒报「请先输入调分数值」（负数再叠一层 `min={0}` 静默吞掉，提示语同样误导）。要驱动它只能 `click` 箭头按钮，取证见 QA-11/12 行与「本批次遗留」第 4 条 |
| QA-05/16 真实服务复验（非 TestClient） | 在快照库上把 `m1s01` 的 `password_updated_at` 改到 8 天前（`must_change=1`）→ `POST /api/auth/login` 返回 **403 `PASSWORD_EXPIRED`「临时密码已过期，请联系管理员重新重置」**；admin 重新重置 → 200，明细字段是 `student_id/username/display_name/**temp_password**/expires_at`（**密码字段名是 `temp_password`，不是 `password`**，踩过一次 422），`expires_at` 正好 +7 天；新临时密码登录 200 且 `must_change_password=true` → 走 `/api/auth/password`（`{old_password,new_password}`，**成功返回空体，不能 `.json()`**）改密后：**旧临时密码再登录 → 401 `BAD_CREDENTIALS`**、新密码 200 且 `must_change=false`、`/api/student/assignments` 200 放行 |

本批次遗留（不阻塞 0.3.0，留待后续）：

- 单题场次时后端的每题列与「总分」同值（页面按口径刻意不重复出题名列，导出仍带那一列）。
- 审计 `detail` 的个别取值形状（如 `student_import` 的 `target_type="user"`、教师重置复用 `TempCredentialOut.student_id` 承载教师 id）规格未逐条点名，按实现取用并已在前端 types 注释标明。

### M7 本机开发端口自适应（0.3.1 批次，已完成）

> **本批次版本号固定 `0.3.1`**：`frontend/package.json` 的 version 由用户在正式发布时统一改，批次内的 commit 不动它。

背景（2026-09-23 用户报"启动脚本失败"）：这台 Windows 机器的 TCP 动态端口池是 **1024-15000**（注册表 `Tcpip\Parameters\MaxUserPort=15000`；Windows 默认应为 49152-65535，机器上装有深信服 aTrust 零信任客户端，此类值多半由它或某个"网络优化"写入），WinNAT / Hyper-V 每次开机再从池子里整段划走端口做地址转换。**落在保留段内的端口绑不上，而且症状不是"端口被占用"**：Docker 报 `bind: An attempt was made to access a socket in a way forbidden by its access permissions`、Vite 报 `listen EACCES`（Vite 只在 `EADDRINUSE` 时才自行顺延，EACCES 直接退出），`netstat` 查不到任何占用者。本机 5050 与 5173 都在保留段内，且保留段会随容器启停实时变化。

变更：

- `scripts/start-local.ps1`：默认端口仍是 5050 / 5173。先真绑一次探测，失败则读 `netsh interface ipv4 show excludedportrange protocol=tcp` **整段跳过**保留区间（本机 5050 上方是 5041-5740 连续七段，逐个试要试 700 次），取第一个可绑定端口。实际端口写入 `backend/data/dev-ports.json`（`{judge, frontend}`，随 `backend/data/` 一起 gitignore）。
- 沙箱容器的端口映射在**创建时**就固定，`docker start` 改不了：已在跑的容器一律沿用其端口，不因"与默认值不同"而重建；只有 `docker start` 失败才删掉换端口重建。`docker run` 端口绑定失败会留下 `created` 空壳，必须先 `docker rm -f` 再试下一个端口（最多 5 次）。
- 端口同步：脚本给子进程设 `GO_JUDGE_URL`；`backend/app/core/config.py` 在环境变量缺失时改读 `dev-ports.json`；`frontend/vite.config.ts` 的 `server.port` 同样读它——手动起 uvicorn / worker / `npm run dev` 都不必再传端口。
- 顺带修：`docker start` 原先不检查退出码，会在 docker 报错之后仍打印 `[ok] 已启动既有沙箱容器`。
- 文档同步：《部署操作指南.md》§0 端口说明与 L6 两条新 FAQ（EACCES 类报错的识别、容器端口映射不可改）、README 快速开始、本文 3.2 / 7.7 / 附录 A / 附录 B 的端口口径。

验证：本机默认端口均被保留，实测顺延到 5341 / 5342 跑通全链路（沙箱 `/version`、后端 `/docs`、前端 `/` 均 200，`/api` 代理返回 401 未登录属正常）；不设 `GO_JUDGE_URL` 时 `config.py` 从 `dev-ports.json` 解析出 5341，`tests/test_integration_gojudge.py` 的 10 项真实判题（AC / WA / TLE / MLE）**首次在本机执行**并通过（此前沙箱根本起不来、整模块被 skip），后端全量 316 passed，前端 `tsc --noEmit` + `vite build` 通过；端口选择函数五个分支（默认可用→用默认、被占→顺延、避开已分配端口、优先沿用上次的、默认释放后回归默认）逐条实测；脚本二次运行全 `[skip]`，幂等。

彻底恢复系统默认的可选做法（需管理员 + 重启，本机未做）：`netsh int ipv4 set dynamicport tcp start=49152 num=16384`。服务器为 Linux，不受影响，容器内外都是 5050。

---

### M8 名单模型重构、逐题调分与审计可配置（0.3.2 批次，已完成）

> **本批次版本号 `0.3.2`**：批次内各功能 commit 不动 `frontend/package.json`，收尾由一条 `chore(release): 更新版本号到 0.3.2` 统一 bump（README 的 shields badge 读的就是它），是否 push / 打 tag 由用户决定。

来源：2026-09-23 用户实机走查 7 条反馈（含 admin 教师详情只读名单卡、教师管理列表两张截图）。7 项功能更新、2 次数据库迁移，按「一个功能性更新一个 commit」落地。

变更：

- **F1 名单口径重构 + 教师子分组 + 发布受众**（最大一项，拆 F1-1~F1-4 四条 commit + 一条后端补正）：`stats.bound_students` 从「层 3 手动名单」改为**「可教组别成员并集 ∪ 手动添加」**——它是受众/成绩/学生端可见性的唯一口径，改这一处即全局生效；撤销组别即刻清名单，并由 `teacher_notices(kind='group_revoked')` 给教师弹窗说明。教师端新增子分组 CRUD 与成员全量替换，子分组同时是新建场次的发布受众（`audience_mode` + `subgroup_ids`）；admin 教师详情名单改矩阵 chip，可教组别改为「已分配列表 + 添加组弹窗」，删掉原左右双栏分配面板与保存前 diff 确认弹窗。`teacher_students` 语义收窄为「手动添加」，unbind 对组别派生学生返回 422 `ROSTER_DERIVED_STUDENT`。
- **F2 着重号未渲染**：`TeacherDetail.tsx` 说明文字里的字面 `**` 改为内联 `<Text weight="bold">`；不引入 Markdown 渲染，并在本文 8.1 补一条「管理端说明文字是纯 JSX，Markdown 不生效」防复发。
- **F3 教师列表冗余「详情」按钮**：删按钮，保留姓名列链接为唯一入口。
- **F4 密码可见性开关 + 弱密码审核（中等档）**：新增 `components/PasswordField.tsx`，替换登录/改密/账号三页共 **7 个** `type="password"` 输入框（小眼睛切明文）；`security.validate_new_password` 在保留原规则基础上新增「必须含字母与数字 / 不能只由 ≤2 种字符重复 / 不能含 ≥5 位连续字符 / 约 60 条常见弱密码表」，错误码仍是 422 `PASSWORD_POLICY`；前端 `passwordPolicy.ts` 与 `PASSWORD_RULES_TEXT` 逐字镜像。
- **F5 系统设置 + 审计日志可配置与导出**（拆 F5-1/F5-2 两条）：`SiteSetting` 加 `audit_enabled`（默认 1）与 `audit_retention_days`（NULL=永久）；关闭审计只停写不删历史；`prune_expired` 在启动、改保留期、以及 `log_audit` 每小时最多一次的惰性守卫里清理；新增审计 xlsx 导出（中文标签单点下发，与页面共用字典）；「主题设置」更名「系统设置」并加审计卡片；关闭后导航隐藏、路由重定向、接口 403 `AUDIT_DISABLED`。
- **F6 多题场次逐学生按题调分**：根因是入口链接取的是该生该场次**全局**最新一次提交（未按题分组）；新增 `GET /api/teacher/assignments/{aid}/students/{sid}/problems` 与页面 `/teacher/assignments/:id/students/:sid`，逐题给系统分/教师调分/有效分并可就地调分；「查看 / 调分」统一改名「调分」，提交下钻页去掉页底冗余返回。
- **F7 学生端「即将结束」**：`state` 由二态扩为 `ongoing | ending | ended`（阈值 `ENDING_SOON_HOURS`=3），两处状态徽标同步为三态（`ending` 红色），「我的场次」页顶部加红色 `MessageBar` 页内横幅（不弹窗，登录即见）。

验证：后端 pytest **346 passed / 1 skipped**（0.3.1 基线 307，本批次新增 39 例：弱密码 13、三态与受众/子分组/名单并集/删除保护/越权/按题调分/审计开关与导出等）；前端 `npx tsc --noEmit` + `npm run build` 0 错。暗色模式与逐页走查按项目既有流程（`backend/data/dev-ports.json` 取实际端口）。

---

## 10. 测试 fixture（backend/tests/fixtures/）

| 文件 | 用途 |
|---|---|
| ac.c | 正确解 |
| wa.c | 部分用例错 |
| tle.c | `while(1);` |
| mle.c | 循环 malloc 不释放 |
| re.c | 除零 / 空指针解引用 |
| ce.c | 语法错误 |
| fork_bomb.c | `while(1) fork();` |
| bigout.c | 输出 >100MB |
| trailing_space.c | 输出带行尾空格（验证 trim 模式应 AC、exact 模式应 WA） |
| float_fmt.c | 用 `%f` 输出而期望为 `%.2f` 风格（验证 float 模式应 AC） |

判题单测：对每个 fixture 断言 verdict；API 冒烟：login→建题→提交→查询 全链路脚本。

---

## 11. 安全总清单（非判题部分）

- 密码 bcrypt（cost≥12）；JWT HS256，密钥来自环境变量，12h 过期。
- 账号密码体系（0.3.0）：新建/统一重置一律初始密码 `12345678` + 首登强制改密（admin/教师/学生同等）；管理员重置发**一次性 8 位随机密码**（56 字符集，排除易混的 `0 O 1 l I o`），**7 天未改密即过期**；密码规则全角色统一（≥8 位、无空格、不得等于学号/姓名/旧密码/初始密码）。
- 未改密状态下后端业务 API 一律 403 `MUST_CHANGE_PASSWORD`（放行白名单见坑 25），前端另有路由级拦截，两道不可互相替代。
- 关键操作写只追加的审计日志（建号/重置/启停/组别成员/组-教师分配/名单拉移/导入/改名删组/手动调分），查看入口在 admin「审计日志」页，无任何修改或删除路径。
- 全库 SQLAlchemy ORM/参数化，禁字符串拼 SQL。
- React 默认转义 + react-markdown 不渲染原始 HTML，防 XSS。
- 上传仅接受文本代码；64KB 上限；不执行上传文件本身（只经 gcc，且只在 go-judge 内）。
- 接口鉴权 + 归属校验（6.1）；管理员接口双重角色校验。
- 登录失败不限流（校内规模），但提交限流：同用户同题 2s 内 1 次（429）。

---

## 12. 已知坑备忘（实现时逐条对照）

1. **CRLF**：Windows 编辑器 `\r\n` → 编译前与用例录入时统一归一化为 LF。
2. **gcc 必须带 `-lm`**：math.h 函数否则链接错误（假 CE）。
3. **浮点输出格式**：`%f` 默认 6 位小数；默认比对模式 trim 仍会 WA，此类题教师应选 float 模式——建题页要给提示文案。
4. **行尾空格**：学生 `printf` 带尾空格是常态，默认 trim 模式即为此。
5. **SQLite 并发**：开 WAL；写事务保持短小；judge 与 web 同库文件需同机同卷（Compose 内共享卷）。
6. **计量单位收敛**：go-judge 各版本返回的时间/内存单位可能不同（ns/bytes 或 ms/kb）——适配器 `gojudge.py` 内统一换算为 ms/KB 并写单测锁定，其余代码只信 RunResult。
7. **go-judge 版本漂移**：compose 固定版本 tag；升级前先跑 fixtures 全量回归。
8. **Windows 开发机**：go-judge 只能跑在 Docker Desktop 的 Linux 容器里；web/worker 可本机跑。
9. **时区**：库内 UTC、展示本地；时间窗比较一律用库内格式字符串比较（ISO 串可字典序比较）。
10. **删除题目前检查场次引用**：否则外键悬空，用 409 拦截。
11. **Docker Desktop 占内存**：开发机给 WSL2 设 `.wslconfig` 内存上限，避免吃满。
12. **暗色模式与硬编码色**：组件内禁止直接写 hex/rgb（品牌默认值只允许存在于 `site_settings`），一律走 Fluent 令牌或主题上下文；每个里程碑验收加一次暗色走查。
13. **背景图约束**：≤5MB、仅 jpg/png/webp；服务端不做二次压缩，上传页提示管理员先压缩；图片层为独立 fixed 层，不叠在组件背景上（否则透明度在暗色下会失真）。**背景图 URL 必须带版本参数**（`?v=<文件名>`）——URL 恒定的话，清除后重新上传会命中浏览器缓存，页面继续显示旧图（刷新才能恢复）。
14. **登录页背景独立**：登录页用 `LoginBackdrop`，不读 `bg_image_url` / `bg_opacity`；其余页面才走 `BackgroundLayers`。改登录页背景时别去动站点背景逻辑。
15. **超 5MB 的背景图**：压缩在浏览器本地做（`browser-image-compression`），服务端仍是硬限 5MB 且不做二次压缩；PNG/WebP 压不到目标大小时退化为 JPEG，**扩展名必须跟着改**（服务端按扩展名校验格式）。
16. **Fluent Dialog 的退出过渡**：`unmountOnClose` 依赖退出动画事件，个别环境下不触发会残留带 modal 语义的节点。需要可靠关闭时用「条件挂载 Dialog」而不是只切 `open` 属性（见 `SettingsPage` 的压缩确认框）。
17. **品牌色阶明暗方向**：Fluent 的 16 阶是 step10 最深、step160 最浅，`brand[80]` 为原色。`createBrandRamp()` 端点顺序写反**不会报错**，只会静默串色（亮色下 Spinner 圆环、浅色徽章底变近黑，按钮按下反而变浅）。改这个函数后必须亮/暗两套主题各走查一遍。
18. **Fluent `Text` 默认 `display: inline`**：`<Text as="h2">` 并不会自动换行，紧跟其后的说明文字会挤在同一行；而且它**不清除 UA 的 `h2 { margin-block: .83em }`**（24px 字号下实测上下各 19.92px），标题与副标题会被撑得很开。页面标题一律用 `components/PageHeader`（内部已 `margin: 0` + 副标题 `marginTop: spacingVerticalXXS`），别在页面里手写 h2 + `Caption1`。
19. **自定义 CSS 变量要写进 `:root`**：`FluentProvider` 只把主题令牌注入它自己的容器节点，`Dialog` / `Menu` 等 portal 渲染在 `body` 下取不到。项目自建的样式变量（`--ql-surface`、`--ql-selection`）由 `Layout` 用 `document.documentElement.style.setProperty` 写在 `:root`。
20. **`group` 是 SQL 保留字**：题目分组列必须叫 `group_name`，叫 `group` 时要和 `GROUP BY` 抢关键字，反引号包裹又极易在拼接处出错。表名 `groups` 没这个问题（本机 SQLite 3.35.5 实测直接 `CREATE TABLE groups` 可用，不需引号）。
21. **DataGrid 多选必设 `getRowId`**：不设时选中态按行 index 记录，一旦前端搜索/筛选过滤掉若干行，选中就会**错位**到别的数据上。设了 `getRowId` 后仍要注意：表头「全选」只作用于当前过滤后可见的行。
22. **blob 下载带不了 JWT**：`<a href="/api/...">` 直链不会带 `Authorization` 头，导出接口必须走 `fetch` 手动带头再 `URL.createObjectURL`。文件名要从 `Content-Disposition` 的 `filename*=UTF-8''` 解析并 `decodeURIComponent`，服务端还得同时给一个 ASCII 兜底 `filename="..."`（中文标题在部分浏览器会被丢弃）。
23. **openpyxl 的三处限制**：sheet 名 ≤31 字符且不得含 `[]:*?/\`（场次标题要先清洗）；读取用 `load_workbook(read_only=True, data_only=True)` 才不吃内存；**写入空字符串的单元格，存盘后读回来是 `None` 而不是 `''`**，测试断言空白列时要按 `None` 处理。
24. **批量重置密码的 bcrypt 计数**：cost=12 下单次哈希约 250ms，逐人算会让接口线性变慢。0.3.0 起分两种模式，别把它们混为一谈——**统一重置为初始密码**才是「整批共用一个 hash」（测试里 monkeypatch 计数守住「只算一次」）；**逐生随机密码**必须每人一个不同密码，人数 >20 时用线程池并行哈希（bcrypt 计算期释放 GIL，线程即真并行），但整批仍在一个事务里同步提交。
25. **强制改密拦截的白名单**：拦截做在 `get_current_user` 里最省事（所有业务依赖都过它），但 `/api/auth/password` 本身也走这个依赖——白名单漏掉它就把「解除拦截」那条路一起锁死了。必须放行 `/api/auth/login`、`/api/auth/me`、`/api/auth/password` 与匿名的 `/api/settings*`；前端路由级 Guard 的可达集合要和它一致，否则改密页自己进不去。
26. **临时密码凭证只能前端拼 CSV**：重置响应以 JSON 明细返回明文临时密码，前端用 Blob 下载（坑 22：`<a href>` 直链带不了 JWT，而且再请求一次就得把密码存到服务端）。有效期是 UTC 串，展示/导出前按本地时区换算（坑 9）。
27. **三层归属不要写成级联同步**：组别（层 1）、组-教师分配（层 2）、教师名单（层 3）各自独立。学生转组不动教师名单，撤销某教师的任教资格也不回收他已拉的学生（历史提交/成绩要留住），只是不能再从该组拉新人——自动增删看起来"更智能"，但会静默毁掉教师的手动名单。

---

## 附录 A 零基础名词表

| 名词 | 一句话解释 |
|---|---|
| 前端 | 浏览器里跑的界面（本项目 React + Fluent） |
| 后端 | 服务器上跑的程序，负责数据和逻辑（本项目 FastAPI） |
| API / 接口 | 前端和后端之间约定的请求地址与数据格式 |
| JSON | 接口传输数据的文本格式，长这样：`{"a":1}` |
| 数据库 / 表 | 持久化存数据的地方；表类似 Excel 的 sheet |
| ORM | 用 Python 类操作数据库、不手写 SQL 的工具 |
| 迁移（Alembic） | 数据库表结构的版本管理，可升级可回滚 |
| JWT / token | 登录后发的一张"电子门票"，之后每次请求带上证明身份 |
| 沙箱 | 把不可信代码关进去运行的隔离环境，限制资源、禁止联网 |
| go-judge | 开源沙箱执行服务：收请求→隔离运行→返回耗时/内存/输出 |
| 容器 / Docker | 轻量级隔离环境，镜像一致、随处可跑 |
| worker | 后台干活的进程（本项目指判题编排进程） |
| 端口 | 程序对外服务的"门牌号"，如 8000、5173、5050 |
| 代理（vite proxy） | 前端开发服务器把 /api 请求转给后端，避免跨域 |
| WAL | SQLite 的并发模式，读写互不阻塞 |
| 设计令牌 | Fluent 里预定义的颜色/圆角/间距变量，保证风格统一 |
| 适配器 | 把第三方服务的差异封装在一个文件里，换实现只改这一个文件 |

## 附录 B 命令速查

```bash
# 沙箱（已核实：criyle/go-judge:v1.12.3，容器内 REST 端口 5050；本机默认端口被保留时脚本会顺延）
docker run -d --name go-judge --privileged --shm-size=256m -p 127.0.0.1:5050:5050 criyle/go-judge:v1.12.3

# 后端
cd backend && pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --reload --port 8000
python -m app.judge.worker
python scripts/init_admin.py

# 前端
cd frontend && npm install && npm run dev

# 判题单测
cd backend && python -m pytest tests/ -v

# 部署（M4 详见 runbook）
docker compose -f docker/compose.yml up -d --build
```

---

*文档结束。变更历史：v1.0（2026-09-17）初稿，需求定稿；v1.1（2026-09-17）开源优先改造：沙箱改 go-judge、补 Alembic/CodeMirror/recharts/react-markdown/dayjs 选型、记录整套 OJ 评估结论；v1.2（2026-09-17）定名"青蓝 QingLan"，仓库名 qinglan，补 Logo 文案约定；v1.3（2026-09-17）新增暗色模式全适配与管理员主题配置（主题色/背景图/透明度，site_settings 表与 /api/settings 接口族、chroma-js 色阶）；v1.4（2026-09-17）新增《部署操作指南.md》运维指南，M4 交付物补 .env.example/backup.sh/reset_password.py；v1.5（2026-09-17）按 M1 任务 6 回写 go-judge 核实结果：仓库实为 criyle/go-judge（EvaluatorOJ 已 404），REST 端口 5050（5051 为 gRPC 默认关闭），固定镜像 v1.12.3，/run 字段 cpuLimit(ns)/memoryLimit(bytes)/copyIn/copyOut 与响应 status 字符串枚举（2.1/3.2/7.4/附录 B 已同步）；v1.6（2026-09-18）M1–M3 验收全过 + M4 WSL2 部署演练：派生镜像 Dockerfile.gojudge 补 gcc、judge 网络 internal、dockerd cgroupns=host、web 容器托管前端（7.4 已回写，指南 §4/§6 已更新）；v1.7（2026-09-19）本机 Docker Desktop 实跑复核：① 更正 7.4 的 cgroup 结论——compose 服务级官方键是 `cgroup: host`（原记"compose 不支持 cgroupns 键"属键名找错），compose.yml 已加该行，dockerd 全局参数降为旧版本兜底；② 判题限额经 go-judge 层实测生效（TLE/MLE/编译错误 stderr 均可取），应用层端到端提交仍未验证；③ 新增一键脚本 `scripts/start-local.ps1` / `stop-local.ps1`（PowerShell 7）；④ 《本地运行手册.md》合并进《部署操作指南.md》作为第一部分，该文件已删除；v1.8（2026-09-19）两处背景相关变更：① 修复"清除后重新上传背景图仍显示旧图"——根因是背景图 URL 恒定（`/api/settings/bg_image`）+ 响应只有 ETag/Last-Modified 而无 Cache-Control，浏览器在该 URL 的有效期内直接复用缓存；现改为 URL 带 `?v=<文件名>` 版本参数，并按版本是否匹配返回 `immutable` / `no-store`（6.2 已补该接口行，坑 13 已补结论）；② 登录页改为独立品牌色动态背景（`LoginBackdrop`，不再使用站点背景图与透明度），亮/暗/窄屏三态自适应并支持 `prefers-reduced-motion`（1/8.1/8.2/坑 14 与 M3 验收项已同步）；v1.9（2026-09-19）四项后台/账号能力：① 背景图超 5MB 不再直接报错，弹 Fluent Dialog 询问是否压缩，确认后用 browser-image-compression 在浏览器本地压到最长边 2560px/≤2MB（PNG/WebP 压不到目标则退化为 JPEG），服务端 5MB 硬限不变；② 新增「亮色/暗色使用不同背景图」开关（`site_settings.bg_dual` + `bg_image_path_dark`，关闭时两模式共用主图，暗色槽位为空回退主图）；③ 新增「主题色从背景图提取」开关（`brand_color_source`，extractBrandColor 采样+色相投票，可撤销、手改颜色自动回落 manual）；④ 新增自助改密码：`POST /api/auth/password` + `/account` 页面，入口在右上角头像菜单「账号设置」；Alembic 0002 迁移、2.1 选型表、3.x DDL、6.1/6.2 接口表、8.1/8.2、坑 15/16 与 M3 验收项均已同步；v2.0（2026-09-20）0.2.0 批次八项（M5）：① 404 页按 Fluent Design 2 重写（居中式提示页不套 PageHeader、卡片毛玻璃走 `.ql-has-bg .fui-Card`）；② 学生分组改为**多组模型**（新表 `groups` + `group_members`，管理员维护、全站共享）+ 管理员批量加入/移出分组、批量重置密码、批量启停；③ 教师端新增「学生」页（`/teacher/students`，只读组定义 + 批量调整自己学生的分组）；④「场次管理」改名「发布」并加页内 Tab（全部/作业/考试），教师侧 mode 文案统一为「考试」（学生端仍为「测试」）；⑤ 教师导出场次成绩 xlsx（`GET /api/teacher/assignments/{id}/export?tz_offset=`，openpyxl + StreamingResponse）；⑥ 题库支持搜索、`group_name` 自由文本分组、行内删除；⑦ 学生导入升级为 `.xlsx`/`.txt`/`.csv` 三列 `学生ID|姓名|组别`（多组分隔符、组不存在自动建、空行静默跳过，新增 422 `INVALID_XLSX` / `INVALID_CSV_ENCODING`）；⑧ 题目编辑草稿存后端（`problems.draft`/`draft_saved_at` + `PUT|DELETE /api/teacher/problems/{id}/draft`）。Alembic **0003** 迁移（`0003_groups_and_problem_group.py`，含真实数据副本上 upgrade→downgrade→upgrade 可逆验证）、openpyxl 依赖、§1.3 角色表、§4 目录树、§5 DDL、§6.2 接口表、§8.1/8.2、坑 20–24 与新增 M5 均已同步；后端 `pytest tests/` 207 passed / 1 skipped（新增 `tests/test_groups_api.py` 44 例、`tests/test_import_export.py` 48 例），前端 `npm run build` 通过，浏览器走查全链路通过；v2.1（2026-09-20）0.2.1 批次：四项 UI 走查修复——① 404 页标题/描述居中（根因：Fluent `Text` 自带 `textAlign:'start'` 打断父级继承，改在 Text 上显式 center；卡片 `maxWidth` 480→560、内边距升至 XXXL）；② 发布页 Tab 与大标题左对齐（负 margin 抵消小号 Tab 的 MNudge 内边距）；③ 删除确认按钮固定红系不随品牌色（新增 `components/dangerStyles.ts`，ProblemList/ProblemEdit 两处对话框接入）；④ 存为草稿对话框：行首全角引号视觉缩进改直陈文案、「存为草稿并退出」精简为「存为草稿」、「直接退出」改红描边红字。附带：api client 对非 JSON 错误体容错（改报「请求失败（HTTP n）」而非裸 SyntaxError）、`BulkActionBar` 去外描边；§8.1 新增「危险操作按钮固定红系」与「对齐像素自查」规范条目（node-repl 纯 zlib PNG 解码器量截图墨迹边缘 + 五条观感错位根因清单）；v2.2（2026-09-20）0.2.2 批次：五项 UI 走查修复——① 标识（Badge）全站改 `size="large"`（16px/10px → 24px/12px），状态类再叠 `.ql-badge-status` 的 `min-width: 56px`，让「启用 / 已停用 / 已放出 / 未放出」在同一列里等宽；② 表头文字在本列水平居中（`.fui-DataGridHeaderCell .fui-DataGridHeaderCell__button{justify-content:center}`，`text-align` 对那个 flex 槽无效）；③ 草稿恢复弹窗文案精简：标题问「是否使用草稿内容？」、说明只留保存时间、按钮改「放弃 / 是」；④ 全站去掉 MessageBar 的 1px 描边；⑤ 404 页改三段式网格，状态码精确落在卡片垂直中线上，罗盘与文案各贴一侧（间距 16→8），说明文案收成一行。①④三条全局覆盖的实现约定（双类名提特异性）已回写 §8.1；v2.3（2026-09-20）0.2.3 修复：亮暗分图 + 「主题色从背景图提取」时，切亮/暗模式主题色不跟随各自的背景图。根因是站点**只有一个 `brand_color`**——无论上传亮图还是暗图都覆盖同一字段，运行时 `App` 只是拿同一个色构建的 ramp 在 `themes.light/dark` 间切换。现新增 `site_settings.brand_color_dark`（Alembic **0004**，可空、无 `server_default`，旧行 NULL 即"沿用亮色"，升级前后行为一致）：抽色按上传槽位分别写 `brand_color` / `brand_color_dark`，打开开关时对两张图各提取一次；前端 `theme.ts:resolveBrandColors()` 按与 `resolveBgUrl()` 相同的条件（`bg_dual=1` 且暗图存在）决定暗色用哪个色，`buildThemes()` 拆成亮/暗两条 ramp；管理页仅在两张图都在用时显示「暗色模式品牌色」取色器，「撤销取色」支持一次回退两个槽位。§5 DDL 与迁移说明、§6.2 接口表、§8.1/8.2、M3 验收项已同步；后端 207 passed / 1 skipped，前端 `tsc --noEmit` + `npm run build` 通过，迁移在真实数据副本上验证 upgrade→downgrade→upgrade 可逆；v2.4（2026-09-21）**0.3.0 批次（M6 权限与密码体系）计划定稿**：入库《权限与密码体系修改意见.md》v1.1 审查修订版（编号规则 PW/AU/BD/LI/SC/GR/IM/DM/QA + 机器可读策略块，本批次范围内为实施权威），§9 新增 M6 里程碑（任务清单 10 项 + QA-01~21 验收）；§0.1 新增规则 8「每完成一个独立更新提交一个本地 commit、不推送」；批次版本号固定 0.3.0。审查拍板要点：PW 方案同等适用于教师账号与种子 admin；存量账号迁移后一律强制改密；DM-05 `password_updated_at` 升 P0（随机密码 7 天过期锚点）；随机密码字符集修正为 56 字符；批量重置阈值 20（统一 12345678 / 逐生随机二选一，随机 >20 线程池并行）；导入第 4 列「教师」写入 teacher_groups；新增 PW-09 凭证 CSV 导出与 AU-06 admin 审计页；删除 admin 直绑写接口与教师改组接口（GET 只读保留）。本条目仅为计划，代码未动，§5/§6.2 等现状章节待 M6 完成后回写；v2.5（2026-09-21）**0.3.0 批次 M6 权限与密码体系实施完成**：① 密码体系（PW-01~09）——新建师生账号与种子 admin 统一初始密码 `12345678` + 首登强制改密（`AccountCreate` 去 password 字段），管理员重置发**一次性 8 位随机密码**（56 字符集、排除 `0 O 1 l I o`）且 7 天过期（锚点 `users.password_updated_at`，NULL=不过期），密码规则全角色统一（≥8 位、无空格、不得等于学号/姓名/旧密码/初始密码），批量重置按阈值 20 分「统一 / 各自随机」两模式、随机且 >20 走 8 线程并行 bcrypt，重置与「导出初始密码表」均以 JSON 明细由前端拼 CSV；② 审计（AU-01~06）——新表 `audit_log` + 唯一入口 `services/audit.py::log_audit`（只 add 不 commit，与业务同事务），启停逐人一条、建号/重置/导入/组员/改名删组/组分配/名单拉移/调分/改密全部留痕，`GET /api/admin/audit_logs` 与 admin 页 `/admin/audit`（只读，无改删入口）；③ 学生归属三层化（BD/GR）——组别与「组-教师分配」（新表 `teacher_groups`）唯一写者是 admin，教师只能在自己可教的组内拉/移**自己的名单**，删 `PUT /admin/teachers/{id}/students`（GET 保留只读）、`POST /teacher/students/group_members`、`GET /teacher/groups`，新增 `GET /teacher/classes` 与 `bind_from_class`/`bind`/`unbind`（整批校验、幂等、不泄露名册）；④ 列表（LI-01/02）`?q=&group_id=&must_change=` 与「未改密」徽标；⑤ 成绩（SC-01~04）新增 `total_score` 与逐题有效分、导出「最高单题分」更名 + 总分列 + 动态每题列、调分边界 `0~full_score`（`null` 仍=撤销）、rejudge 清 `manual_score*`；⑥ 导入（IM-01/02）第 4 列「教师」幂等写 `teacher_groups`（教师名不存在或教师列有值而无组别 → 失败行），旧三列行为不变。迁移 **0005**（DM-01~05）已在真实数据副本上验证可逆、本机开发库已升级（备份 `cg.db.bak-20260921-mig0005`，存量 14 账号一律强制改密）。后端 `pytest tests/` **290 passed / 1 skipped**（新增 `test_password_flow.py`、`test_admin_password_audit_api.py`、`test_teacher_binding_api.py`），前端 `npm run build`（含 tsc）0 错误、version 固定 **0.3.0**；QA-17 的 500 人随机重置实测 **11.4s**。§1.3/§4/§5/§6.2/§8.1/§8.2/§9/§11/§12（坑 24 改写、新增坑 25~27）已回写，《权限与密码体系修改意见》升 **v1.2**（新增 §12 实施结果与 6 条实现取舍），《部署操作指南》升 **v1.3**（§4.4 重写、FAQ 补 `PASSWORD_EXPIRED`），README 同步。**本批次收尾清单（下次会话）**：(a) 按规则 8 补 10 个本地 commit——auto 权限模式会拦 `git commit`，脚本已备好 `~/.qoder-cn/projects/D--somehow-GitHub-qinglan/m6-commits.sh`；(b) 浏览器手工走查强制改密页/凭证 CSV/批量询问 Dialog/组分配 diff/审计页/拉学生页与暗色逐页复核（QA-09/10/13/19/20/21 的观感部分，静态核查已确认无硬编码色）；(c) 遗留缺陷待修：教师逐学生成绩页下钻链接恒空（后端 `StudentRowOut` 从不返回前端声明的 `last_submission_id`）；v2.6（2026-09-21）**M6 测试收尾（本批次第 9/10 项的未完部分）**：① 收尾清单 (a) 已完成——10 个本地 commit `916318e`…`89b17a2` 全部落库、工作区干净、仍未推送远端；② (b) 浏览器逐页走查 + 暗色扫描已完成，取证写在 §9 M6 的「QA-01/02/…/20 浏览器走查」与「QA-21 暗色逐页扫描」两行（隔离快照库 `backend/.tmp-verify/cg.db` + 8010 后端 + 临时 `frontend/vite.walkthrough.config.ts` 走 45173，用完即删；两个新踩的坑：前端是 **HashRouter**，深链必须写 `/#/…`；Fluent `SpinButton` 不吃合成 `input` 事件，得用真按键）；③ 补上 QA-11/QA-12 的自动化缺口——此前只有静态核查与文档描述，**没有任何 API 用例**，新增 `tests/test_score_adjust_rejudge.py` 16 例（边界 0/满分、四类越界 422 且零副作用、无满分配置 422、null 与省略字段都撤销且清锚点、审计 old/new、他人场次 403、rejudge 作废调分与逐测试点结果）；④ 全量回归 `306 passed / 1 skipped`（M6 实施当轮为 290），`npm run build`（含 tsc）0 错误；⑤ §9 M6 小节标题由「待开工」改「已完成」、§0 状态行同步、QA-10 口径行补浏览器复核结论；⑥ 走查继续把 QA-11 与 QA-18 的浏览器部分补完：提交详情页调分「保存→撤销」全链路生效、库里 3 条 `score_manual_adjust` 的 `old/new/full_score` 与屏显逐条对得上，新建教师/新建学生表单各只有 2 个文本框、**0 个 password 输入**；由此抓到 1 个 pytest 拦不住的 **P2 前端缺陷**——`components/NumberInput.tsx` 手输数值不生效、只能 ±箭头（详见「本批次遗留」末条）；(c) 遗留缺陷（含这条新增）仍在，另案处理；**v2.7（2026-09-23）0.3.1 批次 M7 本机开发端口自适应**：定位根因为 Windows 保留端口段（注册表 `MaxUserPort=15000` 把 TCP 动态端口池压到 1024-15000，WinNAT/Hyper-V 再整段划走）导致 5050 与 5173 绑定 EACCES；`start-local.ps1` 改为默认端口优先、失败时读 `netsh excludedportrange` 整段跳过顺延，实际端口写入 `backend/data/dev-ports.json` 并由 `config.py` 与 `vite.config.ts` 共读（手动起服务不必传端口）；沙箱容器一律沿用已在运行的端口、`docker start` 失败才换端口重建，并修掉 `docker start` 不查退出码造成的假 `[ok]`；实测顺延到 5341/5342 全链路 200、10 项真实判题首次在本机执行通过、后端 316 passed、前端 build 0 错；§0 状态行与《部署操作指南.md》§0/L6、README、本文 3.2/7.7/附录 A/B 端口口径同步；v2.8（2026-09-23）**0.3.1 修复批次（FIX-1~7，用户验收 6 条）**：① FIX-1 后端 `stats.student_rows` 补 `last_submission_id`（`max(mine, key=lambda s:(submitted_at, s.id))`，与 `last_submitted_at` 同源同刻、不新增查询）、`StudentRowOut` 增该字段，逐学生成绩「查看 / 调分」下钻链接从此可达，`export.py` 按显式键取数故 xlsx 列布局不变，新增 `test_import_export.py::test_drill_link_uses_latest_submission`；② FIX-2 `components/NumberInput.tsx` 的 `onChange` 增 `displayValue` 兜底 `parseFloat`，受控 `SpinButton` 手输数值失焦不再被清空（±箭头/Enter/撤销路径不变），越界仍由服务端 422 `MANUAL_SCORE_OUT_OF_RANGE` 兜底（默认不带 `max`/`full_score` 增强，见 Q2）；③ FIX-3 审计日志页删动作/对象类型徽标下的原始 code、新增「操作者 ID」独立列（已删除账号显示 `—`）、刷新按钮 `alignSelf:flex-end` 与筛选下拉同排对齐；④ FIX-4 四处管理表（admin 教师管理/学生管理/教师详情名单、教师端我的学生）删「改密状态」列与「未改密/已改密」徽标、删学生管理改密筛选（`PasswordFilter`/state/`mustChange`/fetch 入参）与教师端「未改密」提示句，后端 `must_change_password` 字段/PW-02 拦截/`?must_change=`/强制改密路由全部保留（`types.ts` 字段声明保留），批量重置弹窗 `alreadyChanged` 与「导出初始密码表」CSV 不动；⑤ FIX-5 `components/time.ts` 新增 `windowPhase`、发布列表在时间窗后加「状态」列（未开始蓝/进行中绿/已结束灰，纯时间窗前端推导，不做 ticking；blue 用 `colorPaletteBlueForeground2`，本 Fluent 版本无 `…Foreground1`）；⑥ FIX-6 下钻列头改「查看 / 调分」、链接文案改「查看 / 调分」；⑦ FIX-7 逐学生成绩已交学生姓名去 `semibold` 统一字重（「未交」红徽标保留）。§9 M6「本批次遗留」删除下钻链接与调分框两条（已由 FIX-1/FIX-2 修复）；《部署操作指南.md》§5 验收清单两条改密徽标表述改写（0.3.1 起不再展示、仅存 API）、补手动调分页唯一入口路径行，本文 §6.2 接口表 LI-02 注、§11 页面表 `/teacher/submissions/:sid` 入口与三处管理表改密列移除同步。回归：前端 `npm run build`（含 tsc）0 错，后端 `pytest tests/` **317 passed / 1 skipped**（较 0.3.0 收尾 +1 例）；version 仍固定 0.3.1 批次不动 `package.json`。v2.7（2026-09-23）0.3.2 功能批次回写（权威实施计划见仓库根《修复计划-0.3.2.md》，7 项功能 / 2 次迁移 / 14 条 commit）：F1 名单口径改为「可教组别成员并集 ∪ 手动添加」（`stats.bound_students` 单一口径，撤销组别即刻生效）＋教师子分组＋发布受众（迁移 0007 建 4 表并给 `assignments` 加 `audience_mode`）＋admin 矩阵名单与「添加组」弹窗＋教师端子分组页与组别撤销弹窗（`teacher_notices`）；F2 说明文字去字面 `**`（§8.1 补「管理端文字是纯 JSX、Markdown 不生效」）；F3 删教师列表「详情」按钮；F4 密码小眼睛（登录/改密/账号共 7 处）＋弱密码中等档审核（字母数字双含、拒全同/连续/常见弱密码）；F5 系统设置与审计可配置/导出（迁移 0006 加 `audit_enabled`/`audit_retention_days`，`prune_expired`、`AUDIT_ACTION_LABELS` 单点中文名、`/admin/audit_logs/export`、关闭后 403 `AUDIT_DISABLED` 与导航/路由隐藏）；F6 多题场次逐学生按题调分（新端点 + `/teacher/assignments/:id/students/:sid`，「查看 / 调分」统一改名「调分」，提交下钻页删页底冗余返回）；F7 学生端 `state` 扩为三态并加「即将结束」页内红色横幅（阈值 `ENDING_SOON_HOURS`=3）。同步：§5 枚举与 DDL、迁移说明、§6.2 接口清单（教师子分组/提示、按题调分、审计导出与 403、设置读写、受众与 `source` 字段、密码规则加严）、§8.1/§8.2（新增按题调分路由、「系统设置」更名、审计页导出与关停）、§9 新增 M8、《部署操作指南.md》调分入口行。回归：前端 `npm run build`（含 tsc）0 错；后端 `pytest` **346 passed / 1 skipped**（较 0.3.1 基线 307 增 39 例）；两支迁移在库副本上 upgrade→downgrade→upgrade 往返无损、13 行 assignments 保持 `'all'`；version 随 `chore(release)` 更新为 `0.3.2`。*
